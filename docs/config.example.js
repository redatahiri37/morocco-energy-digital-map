// Copy this file to `config.js` and paste your own Mapbox token.
// `config.js` is gitignored — it must never be committed.
//
// Get a token at https://account.mapbox.com/access-tokens
// For production, restrict the token to your GitHub Pages URL in the
// Mapbox dashboard (Settings → Tokens → URL restrictions).

window.APP_CONFIG = {
  mapboxToken: "pk.REPLACE_ME",
  defaultCountry: "morocco"
};
