# Council sitting — 2026-08-25

## Sitting — 12:09 (Europe/Paris)

REPORT-only sitting. First sitting in 4 days — the last recorded sitting
is 2026-08-21 02:55 (PR #43, unmerged). `main`'s tip is still `9a4d5a4`,
unchanged since 2026-08-04. Nothing under `docs/**` differs from what PR
#43 already reconstructed, so this sitting pulls that docket forward
rather than reconvening the six seats from scratch — regenerating would
reproduce the same 17 objectives verbatim (COUNCIL.md §8, "pull, don't
regenerate"). Two things are new: the gap itself, and its cause.

### State established before convening
- `git log` tip on `main`: `9a4d5a4`, unchanged since 2026-08-04 14:15.
  `git status` clean on a fresh branch off `main`.
- Freshly re-verified rung-1/2 preconditions directly: `node --check
  docs/app.js` and `docs/countries.config.js` both clean; all 9
  `docs/data/morocco/*.geojson` files parse as valid JSON via a fresh
  Python `json.load` pass. `docs/index.html:122` and `docs/app.js:968,997`
  still read `reda.tahiri@example.com`. Nothing is down, nothing is
  broken, no published fact was found wrong. No rung-1/2 candidate
  pre-empts the docket.
- Re-listed open PRs via the GitHub API: **still 42 open PRs, #2–#43**,
  identical set to what PR #43 itself found — none merged, closed, or
  updated since 2026-08-21. The most recent, PR #43 ("Council sitting —
  2026-08-21 02:55"), is 4 days old. No PR #44 existed before this
  sitting created one.
- **New finding — why the gap:** `list_triggers` on the Routine driving
  these sittings shows `cron_expression: "0 10 * * 1-5"` (once per
  weekday, 10:00 UTC), `updated_at: 2026-08-23T14:40:08Z`. COUNCIL.md §4
  and the Chair's own briefing describe a 5-hourly cadence ("roughly five
  happen per day"); the actual configured cadence, as of 2026-08-23, is
  materially slower. That accounts for no sitting over the weekend
  (2026-08-22/23). No branch or PR exists for 2026-08-24 (a weekday under
  the new cron) either — the Monday firing is unaccounted for from what's
  readable in this session; not investigated further, as it's outside the
  map remit and Reda is better placed to check the Routine's own history.
- Read PR #43's minutes and its reconstructed Board directly (`git show
  origin/council/2026-08-21-0055:council/2026-08-21.md` /
  `:council/BOARD.md`) rather than trusting `main`'s frozen tree — same
  approach PR #43 itself used against PR #42, for the same reason.

### Docket (pulled forward from `council/2026-08-21-0055`'s Board —
unchanged, since nothing in the repo has moved since it was written)

**frontend-engineer** — OBJ-1 (Shipped 2026-08-04, `c808b1e`, merged),
OBJ-2 (loading state for `#layerList`/`#kpiGrid`, S), OBJ-3 (topbar
overflow handling ≤375px, S)

**coord-validator** — OBJ-1 (vintage field on `industrial.geojson`, S —
needs real external research before an unattended sitting fills in a
year; a wrong vintage is itself a rung-1 violation), OBJ-2 (sample-verify
`national-hv` + `transmission-lines` — **resolved 2026-08-20: PASS, 0
FAIL, 0 UNVERIFIED**; new finding of 57% corridor overlap with
`transmission-lines` folded into map-debugger-2), OBJ-3 (remove/mark
deprecated `grid-lines.geojson`, S, ready)

**map-debugger** — OBJ-1 (dead mailto, S, blocked pending Reda confirming
a real address), OBJ-2 (wire `national-hv` + `transmission-lines` into
the map, L, unblocked on coordinates, now blocked on a rendering-dedup
design call), OBJ-3 (visible MapLibre runtime-error state, S), OBJ-4
(light-theme topbar contrast, S, ready, no blockers)

**map-tester** — OBJ-1 (orphan-data check, S), OBJ-2 (write the GO
evidence bar, S), OBJ-3 (popup field-mapping audit, S)

**platform-engineer** — OBJ-1 (**SHIP-ruled 2026-08-20 23:00**, CI
validation workflow, fully built and GO'd — see PR #42, still unmerged),
OBJ-2 (uptime check against the live map URL, S), OBJ-3 (`docs/_headers`
cache-control for `docs/data/*.geojson`, S)

**security-engineer** — OBJ-1 (SRI hashes on the MapLibre tags, S), OBJ-2
(disclose third-party requests on load, S), OBJ-3 (CSP via
`docs/_headers`, S)

### Ruling

**Rung check:** no rung-1 or rung-2 candidate is live on `main`. Nothing
new pre-empts the docket.

SHIP:    **none.** The 24h clock has clearly reset since PR #42's SHIP
         ruling (2026-08-20 23:00 — 4 days ago), so the budget mechanism
         itself does not block a new SHIP this time, unlike PR #43's
         reasoning. The Chair withholds one anyway, on a different
         ground: 42 already-ruled, already-gated branches sit unmerged,
         including one (PR #42) that is a complete, GO'd, S-sized CI fix
         ready to merge today with zero further work. Nothing this
         sitting could rule and execute reaches a regulator or DC
         developer faster than that PR already sitting untouched — ruling
         a fresh SHIP produces PR #44 in the same pile, not a delivered
         capability. The bottleneck is entirely a human merge action, not
         a shortage of ready work. Manufacturing more of it does not help.
REPORT:  **none run this sitting**, same reasoning as PR #43: both
         standing candidates (coord-validator-2, platform-engineer-1
         scoping) are already resolved on the reconstructed Board and
         nothing has changed to warrant re-running either. This sitting's
         effort went to confirming state and diagnosing the 4-day gap
         instead.
Vetoed:  none — no seat vetoed anything; nothing new was proposed to test
         against a veto.
Docketed to council/BOARD.md: unchanged from `council/2026-08-21-0055`'s
Board — OBJ-frontend-engineer-2, OBJ-frontend-engineer-3,
OBJ-coord-validator-1, OBJ-coord-validator-3, OBJ-map-debugger-1 (blocked
pending human), OBJ-map-debugger-2 (blocked pending dedup design),
OBJ-map-debugger-3, OBJ-map-debugger-4, OBJ-map-tester-1, OBJ-map-tester-2,
OBJ-map-tester-3, OBJ-platform-engineer-2, OBJ-platform-engineer-3,
OBJ-security-engineer-1, OBJ-security-engineer-2, OBJ-security-engineer-3

(No IDs minted this sitting. Ledger unchanged: frontend-engineer 4,
coord-validator 4, map-debugger 5, map-tester 4, platform-engineer 4,
security-engineer 4.)

### Outcome
Commit:  none — REPORT-only sitting, nothing executed against `docs/**`.
Gates:   n/a — no change proposed, nothing to gate.
Carried: **Top priority, still needs Reda, now 4 days older:** 42 open
         council PRs (#2–#43) sit unmerged against `main`, dating back to
         2026-08-04. `main` has not moved since `c808b1e`. The
         fully-built, double-gated CI workflow in PR #42 (2026-08-20
         23:00) is the cheapest possible first merge — smallest,
         cleanest, zero product-surface change, closes the rung-2 gap
         ("the pipeline can ship something broken") that justified
         ruling it in the first place. Recommend, unchanged from
         2026-08-21: merge PR #42 first, then triage the remaining 41 for
         current vs. superseded (many will conflict with each other once
         the first lands, since all 42 branch from the same stale
         `main`).

         **New this sitting:** the Routine's cron was changed 2026-08-23
         to `0 10 * * 1-5` (once/weekday), a much slower cadence than
         COUNCIL.md §4 documents. If this was Reda's deliberate response
         to the 2026-08-21 recommendation to pause the aggressive cadence
         until the backlog clears, that's the right call — but COUNCIL.md
         §4's "roughly five happen per day" framing for the SHIP budget
         is now stale and should be corrected by whoever next edits the
         constitution, to avoid a future Chair reasoning about the budget
         from a cadence that no longer exists. Recommend checking why the
         Monday 2026-08-24 slot didn't fire, independent of the map work
         itself.

         Once `main` catches up, the next-ranked SHIP-eligible items are
         still OBJ-map-debugger-4 (light-theme contrast, S, ready) and
         OBJ-coord-validator-3 (deprecated `grid-lines.geojson`, S,
         ready). OBJ-map-debugger-1 stays blocked on Reda confirming a
         real contact address; OBJ-coord-validator-1 needs external
         research before an unattended sitting should fill in a vintage
         year; OBJ-map-debugger-2 needs a map-debugger dedup design call.

         Next sitting must re-check `main`'s tip and the open-PR count
         before ruling on the SHIP budget — do not assume this sitting's
         read is still current.
