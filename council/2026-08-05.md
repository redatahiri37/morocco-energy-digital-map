# Council sitting — 2026-08-05

## Sitting — 10:15 (Europe/Paris)

Third-ever sitting, first of 2026-08-05. Time derived from the established
5-hourly chain anchored at the last sitting (2026-08-04 14:15 → 19:15 →
00:15 → 05:15 → **10:15**). `git log` tip unchanged since the last sitting:
`9a4d5a4` (merge of PR #1, carrying `c808b1e`/`4100942`). Working tree
clean. Full detail for every objective lives on `council/BOARD.md`; this
file records only the ruling.

### State established before convening
- `git log` tip unchanged: `9a4d5a4`. Nothing has landed since the
  2026-08-04 14:15 sitting shipped `c808b1e`.
- Rung-1/2 pre-empt check: `node --check docs/app.js` and
  `docs/countries.config.js` both clean; all 9 `docs/data/morocco/*.geojson`
  files parse as valid JSON. Nothing is down, nothing is broken. No rung-1/2
  candidate exists this sitting.
- **SHIP budget checked directly against the actual commit timestamp, not
  assumed from the calendar date.** COUNCIL.md §4 grants "at most 1 [SHIP]
  per rolling 24 h, across all sittings" — a rolling window, not a
  calendar-day reset. The last ship's commits landed 2026-08-04
  14:35:33–14:37:08+02:00 (`c808b1e`/`4100942`). This sitting, at 10:15 the
  next day, falls **inside** that 24-hour window (~19h40m elapsed). The
  budget does not reopen until roughly 14:35–14:37 Europe/Paris today. The
  fact that `council/2026-08-05.md` did not exist before this sitting is
  true but irrelevant under a rolling-window reading. **This sitting is
  REPORT-only regardless of docket ranking.**
- Re-verified two standing candidates directly rather than trusting prior
  minutes: `docs/index.html:122` and `docs/app.js:968,997` still read
  `mailto:reda.tahiri@example.com` (RFC 2606-reserved placeholder domain).
  `docs/brand.css:32-36` still sets `.topbar .ghost-btn,.topbar
  .icon-btn{color:rgba(255,255,255,.85)}` unconditionally, with no
  `[data-theme="light"]` override — the light-theme contrast bug is still
  live.
- New observation this sitting, frontend-engineer remit: `docs/app.js:255-257`
  catches any per-layer fetch failure, `console.warn`s it, and substitutes
  `layerData[L.id] = {features:[]}`; `renderLayerList()` (`app.js:289`)
  renders `<span class="layer-count">${fc.features.length}</span>`, which
  reads as `0` in both the "legitimately empty" and "fetch failed" cases —
  visually indistinguishable. Not previously on the Board. Minted as
  `OBJ-frontend-engineer-4`.

### Docket (18 objectives filed this sitting, grouped by seat)

**frontend-engineer**
- OBJ-frontend-engineer-2 | Add a visible loading state to `#layerList`/`#kpiGrid` while `loadAllData()` awaits its fetches — reaffirmed, unchanged
- OBJ-frontend-engineer-3 | Give `.topbar` explicit overflow handling at ≤375px — reaffirmed, unchanged
- OBJ-frontend-engineer-4 | Distinguish a failed per-layer fetch from a legitimately empty layer in `loadAllData()`/`renderLayerList()` — **new this sitting**, minted OBJ-frontend-engineer-4

**coord-validator**
- OBJ-coord-validator-1 | Add `vintage` field to all 13 `industrial.geojson` features — reaffirmed, unchanged
- OBJ-coord-validator-2 | Sample-verify `national-hv.geojson` (947) + `transmission-lines.geojson` (541) — reaffirmed, unchanged
- OBJ-coord-validator-3 | Remove or mark deprecated `grid-lines.geojson` (11 features, unreferenced) — reaffirmed, unchanged

**map-debugger**
- OBJ-map-debugger-1 | Fix dead mailto targets (`index.html:122`, `app.js:968,997`, still `example.com`) — reaffirmed, unchanged, top-ranked SHIP candidate once budget reopens
- OBJ-map-debugger-4 | Fix light-theme topbar button contrast in `docs/brand.css:32-36` — reaffirmed, unchanged
- OBJ-map-debugger-3 | Surface a visible error state for mid-session MapLibre runtime failures — reaffirmed, unchanged
- (OBJ-map-debugger-2 — L, wiring `national-hv`+`transmission-lines` into the map — stays parked in Docketed, not re-filed this sitting; still blocked on OBJ-coord-validator-2)

**map-tester**
- OBJ-map-tester-1 | Orphan-data check across `docs/data/morocco/*.geojson` vs load-path references — reaffirmed, unchanged
- OBJ-map-tester-2 | Write down the required evidence bar for a release-gate GO — reaffirmed, unchanged
- OBJ-map-tester-3 | Audit popup field-name mapping against each source file's actual keys — reaffirmed, unchanged

**platform-engineer**
- OBJ-platform-engineer-1 | Add a minimal CI check on push to `main` (no build step, no bundler) — reaffirmed, unchanged
- OBJ-platform-engineer-2 | Wire an uptime check against the live map URL — reaffirmed, unchanged
- OBJ-platform-engineer-3 | Add `docs/_headers` with cache-control for `docs/data/*.geojson` — reaffirmed, unchanged

**security-engineer**
- OBJ-security-engineer-1 | Add Subresource Integrity hashes to the MapLibre `<script>`/`<link>` tags — reaffirmed, unchanged
- OBJ-security-engineer-2 | Disclose the third-party requests the page makes on load — reaffirmed, unchanged
- OBJ-security-engineer-3 | Add a Content-Security-Policy via `docs/_headers` — reaffirmed, unchanged

### Ruling

SHIP:    none — the rolling 24h SHIP budget (COUNCIL.md §4) is still
         committed to the previous ship. `c808b1e`/`4100942` landed
         2026-08-04 14:35:33–14:37:08+02:00; this sitting (10:15 the next
         day) falls inside that 24-hour window. This pre-empts ranking
         discussion entirely — no rung-1/2 candidate exists either
         (JS syntax clean, all GeoJSON valid, nothing down), so there is no
         override available. This sitting is REPORT-only, stated plainly
         per COUNCIL.md §4.

REPORT:  OBJ-coord-validator-2, OBJ-platform-engineer-1 — reaffirmed
         unchanged from the last sitting. Both remain the only items that
         block a future S from becoming shippable: coord-validator-2 gates
         OBJ-map-debugger-2's L→S split; platform-engineer-1 needs its
         scope pinned to pure validation before it can split into an S
         without tripping the build-step/bundler structural veto. Neither
         has been executed since it was last flagged. Now reaffirmed as
         REPORT across two consecutive sittings with no visible execution —
         flagged in Carried below for the next Chair to consider handing
         off more concretely.

Vetoed:  none — all 18 objectives checked against the structural and truth
         vetoes in COUNCIL.md §5; none introduces a dependency/build
         step/bundler, touches the protected layer schema fields, rebrands,
         extracts CSS into a new stylesheet, or sweeps multiple sections.
         Nothing required relitigating.

Docketed to council/BOARD.md: OBJ-frontend-engineer-2, OBJ-frontend-engineer-3,
OBJ-frontend-engineer-4 (new), OBJ-coord-validator-1, OBJ-coord-validator-2,
OBJ-coord-validator-3, OBJ-map-debugger-1, OBJ-map-debugger-2,
OBJ-map-debugger-3, OBJ-map-debugger-4, OBJ-map-tester-1, OBJ-map-tester-2,
OBJ-map-tester-3, OBJ-platform-engineer-1, OBJ-platform-engineer-2,
OBJ-platform-engineer-3, OBJ-security-engineer-1, OBJ-security-engineer-2,
OBJ-security-engineer-3

(One ID minted this sitting: OBJ-frontend-engineer-4. Ledger moves
frontend-engineer 4 → 5. All other ledgers unchanged: coord-validator 4,
map-debugger 5, map-tester 4, platform-engineer 4, security-engineer 4.)

### Outcome
Commit:  none — REPORT-only sitting, no product code touched. This minutes
         file and the `council/BOARD.md` update are the only changes on
         this branch.
Gates:   desktop n/a | light n/a | 375px n/a | data n/a | secrets n/a |
         map-tester n/a — no code shipped this sitting, no gates apply.
Carried: **SHIP budget reopens ~14:35–14:37 Europe/Paris today
         (2026-08-05).** The next sitting at or after that time may ship;
         recompute from the actual commit timestamp, don't assume a fresh
         calendar day resets it. Top-ranked SHIP candidate once the slot
         reopens: **OBJ-map-debugger-1** (dead mailto placeholders,
         unchanged ranking since 2026-08-04, S, unblocked, confirmed still
         live this sitting) — note for whichever seat executes it: the
         current placeholder is `reda.tahiri@example.com`; do not guess a
         replacement address, confirm the real monitored mailbox with the
         repo owner before committing. Close seconds: OBJ-map-debugger-4
         (light-theme contrast) and the new OBJ-frontend-engineer-4
         (fetch-failure vs. empty-layer ambiguity), both S, both unblocked.
         OBJ-map-debugger-2 (L) stays blocked on OBJ-coord-validator-2;
         OBJ-platform-engineer-1 (M) stays blocked on its own scoping
         report — both now reaffirmed as REPORT across two consecutive
         sittings with no execution; if a third sitting reaffirms them
         unexecuted, consider a more concrete handoff instead of re-
         reporting indefinitely.
