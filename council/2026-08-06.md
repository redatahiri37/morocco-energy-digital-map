# Council sitting — 2026-08-06

## Sitting — 07:57 (Europe/Paris)

### State
`git log` since the last sitting (2026-08-04 15:18, commit `9a4d5a4`) showed zero new commits — HEAD unchanged, working tree clean at sitting start. The prior SHIP (OBJ-frontend-engineer-1, `c808b1e`) is confirmed still intact: `docs/app.js` still emits native `<input type="checkbox">` per layer row, `docs/style.css:112,197` still carry the `:focus-visible` outline rules — no drift. No rung-1 breach (coord-validator: no published fact currently wrong). Rung-2 (site up/down) could not be externally confirmed — both `WebFetch` and `curl` to `https://atlas-nexus-69o.pages.dev/` were rejected by the sandbox's own outbound proxy (403 policy denial on the CONNECT), not a response from the site; no code-level regression exists to suggest an outage. Last SHIP was 2026-08-04 — more than 24h ago — so today's SHIP budget was open, and no `council/2026-08-06.md` existed before this sitting.

### Docket
**frontend-engineer**
- OBJ-frontend-engineer-2 | Add a visible loading state to `#layerList`/`#kpiGrid` while `loadAllData()` awaits its fetches
- OBJ-frontend-engineer-3 | Give `.topbar` explicit overflow handling at ≤375px
- OBJ-frontend-engineer-4 | *(new)* Add Escape-key close and focus management to `#methodologyModal`

**coord-validator**
- OBJ-coord-validator-1 | Add a `vintage` field to all 13 features in `industrial.geojson`
- OBJ-coord-validator-2 | Sample-verify `national-hv.geojson` (947 features) and `transmission-lines.geojson` (541 features)
- OBJ-coord-validator-3 | Remove or clearly mark deprecated `grid-lines.geojson` (11 features)

**map-debugger**
- OBJ-map-debugger-4 | Fix light-theme topbar button contrast in `docs/brand.css`
- OBJ-map-debugger-1 | Fix the "Report an error" mailto targets
- OBJ-map-debugger-3 | Surface a visible error state for mid-session MapLibre runtime failures

**map-tester**
- OBJ-map-tester-1 | Add an orphan-data check for `docs/data/morocco/*.geojson`
- OBJ-map-tester-2 | Write down the required evidence bar for a release-gate GO
- OBJ-map-tester-3 | Audit popup field-name mapping against source file property keys

**platform-engineer**
- OBJ-platform-engineer-1 | Add a minimal CI check on push to `main`
- OBJ-platform-engineer-2 | Wire an uptime check against the live map URL
- OBJ-platform-engineer-3 | Add `docs/_headers` with explicit cache-control for `docs/data/*.geojson`

**security-engineer**
- OBJ-security-engineer-1 | Add Subresource Integrity hashes to the MapLibre `<script>`/`<link>` tags
- OBJ-security-engineer-2 | Disclose the third-party requests the page makes on every load
- OBJ-security-engineer-3 | Add a Content-Security-Policy via `docs/_headers`

All 18 are reaffirmations of Board entries except OBJ-frontend-engineer-4, freshly surfaced this sitting (zero `Escape`/`keydown` handling anywhere in `docs/app.js` for `#methodologyModal`, no focus trap, no focus-return).

### Ruling
**SHIP: OBJ-map-debugger-4** — "Fix light-theme topbar button contrast in `docs/brand.css`." North Star: rung 4 of the precedence ladder ("the audience cannot read what is already there") — a regulator or DC developer using light mode could not read the Solaire/Methodology/GitHub/theme-toggle buttons at all (confirmed live: white text, `rgba(255,255,255,.85)`, on a white `#fff` button background). This is the highest-precedence *live, reproduced* defect on today's docket — broader blast radius than OBJ-map-debugger-1 (mailto only fires on a deliberate "report" action) and it hits every light-theme session immediately on load. S-sized, single-file CSS-only change, zero data/secrets surface, security-cleared with the "shared brand.css" risk downgraded (map's `docs/brand.css` and Solar's `solar/brand.css` are confirmed separate physical files — this edit cannot reach Solar's live chrome).

OBJ-map-debugger-1 (dead mailto) and OBJ-platform-engineer-3 (cache-control headers) were both real S-sized candidates and stay next-ranked — see Carried.

**REPORT:** OBJ-coord-validator-2, OBJ-platform-engineer-1 — both M-sized and blocking further-ranked work behind them (OBJ-map-debugger-2's wiring-in of 1,488 unverified grid features cannot ship until OBJ-coord-validator-2 clears; OBJ-platform-engineer-1's CI check needs its validation scope pinned down before it can split into a ship-able S). Designated for a scoping/investigation pass; not executed this sitting — Chair does not implement, and this sitting's budget went to the SHIP execution and gate.

**Vetoed:** none newly vetoed this sitting. coord-validator reaffirmed the standing block on OBJ-map-debugger-2 (COUNCIL.md §5 truth veto — no `docs/data/**` layer ships live without a zero-FAIL coord-validator report) — not a new veto, a carried constraint; OBJ-map-debugger-2 was not on this sitting's 18-item docket.

**Docketed to `council/BOARD.md`:** all 18 above, reaffirmed in place. One new ID minted: OBJ-frontend-engineer-4 (ledger: frontend-engineer → 5).

### Outcome
**Commit:** `f29beca357382b39b72e582a4eeb38360d94b593` — "Fix light-theme topbar button text color in docs/brand.css". Executed by frontend-engineer (owns `docs/brand.css` per COUNCIL.md §3) after this ruling: added
```css
[data-theme="light"] .topbar .ghost-btn,
[data-theme="light"] .topbar .icon-btn { color: #18181a; }
[data-theme="light"] .topbar .ghost-btn:hover,
[data-theme="light"] .topbar .icon-btn:hover { color: #fff; }
```
Specificity (0,3,0) beats the existing unconditional `.topbar .ghost-btn` rule (0,2,0) regardless of source order, so dark theme and existing hover states are untouched by construction, not by luck of ordering — independently re-derived and confirmed by map-tester from source (`docs/brand.css`, `docs/style.css:113-115`, `docs/index.html:21-22` load order), not just trusted from the executing seat's claim.

**Gates:**
- desktop ✓ — dark-theme computed styles unchanged (`rgba(255,255,255,.85)` text on `rgb(37,37,42)` bg, all 4 buttons); console clean (only pre-existing sandbox-network CDN blocks unrelated to this change); screenshot `desktop-dark.png`.
- light ✓ — before: all 4 buttons `rgba(255,255,255,.85)` text on `rgb(255,255,255)` bg (literal white-on-white). After: `rgb(24,24,26)` text on `rgb(255,255,255)` bg, ≈19:1 contrast, for Solaire/Methodology/GitHub/theme-toggle; hover state independently verified unchanged (`rgb(255,255,255)` on `rgba(255,107,53,.15)`). Screenshot `desktop-light.png`.
- 375px ✓ — before/after comparison: pre-fix screenshot shows the Solaire button as a blank white rectangle; post-fix shows "☀ Solaire" clearly readable. `document.documentElement.scrollWidth === clientWidth === 375` identical before/after — no overflow regression introduced.
- data n/a — no `docs/data/**` touched.
- secrets ✓ — security-engineer: diff-scope CLEAN (10 lines added to `docs/brand.css` only, nothing else), secret-scan CLEAN, `solar/**` untouched, history-since-last-sitting CLEAN (exactly this one commit). Verdict: SAFE-TO-PUSH.
- map-tester **GO** — real GO, not GO-STATIC: independently re-derived every quoted computed-style value from source (CSS specificity/cascade math), confirmed it matches the executing seat's browser evidence exactly, confirmed all three browser gates clear this sitting's own evidence bar (OBJ-map-tester-2).

**Carried:** OBJ-map-debugger-4 is now Shipped — see `council/BOARD.md`. OBJ-map-debugger-1 (dead mailto, S) and OBJ-platform-engineer-3 (cache-control headers, S) are the next-ranked SHIP candidates once budget reopens (>24h from this sitting). OBJ-map-debugger-2 (L, wiring in 1,488 grid features) stays blocked on OBJ-coord-validator-2's verification report; OBJ-platform-engineer-1 (M, CI check) stays blocked on its own scoping report — both designated REPORT this sitting but not yet executed (dispatch handed back, same as the SHIP). New this sitting: OBJ-frontend-engineer-4 (methodology modal keyboard/focus trap) filed to the Board, not fixed here — out of scope for today's SHIP slot, no sweep. Next sitting's SHIP slot is closed regardless of ranking — one SHIP per rolling 24h, and this one already landed today; next sitting is REPORT-only.
