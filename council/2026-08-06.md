# Council sitting — 2026-08-06

## Sitting — 02:53 (Europe/Paris)

Eighth actual sitting since 2026-08-03, but `main`'s own Board and minutes
only reflect the 2026-08-04 14:15 sitting — the seven sittings since then
(`council/2026-08-04-1303`, `-1753`, `-2255`; `council/2026-08-05-0800`,
`-1015`, `-1253`, `-2253`, opened as PRs #2–#8) all branched independently
off `main` and none has merged. This sitting reads the most recent of
those seven — `council/2026-08-05-2253` — for the narrative, since it is
the freshest true record of Council state, and treats it (not `main`'s
stale Board) as standing memory, per COUNCIL.md §8's own instruction to
pull, not regenerate. `main`'s actual Board is stale on this one specific
point and is corrected below.

### State established before ruling
- `git log` tip: `9a4d5a4` (`origin/main`, freshly fetched) — unchanged
  since the 2026-08-04 14:15 sitting shipped `c808b1e` via PR #1. No
  further commit has landed on `main` since.
- `git diff --stat origin/main origin/council/2026-08-05-2253 -- docs/`
  returns **empty** — confirmed byte-for-byte that nothing under `docs/**`
  has changed since the 22:53 sitting inspected it. Only `council/**`
  differs (that sitting's own unmerged minutes + Board edit).
- `node --check docs/app.js` and `docs/countries.config.js`: both clean.
  Every `docs/data/morocco/*.geojson` file parses as valid JSON (checked
  all files individually). Working tree clean. No rung-2 candidate
  ("site down / pipeline broken") is live.
- Re-verified live, not assumed: `docs/index.html:122` and
  `docs/app.js:968,997` still read `mailto:reda.tahiri@example.com` — the
  dead placeholder is still there, exactly as the 22:53 sitting found it.
  Also spot-verified, unchanged: no `.github/workflows/` directory, no
  `docs/_headers`, no `integrity=` attribute anywhere in
  `docs/index.html`, no CSP; `docs/brand.css:32-36` still sets
  `.topbar .ghost-btn,.topbar .icon-btn{color:rgba(255,255,255,.85)}`
  unconditionally; `docs/app.js` still reads `p.precision` in both popup
  builders. No rung-1 candidate ("a published fact is wrong") is live
  either — nothing above is a *wrong* fact, all are pre-existing,
  already-docketed gaps.
- **The controlling fact for today's ruling:** the 2026-08-05 22:53
  sitting ruled SHIP on `OBJ-map-debugger-1` (fix the dead mailto
  targets) but **held execution** rather than committing, because its own
  implementation spec asserted a specific real personal replacement
  address as "confirmed" when that was an unverified inference, not a
  fact in the repo — publishing a named individual's personal email
  site-wide is a disclosure decision needing the site owner's explicit
  out-of-band sign-off, not something an unattended sitting can supply.
  `OBJ-map-debugger-1` is **In Progress, held** — not shipped, not
  vetoed. COUNCIL.md §8 caps In Progress at 1 objective at a time. This
  sitting is, again, unattended with no live human input in this
  conversation, so it has no more standing to supply that sign-off than
  the 22:53 sitting did. The WIP slot therefore stays occupied and **no
  objective may be moved into In Progress or ruled into the SHIP
  execution slot this sitting**, independent of the separate fact that
  the rolling-24h SHIP-commit clock is itself open (no commit has landed
  since `c808b1e`, >24h old now) — WIP-limit-1 is the binding constraint
  here, not the clock.

### Docket
18 objectives on the table (2+3+4+3+3+3 — map-debugger's 4 includes its
own In-Progress item, reaffirmed alongside its 3 Docketed). All pulled
from `council/2026-08-05-2253`'s Board (the freshest true record); none
minted fresh this sitting — nothing under `docs/**` changed to justify a
new observation. A genuine fresh-eyes pass was made against
frontend-engineer's remit specifically (checked panel-collapse/expand
wiring, GitHub link hydration, KPI grid, search/empty states, mobile
bottom-sheet remnants) rather than reflexively re-punting; nothing new
was found, so frontend-engineer's docket stays at 2, not 3 — see Carried.

**frontend-engineer** (2, reaffirmed, unchanged)
- OBJ-frontend-engineer-2 | Add a visible loading state to `#layerList`/`#kpiGrid` while `loadAllData()` awaits its fetches in `docs/app.js`
  unlocks:  a DC developer on a slow connection doesn't mistake "still loading" for "this map has no data" and leave before the layers finish loading
  evidence: throttled-network reload shows a loading indicator in the panel between first paint and the first rendered layer row, not a blank sidebar
  size:     S
  risk:     low — additive UI only, no change to fetch logic
- OBJ-frontend-engineer-3 | Give `.topbar` explicit overflow handling at ≤375px in `docs/style.css`
  unlocks:  a DC developer checking the map on a phone can reach every topbar control (country switch, Methodology, GitHub, theme toggle) without one being clipped off-screen
  evidence: at 375px width no topbar control is clipped or unreachable and the page has no horizontal scrollbar
  size:     S
  risk:     low — layout-only, scoped to the existing 375px media query; `.topbar` is `display:flex` with `gap` and no `flex-wrap`/`overflow-x` today

**coord-validator** (3, reaffirmed, unchanged)
- OBJ-coord-validator-1 | Add a `vintage` field to all 13 features in `docs/data/morocco/industrial.geojson`
  unlocks:  a regulator citing OCP's, Renault's or another site's estimated demand can state which year the estimate is from, instead of it being presented as implicitly current
  evidence: all 13 features carry a `vintage` property; the popup source-row surfaces it
  size:     S
  risk:     none to the render path — additive property, ~1 KB file growth
- OBJ-coord-validator-2 | Sample-verify `national-hv.geojson` (947 ONEE 60 kV features) and `transmission-lines.geojson` (541 WBG features)
  unlocks:  a regulator who directly fetches either public file (both cite ONEE/WBG as authoritative) can trust the routing, or the map withdraws the citation — instead of silently hosting 1,488 unchecked "ONEE/WBG-sourced" line segments
  evidence: a coord-validator report with a stated sample size and a FAIL/PASS/UNVERIFIED count; needs a bbox/topology/endpoint-cluster method since features are anonymously named
  size:     M
  risk:     none to ship (read-only); reputational risk is what's already live
- OBJ-coord-validator-3 | Remove or clearly mark deprecated `docs/data/morocco/grid-lines.geojson` (11 features)
  unlocks:  a developer or regulator who fetches `docs/data/` directly doesn't get a stale, unmaintained duplicate of `interconnectors.geojson`/`planned-corridors.geojson` that can silently drift from the live files
  evidence: file removed or a `deprecated: true` root note added; the "legacy fallback" comment at `app.js:86` removed
  size:     S
  risk:     none — file is unreferenced by any live code path

**map-debugger** (4 — 1 In Progress/held + 3 Docketed, all reaffirmed, unchanged)
- OBJ-map-debugger-1 | Fix dead mailto targets (`docs/index.html:122`, `docs/app.js:968,997`, re-verified live still `example.com`) — **In Progress, held pending owner sign-off on a replacement contact address**; not re-proposed from zero, not re-ruled today (see State established)
  unlocks:  a regulator or DC developer who spots a wrong coordinate or stale figure can actually get the correction to land, instead of every click going to an IANA-reserved documentation domain that cannot receive mail
  evidence: mailto target is a real, monitored address in all 3 locations
  size:     S
  risk:     none — string replacement in 2 files, no logic change
- OBJ-map-debugger-4 | Fix light-theme topbar button contrast in `docs/brand.css`
  unlocks:  a regulator or DC developer using light mode can actually read the Solaire/Methodology/GitHub/theme-toggle buttons, instead of white-on-white text (`brand.css:32-36` sets `.topbar .ghost-btn,.topbar .icon-btn{color:rgba(255,255,255,.85)}` unconditionally, silently overriding `style.css`'s light-theme text-color rule since brand.css loads second)
  evidence: in light theme, all four topbar buttons show visible, sufficient-contrast text against their background
  size:     S
  risk:     low — brand.css is shared with Atlas Solar's chrome; a fix must not break Solar's topbar
- OBJ-map-debugger-3 | Surface a visible error state for mid-session MapLibre runtime failures (`docs/app.js:224`, `map.on("error",...)`)
  unlocks:  a regulator whose basemap tiles fail mid-session sees a message explaining the map is degraded, instead of an unexplained frozen/blank canvas — the runtime handler currently only `console.warn`s
  evidence: a simulated tile failure after successful init surfaces a visible in-page message, not just a console warning
  size:     S
  risk:     low — must not fire on benign/recoverable MapLibre warnings or it will falsely alarm users on a healthy map
- OBJ-map-debugger-2 | Wire `national-hv.geojson` (947) + `transmission-lines.geojson` (541) into the live map as renderable layers
  unlocks:  a DC developer assessing grid headroom near a candidate site can currently see only 11 editorial grid lines plus whatever OSM happens to have — ~1,488 curated ONEE/WBG features already sit in this repo, fully unrendered
  evidence: toggling the grid layer renders both feature sets; panel layer counts match file feature counts
  size:     L
  risk:     performance (1,488 line features on one source), visual clutter, inherits unresolved validation status from OBJ-coord-validator-2 — must not ship ahead of it; needs splitting

**map-tester** (3, reaffirmed, unchanged)
- OBJ-map-tester-1 | Add an orphan-data check: list every `docs/data/morocco/*.geojson` file and flag any with zero references in the load path
  unlocks:  the next time a data file is added or edited, a regulator or DC developer relying on "the map shows what's in `docs/data/`" doesn't silently lose a layer — currently 3 orphans exist, caught only by manual grep
  evidence: a script/checklist step reports the orphan list; currently returns 3
  size:     S
  risk:     none — read-only verification script
- OBJ-map-tester-2 | Write down what "browser-level evidence" must contain for a release-gate GO
  unlocks:  a fixed, written evidence bar for desktop/light/375px console + screenshot requirements, closing the gap between `GO-STATIC` and a real GO
  evidence: a short written checklist enumerating required evidence items
  size:     S
  risk:     none — documentation-only
  **Chair flag (new this sitting):** this objective's own `unlocks:` line, as currently written on the Board, reads "a Chair ruling a future SHIP can check a submitted GO..." — that literally names the Council's own chair role, which COUNCIL.md §6 explicitly forbids ("`unlocks:` may not name the Council, the codebase, or code quality"). It has been reaffirmed unchanged across at least two prior sittings without this being caught. Not vetoed and not reworded unilaterally here — map-tester should rewrite the `unlocks:` line to name the actual audience benefit (e.g., a regulator/DC developer trusts a "GO" they see because it was checked against a fixed public bar, not a judgment call) before this is ever next up for SHIP ranking; it fails the North Star Test as literally worded today.
- OBJ-map-tester-3 | Audit popup field-name mapping against each source file's actual property keys
  unlocks:  a DC developer reading a line's popup can trust that "Precision: approximate" reflects that line's real value, not a hardcoded fallback — confirmed live mismatch: `openLinePopup()` reads `p.precision`, but neither unwired file has that exact key
  evidence: a per-layer field-mapping audit confirming every property the popup reads exists under that exact key in every file that layer draws from
  size:     S
  risk:     none — audit only

**platform-engineer** (3, reaffirmed, unchanged)
- OBJ-platform-engineer-1 | Add a minimal CI check on push to `main` (no build step, no bundler)
  unlocks:  a regulator or DC developer visiting the map right after a bad commit is not served a broken page, because a check runs automatically instead of depending on a human remembering to run map-tester first — no `.github/workflows/` exists (re-confirmed this sitting)
  evidence: a CI config JSON-validates every `docs/data/*.geojson` and checks that `docs/index.html`/`docs/app.js` reference only files that exist; fails on a deliberately broken test commit
  size:     M
  risk:     must stay pure shell/validation — a careless implementation could itself introduce the build-step/bundler the structural veto forbids
- OBJ-platform-engineer-2 | Wire an uptime check against the live map URL
  unlocks:  a regulator or DC developer trying to reach the map during a real outage is not left assuming the map doesn't exist for however long it takes someone to notice by hand
  evidence: a scheduled check exists and something (log/notification) proves it fired at least once
  size:     S
  risk:     low — read-only external HTTP check; must not require a new secret
- OBJ-platform-engineer-3 | Add `docs/_headers` with an explicit cache-control policy for `docs/data/*.geojson`
  unlocks:  a regulator or DC developer who reloads the map right after a data correction ships actually sees the corrected figure, instead of a stale cached copy — no `docs/_headers` exists (re-confirmed this sitting)
  evidence: `docs/_headers` sets an explicit, short max-age (or must-revalidate) on `docs/data/*.geojson`; a fetch immediately after a data commit is confirmed to bypass/refresh the cache
  size:     S
  risk:     low — too short raises origin load, too long reintroduces the stale-data problem

**security-engineer** (3, reaffirmed, unchanged)
- OBJ-security-engineer-1 | Add Subresource Integrity hashes to the MapLibre `<script>`/`<link>` tags in `docs/index.html`
  unlocks:  a regulator's or DC developer's browser refuses to execute a tampered `maplibre-gl.js` if unpkg is ever compromised or MITM'd, instead of silently running whatever the CDN serves — zero `integrity=` attributes exist (re-confirmed this sitting)
  evidence: script/link tags carry a correct `integrity` hash matching the pinned 4.7.1 build; a deliberately wrong test hash causes the browser to block the resource
  size:     S
  risk:     low — hash must be regenerated if the pinned CDN version ever changes
- OBJ-security-engineer-2 | Disclose the third-party requests the page makes on every load (Google Fonts, unpkg, CARTO/OSM tiles)
  unlocks:  a regulator evaluating whether the map meets the data-protection bar they'd apply to their own agency's tools can see the third-party data flow disclosed on the page, instead of finding it themselves via devtools
  evidence: the page states which third parties receive a request on load
  size:     S
  risk:     none — documentation-only addition
- OBJ-security-engineer-3 | Add a Content-Security-Policy via `docs/_headers`
  unlocks:  a regulator's or DC developer's browser blocks/reports any unexpected script origin the moment one is injected, instead of it running silently until someone greps the source by hand — no CSP exists anywhere (re-confirmed this sitting)
  evidence: a CSP restricts `script-src`/`style-src`/`connect-src` to known-good origins; the live page shows zero CSP console violations
  size:     S
  risk:     medium — an overly strict CSP silently breaks the map (blocked tile requests = blank map); must be tested at all three gates before ship

### Ruling

SHIP:    none — the WIP slot (COUNCIL.md §8, cap of 1 In Progress) is
         occupied by `OBJ-map-debugger-1`, held pending the site owner's
         out-of-band choice of a replacement contact address. No rung-1
         ("a published fact is wrong") or rung-2 ("site down / pipeline
         broken") candidate is live this sitting to pre-empt that block —
         verified directly (JS syntax clean, all GeoJSON parse, working
         tree clean, no incident) rather than assumed. This sitting is
         itself unattended with no human input in the conversation, so it
         has exactly as little standing to supply that sign-off as the
         2026-08-05 22:53 sitting did — supplying one anyway would repeat
         the same mistake that caused that sitting to hold in the first
         place. The rolling-24h SHIP-commit clock is separately open (no
         commit has landed on `main` since `c808b1e`, now well over 24h
         old), but that is not the controlling constraint today — WIP-1
         is. `OBJ-map-debugger-1` is not re-ruled, not re-vetoed, and not
         re-proposed from zero; it stays exactly where the 22:53 sitting
         left it.

REPORT:  OBJ-coord-validator-2, OBJ-platform-engineer-1 — reaffirmed,
         same reasoning as every prior sitting since first raised: both
         are the board's only M-sized items and both gate a future S
         (coord-validator-2 must resolve before OBJ-map-debugger-2 can be
         split into anything shippable; platform-engineer-1 needs its
         scope pinned to a pure-validation checklist before it can split
         into an S without tripping the build-step/bundler structural
         veto). Flagged as a genuine process concern in Carried below:
         neither has actually been investigated across at least four
         consecutive REPORT designations now — the Chair rules the
         REPORT slot but does not execute it, and execution appears to
         not be happening as a separate dispatched step the way SHIP
         execution has been.

Vetoed:  none — no seat exercised a veto this sitting; every reaffirmed
         objective still sits inside the standing structural and truth
         vetoes, and nothing changed under `docs/**` to require
         relitigating any of them. (One Board-wording defect was flagged,
         not vetoed — see OBJ-map-tester-2 above.)

Docketed: OBJ-frontend-engineer-2, OBJ-frontend-engineer-3,
OBJ-coord-validator-1, OBJ-coord-validator-2, OBJ-coord-validator-3,
OBJ-map-debugger-2, OBJ-map-debugger-3, OBJ-map-debugger-4,
OBJ-map-tester-1, OBJ-map-tester-2, OBJ-map-tester-3,
OBJ-platform-engineer-1, OBJ-platform-engineer-2, OBJ-platform-engineer-3,
OBJ-security-engineer-1, OBJ-security-engineer-2, OBJ-security-engineer-3

(OBJ-map-debugger-1 stays In Progress/held — see `council/BOARD.md`. No
IDs minted this sitting; every ledger stays at its prior value:
frontend-engineer 4, coord-validator 4, map-debugger 5, map-tester 4,
platform-engineer 4, security-engineer 4.)

### Outcome
Commit:  none — REPORT-only sitting; the WIP slot is occupied and no
         rung-1/2 fact pre-empted it, so nothing was eligible for the
         SHIP execution slot today. `council/BOARD.md` on `main` is
         updated in this sitting's PR to reflect the true current state
         (`OBJ-map-debugger-1` In Progress/held), correcting the drift
         left by seven unmerged sitting PRs.
Gates:   desktop n/a | light n/a | 375px n/a | data n/a | secrets n/a |
         map-tester n/a — no code change was ruled this sitting, so
         nothing was gated.
Carried: (1) `OBJ-map-debugger-1` remains In Progress, held — only the
         site owner, out of band, can supply the replacement contact
         target/mechanism; do not re-propose it from zero at the next
         sitting either. (2) `OBJ-coord-validator-2` and
         `OBJ-platform-engineer-1` remain REPORT-designated but
         un-investigated across multiple sittings now — the next sitting
         or an out-of-band step should actually execute one of these
         reads rather than reaffirm the designation an eighth time. (3)
         `OBJ-map-tester-2`'s `unlocks:` line names the Chair, in literal
         violation of COUNCIL.md §6 — map-tester should reword it before
         it is next ranked; not corrected unilaterally this sitting. (4)
         frontend-engineer's docket genuinely re-examined with fresh eyes
         this sitting (not just carried forward unexamined) and still
         sits at 2, not 3 — see State established / Docket note; a real
         third has not yet surfaced. (5) Outside Council remit but
         material to the pipeline: seven sitting PRs (#2–#8, branches
         `council/2026-08-04-1303`, `-1753`, `-2255`,
         `council/2026-08-05-0800`, `-1015`, `-1253`, `-2253`) are open
         and unmerged on GitHub, dating back to 2026-08-04 13:03 — `main`
         has been drifting from actual Council state for over 40 hours as
         a result. This does not unlock a decision for a regulator or DC
         developer, so it cannot be filed as a Council objective (fails
         the North Star Test / the "may not name the codebase" bar), but
         it is flagged here as an operational fact the human owner should
         see.
