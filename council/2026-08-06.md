# Council sitting — 2026-08-06

## Sitting — 07:57 (Europe/Paris)

### State
`git log` since the last sitting (2026-08-04 15:18, commit `9a4d5a4`) showed zero new commits — HEAD unchanged, working tree clean at sitting start. The prior SHIP (OBJ-frontend-engineer-1, `c808b1e`) is confirmed still intact: `docs/app.js` still emits native `<input type="checkbox">` per layer row, `docs/style.css:112,197` still carry the `:focus-visible` outline rules — no drift. No rung-1 breach (coord-validator: no published fact currently wrong). Rung-2 (site up/down) could not be externally confirmed — both `WebFetch` and `curl` to `https://atlas-nexus-69o.pages.dev/` were rejected by the sandbox's own outbound proxy (403 policy denial on the CONNECT), not a response from the site; no code-level regression exists to suggest an outage. Last SHIP was 2026-08-04 — more than 24h ago — so today's SHIP budget was open, and no `council/2026-08-06.md` existed before this sitting.

### Docket
**frontend-engineer**
- OBJ-frontend-engineer-2 | Add a visible loading state to `#layerList`/`#kpiGrid` while `loadAllData()` awaits its fetches
- OBJ-frontend-engineer-3 | Give `.topbar` explicit overflow handling at ≤375px
- OBJ-frontend-engineer-4 | *(new)* Add Escape-key close and focus management to `#methodologyModal`

**coord-validator**
- OBJ-coord-validator-1 | Add a `vintage` field to all 13 features in `industrial.geojson`
- OBJ-coord-validator-2 | Sample-verify `national-hv.geojson` (947 features) and `transmission-lines.geojson` (541 features)
- OBJ-coord-validator-3 | Remove or clearly mark deprecated `grid-lines.geojson` (11 features)

**map-debugger**
- OBJ-map-debugger-4 | Fix light-theme topbar button contrast in `docs/brand.css`
- OBJ-map-debugger-1 | Fix the "Report an error" mailto targets
- OBJ-map-debugger-3 | Surface a visible error state for mid-session MapLibre runtime failures

**map-tester**
- OBJ-map-tester-1 | Add an orphan-data check for `docs/data/morocco/*.geojson`
- OBJ-map-tester-2 | Write down the required evidence bar for a release-gate GO
- OBJ-map-tester-3 | Audit popup field-name mapping against source file property keys

**platform-engineer**
- OBJ-platform-engineer-1 | Add a minimal CI check on push to `main`
- OBJ-platform-engineer-2 | Wire an uptime check against the live map URL
- OBJ-platform-engineer-3 | Add `docs/_headers` with explicit cache-control for `docs/data/*.geojson`

**security-engineer**
- OBJ-security-engineer-1 | Add Subresource Integrity hashes to the MapLibre `<script>`/`<link>` tags
- OBJ-security-engineer-2 | Disclose the third-party requests the page makes on every load
- OBJ-security-engineer-3 | Add a Content-Security-Policy via `docs/_headers`

All 18 are reaffirmations of Board entries except OBJ-frontend-engineer-4, freshly surfaced this sitting (zero `Escape`/`keydown` handling anywhere in `docs/app.js` for `#methodologyModal`, no focus trap, no focus-return).

### Ruling
**SHIP: OBJ-map-debugger-4** — "Fix light-theme topbar button contrast in `docs/brand.css`." North Star: rung 4 of the precedence ladder ("the audience cannot read what is already there") — a regulator or DC developer using light mode could not read the Solaire/Methodology/GitHub/theme-toggle buttons at all (confirmed live: white text, `rgba(255,255,255,.85)`, on a white `#fff` button background). This is the highest-precedence *live, reproduced* defect on today's docket — broader blast radius than OBJ-map-debugger-1 (mailto only fires on a deliberate "report" action) and it hits every light-theme session immediately on load. S-sized, single-file CSS-only change, zero data/secrets surface, security-cleared with the "shared brand.css" risk downgraded (map's `docs/brand.css` and Solar's `solar/brand.css` are confirmed separate physical files — this edit cannot reach Solar's live chrome).

OBJ-map-debugger-1 (dead mailto) and OBJ-platform-engineer-3 (cache-control headers) were both real S-sized candidates and stay next-ranked — see Carried.

**REPORT:** OBJ-coord-validator-2, OBJ-platform-engineer-1 — both M-sized and blocking further-ranked work behind them (OBJ-map-debugger-2's wiring-in of 1,488 unverified grid features cannot ship until OBJ-coord-validator-2 clears; OBJ-platform-engineer-1's CI check needs its validation scope pinned down before it can split into a ship-able S). Designated for a scoping/investigation pass; not executed this sitting — Chair does not implement, and this sitting's budget went to the SHIP execution and gate.

**Vetoed:** none newly vetoed this sitting. coord-validator reaffirmed the standing block on OBJ-map-debugger-2 (COUNCIL.md §5 truth veto — no `docs/data/**` layer ships live without a zero-FAIL coord-validator report) — not a new veto, a carried constraint; OBJ-map-debugger-2 was not on this sitting's 18-item docket.

**Docketed to `council/BOARD.md`:** all 18 above, reaffirmed in place. One new ID minted: OBJ-frontend-engineer-4 (ledger: frontend-engineer → 5).

### Outcome
**Commit:** `f29beca357382b39b72e582a4eeb38360d94b593` — "Fix light-theme topbar button text color in docs/brand.css". Executed by frontend-engineer (owns `docs/brand.css` per COUNCIL.md §3) after this ruling: added
```css
[data-theme="light"] .topbar .ghost-btn,
[data-theme="light"] .topbar .icon-btn { color: #18181a; }
[data-theme="light"] .topbar .ghost-btn:hover,
[data-theme="light"] .topbar .icon-btn:hover { color: #fff; }
```
Specificity (0,3,0) beats the existing unconditional `.topbar .ghost-btn` rule (0,2,0) regardless of source order, so dark theme and existing hover states are untouched by construction, not by luck of ordering — independently re-derived and confirmed by map-tester from source (`docs/brand.css`, `docs/style.css:113-115`, `docs/index.html:21-22` load order), not just trusted from the executing seat's claim.

**Gates:**
- desktop ✓ — dark-theme computed styles unchanged (`rgba(255,255,255,.85)` text on `rgb(37,37,42)` bg, all 4 buttons); console clean (only pre-existing sandbox-network CDN blocks unrelated to this change); screenshot `desktop-dark.png`.
- light ✓ — before: all 4 buttons `rgba(255,255,255,.85)` text on `rgb(255,255,255)` bg (literal white-on-white). After: `rgb(24,24,26)` text on `rgb(255,255,255)` bg, ≈19:1 contrast, for Solaire/Methodology/GitHub/theme-toggle; hover state independently verified unchanged (`rgb(255,255,255)` on `rgba(255,107,53,.15)`). Screenshot `desktop-light.png`.
- 375px ✓ — before/after comparison: pre-fix screenshot shows the Solaire button as a blank white rectangle; post-fix shows "☀ Solaire" clearly readable. `document.documentElement.scrollWidth === clientWidth === 375` identical before/after — no overflow regression introduced.
- data n/a — no `docs/data/**` touched.
- secrets ✓ — security-engineer: diff-scope CLEAN (10 lines added to `docs/brand.css` only, nothing else), secret-scan CLEAN, `solar/**` untouched, history-since-last-sitting CLEAN (exactly this one commit). Verdict: SAFE-TO-PUSH.
- map-tester **GO** — real GO, not GO-STATIC: independently re-derived every quoted computed-style value from source (CSS specificity/cascade math), confirmed it matches the executing seat's browser evidence exactly, confirmed all three browser gates clear this sitting's own evidence bar (OBJ-map-tester-2).

**Carried:** OBJ-map-debugger-4 is now Shipped — see `council/BOARD.md`. OBJ-map-debugger-1 (dead mailto, S) and OBJ-platform-engineer-3 (cache-control headers, S) are the next-ranked SHIP candidates once budget reopens (>24h from this sitting). OBJ-map-debugger-2 (L, wiring in 1,488 grid features) stays blocked on OBJ-coord-validator-2's verification report; OBJ-platform-engineer-1 (M, CI check) stays blocked on its own scoping report — both designated REPORT this sitting but not yet executed (dispatch handed back, same as the SHIP). New this sitting: OBJ-frontend-engineer-4 (methodology modal keyboard/focus trap) filed to the Board, not fixed here — out of scope for today's SHIP slot, no sweep. Next sitting's SHIP slot is closed regardless of ranking — one SHIP per rolling 24h, and this one already landed today; next sitting is REPORT-only.

---

## Sitting — 12:59 (Europe/Paris)

Third sitting of the day (per the true unmerged-branch record; `main` itself is
still at `9a4d5a4` and reflects none of today's sittings). This sitting builds
directly on `council/2026-08-06-0757`'s branch rather than forking fresh from
`main` again, so today's minutes stay in one file instead of fragmenting
further across yet another independent PR.

### State
- `git log` tip unchanged since 07:57: `9a4d5a4` (`origin/main`). No new
  commits landed. The 07:57 SHIP (`OBJ-map-debugger-4`, `f29beca`) remains
  unmerged on its own branch (PR #10, open) — nine council-sitting branches
  (PRs #2–#10) now sit open and unmerged against `main`, none conflicting
  with each other under `docs/**`.
- `node --check` clean on `docs/app.js` and `docs/countries.config.js`; all
  `docs/data/morocco/*.geojson` parse as valid JSON. No rung-1 (published
  fact wrong) candidate live. Rung-2 (site down) unconfirmable again: `curl`
  to `https://atlas-nexus-69o.pages.dev/` returned a 403 CONNECT-tunnel
  policy denial from this sandbox's own outbound proxy, not a response from
  the site — identical to every prior sitting's finding, not a new signal.
- **Controlling fact for this ruling:** this file already records a SHIP at
  07:57 today (`OBJ-map-debugger-4`, `f29beca`). Per COUNCIL.md §4's
  rolling-24h budget, this sitting is **REPORT-only**, regardless of
  ranking, and not negotiable upward absent a live rung-1/2 emergency (none
  exists — see above).

### Docket
Nothing under `docs/**` has changed since 07:57 (confirmed above), so all 18
objectives are pulled unchanged from `council/BOARD.md` rather than
regenerated per seat — same precedent as the 2026-08-04 14:15 and
2026-08-05 02:53 sittings.

**frontend-engineer** — OBJ-frontend-engineer-2, -3, -4 (reaffirmed, unchanged)
**coord-validator** — OBJ-coord-validator-1, -2, -3 (reaffirmed, unchanged)
**map-debugger** — OBJ-map-debugger-1, -2, -3 (reaffirmed, unchanged; -4 now Shipped, off the docket)
**map-tester** — OBJ-map-tester-1, -2, -3 (reaffirmed, unchanged)
**platform-engineer** — OBJ-platform-engineer-1, -2, -3 (reaffirmed, unchanged)
**security-engineer** — OBJ-security-engineer-1, -2, -3 (reaffirmed, unchanged)

### Ruling
**SHIP: none** — budget already spent today (07:57, `OBJ-map-debugger-4`).
Not negotiable upward inside a sitting (COUNCIL.md §4); no rung-1/2
emergency exists to argue otherwise.

**REPORT: OBJ-coord-validator-2, OBJ-platform-engineer-1** — both *actually
executed* this sitting via the Agent tool, strictly read-only, no edits,
rather than designated-and-deferred again (as at 2026-08-04 14:15 and
2026-08-06 07:57). REPORT is explicitly read-only work, and with no SHIP
this sitting the full budget was free for it:

- **OBJ-coord-validator-2:** exhaustive bbox/structural scan of all 1,488
  features across `national-hv.geojson` + `transmission-lines.geojson` — 0
  FAILs, 0 out-of-Morocco coordinates, 0 degenerate geometry. A 26-endpoint
  topology sample (13 features/file) checked against known Moroccan
  cities/sites — all geographically plausible; 5/13 per file remain
  UNVERIFIED only because Nominatim reverse-geocoding is blocked by this
  session's own egress policy (403, reported per the proxy README — a
  sandbox limitation, not a data defect). **New finding:**
  `transmission-lines.geojson` duplicates ~15 already-*live* WBG "planned
  corridor" segments from `planned-corridors.geojson` at exact (0.0 km)
  vertex match — wiring it in as-is (`OBJ-map-debugger-2`) would
  double-render those segments. `national-hv.geojson` shows no such
  duplication and is clear to proceed. Full agent report quoted below.
- **OBJ-platform-engineer-1:** scoped the M-sized CI objective down to a
  genuinely S-sized first slice — one workflow file
  (`.github/workflows/validate.yml`), three pure-shell steps (JSON-validate
  every `docs/data/morocco/*.geojson`; `node --check` on `docs/app.js`/
  `docs/countries.config.js`; grep-verify every `file:` reference in
  `docs/countries.config.js` resolves to a real file), zero new
  dependencies, zero `npm install`/`package.json`. Full YAML sketch and
  agent report quoted below.

**Vetoed:** none.

**Board updates this sitting** (investigation output, not a SHIP — per
COUNCIL.md §8 the Chair updates the Board after every ruling):
- `OBJ-coord-validator-2` annotated with today's report; stays Docketed at
  M — full confidence is blocked on the sandbox's Nominatim denial, not on
  the map's data.
- **New: `OBJ-coord-validator-4`** | Dedupe `transmission-lines.geojson`'s
  ~15 WBG-planned segments against the live `planned-corridors.geojson`
  before `OBJ-map-debugger-2` wires it in. S. Minted (ledger:
  coord-validator → 5).
- **`OBJ-platform-engineer-1` retired, superseded by new
  `OBJ-platform-engineer-4`** | Add `.github/workflows/validate.yml`
  (JSON-validate GeoJSON + `node --check` + referenced-file existence
  check) — the real S-sized first slice per this sitting's scoping report;
  its full stated scope is this one file, so the parent M item is absorbed
  rather than kept as separate remaining scope. S. Minted (ledger:
  platform-engineer → 5).

**Docketed to `council/BOARD.md`:** OBJ-frontend-engineer-2, -3, -4;
OBJ-coord-validator-1, -2, -3, -4 (new); OBJ-map-debugger-1, -2, -3;
OBJ-map-tester-1, -2, -3; OBJ-platform-engineer-2, -3, -4 (new, supersedes
-1); OBJ-security-engineer-1, -2, -3.

### Outcome
**Commit:** none — REPORT-only sitting, no code/data edits made
(COUNCIL.md §4: REPORT is read-only).
**Gates:** n/a — no shippable change this sitting.

**OBJ-coord-validator-2 full report:**
> Method note (blocker): Nominatim (`nominatim.openstreetmap.org`) is
> blocked by org egress policy for this session (403 on both direct
> curl-through-proxy and WebFetch — confirmed via
> `$HTTPS_PROXY/__agentproxy/status`, `recentRelayFailures`). Per proxy
> README, this is a policy denial to report, not route around. Named-lookup
> and reverse-geocode verification could not be performed. Substituted: (1)
> exhaustive structural/bbox scan of all 947+541 features, (2) manual
> proximity check of a 26-endpoint sample (13/file) against 20 known
> Moroccan city/site coordinates, (3) cross-file geometric overlap check
> against `interconnectors.geojson` and `planned-corridors.geojson`.
>
> **1. Structural sanity (exhaustive, both files):** 0/947 and 0/541
> features outside Morocco bbox. 6 duplicate-consecutive-vertex instances
> per file at identical indices — minor, not zero-length lines, likely
> shared conversion artifact. No NaN/null coords.
>
> **2. Topology plausibility (sample: 13 features/file, 26 endpoints
> each):** All PASS geographically — every sampled endpoint falls on the
> Moroccan mainland near a real corridor (Oujda, Tangier, Essaouira, Safi,
> Jorf Lasfar, Rabat, Kenitra, Casablanca, Nador, Dakhla, Marrakech, etc.),
> none in ocean/Algeria/Spain/empty desert. 5/13 per file flagged
> UNVERIFIED without Nominatim, not FAIL.
>
> **3. Cross-file consistency (significant finding):** 5 of 8 features in
> the live, rendered `planned-corridors.geojson` have vertices at exact
> (0.0 km) coordinate match against features in orphaned
> `transmission-lines.geojson`. The WBG planned-line geometry is already
> live on the map via `planned-corridors.geojson` — wiring in
> `transmission-lines.geojson` as-is would duplicate ~15 planned-line
> segments. `interconnectors.geojson` features sit 1–2.5 km from nearby HV
> lines (expected, not duplicates).
>
> **Verdict:** No FAILs found — geometry looks real, not fabricated.
> Recommend proceeding cautiously on `national-hv.geojson` (no duplication
> issue found) but do not wire `transmission-lines.geojson` in as-is —
> dedupe its WBG "planned" segments against `planned-corridors.geojson`
> first, or `OBJ-map-debugger-2` will double-render ~15 already-live
> corridor segments. ONEE/WBG citation stands; full confidence blocked
> pending Nominatim access.

**OBJ-platform-engineer-1 full report:**
> Read-only. No files created or edited.
>
> **1. What the first check validates:** JSON-parse every
> `docs/data/morocco/*.geojson`; `node --check` on `docs/app.js` and
> `docs/countries.config.js`; grep-verify every `file:` value in
> `docs/countries.config.js` resolves to an existing file under
> `docs/data/morocco/`. All read against files already in the repo —
> nothing installed, generated, or bundled.
>
> **2. Workflow sketch:**
> ```yaml
> name: validate
> on:
>   push:
>     branches: [main]
> jobs:
>   validate:
>     runs-on: ubuntu-latest
>     steps:
>       - uses: actions/checkout@v4
>       - name: JSON-validate geojson
>         run: |
>           for f in docs/data/morocco/*.geojson; do
>             python3 -m json.tool "$f" > /dev/null || { echo "INVALID: $f"; exit 1; }
>           done
>       - name: Syntax-check JS
>         run: node --check docs/app.js && node --check docs/countries.config.js
>       - name: Referenced data files exist
>         run: |
>           grep -oP '(?<=file: ")[^"]+\.geojson' docs/countries.config.js | while read -r f; do
>             [ -f "docs/data/morocco/$f" ] || { echo "MISSING: $f"; exit 1; }
>           done
> ```
> `actions/checkout@v4` is the only marketplace action; `python3`/`node`/
> `grep` are preinstalled on `ubuntu-latest`. Zero `npm install`, zero
> `package.json`, zero build step.
>
> **3. What makes this S, not M:** one workflow file, one file area, fully
> reversible by deletion, testable in one sitting by pushing a deliberately
> broken commit to a scratch branch. Uptime monitoring (`OBJ-platform-
> engineer-2`) and broader linting/a11y checks stay separate objectives —
> bundling them back in is what made this M.
>
> **4. Structural-veto risk:** real only if someone later reaches for
> `npm ci`/a linter package "to do it properly." The scoped version never
> touches `package.json` or installs anything, so it does not trip
> COUNCIL.md §5's dependency/build-step veto.

**Carried:** `OBJ-map-debugger-1` (dead mailto, S) and
`OBJ-platform-engineer-3` (cache-control headers, S) remain the
next-ranked SHIP candidates once the budget reopens (not before
2026-08-07, >24h from the 07:57 ship). New `OBJ-platform-engineer-4` (S, CI
validate workflow) is now also SHIP-eligible and should be ranked against
those two once the budget is open. `OBJ-coord-validator-4` (dedupe
`transmission-lines.geojson`, S) blocks `OBJ-map-debugger-2` (L) together
with the now-narrower remaining gap in `OBJ-coord-validator-2`.
**Flagging for the user out-of-band, per COUNCIL.md §4's "the user decides
out of band" provision** — not a Council ruling, the Chair has no merge
authority: nine-going-on-ten council-sitting branches (PRs #2–#11) are now
open and unmerged against `main`, including one real shipped fix
(`f29beca`, the light-theme contrast repair from 07:57) that is not yet
live on the deployed map. Worth a look when there's time.
