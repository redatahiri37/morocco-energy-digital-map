# Council sitting — 2026-08-07

## Sitting — 17:53 (Europe/Paris)

Unattended, scheduled sitting — no human in the loop. First sitting of the
day: no `council/2026-08-05.md`, `2026-08-06.md`, or prior `2026-08-07.md`
exist, so today's SHIP budget (max 1 per rolling 24 h) is unspent
regardless of the 2026-08-04 14:15 sitting's ship.

### State established before convening
- `git log` tip unchanged since the last sitting: `9a4d5a4`. Working tree
  clean, `main` up to date with `origin/main`. Nothing under `docs/**` has
  been touched since `c808b1e` — every seat's Board section was pulled and
  re-verified against live source rather than regenerated from scratch.
- Checked for a live rung-1/rung-2 pre-empt directly, not assumed:
  `node --check docs/app.js` and `docs/countries.config.js` both clean; all
  `docs/data/morocco/*.geojson` files parse as valid JSON. No incident.
  Nothing pre-empts the docket.
- Re-verified both standing SHIP candidates against current source:
  `docs/index.html:122` / `docs/app.js:968,997` still read
  `mailto:reda.tahiri@example.com` (OBJ-map-debugger-1, unchanged).
  `docs/brand.css:32-36` still set `.topbar .ghost-btn,.topbar
  .icon-btn{color:rgba(255,255,255,.85)}` unconditionally, no
  `[data-theme="light"]` variant in the file, still shadowing
  `docs/style.css:113`'s light-theme override (OBJ-map-debugger-4,
  unchanged).
- One new observation, surfaced re-reading `docs/app.js` for
  frontend-engineer (whose Board docket had only 2 items after OBJ-1
  shipped): the per-layer fetch failure at `app.js:248-259` is caught and
  silently replaced with an empty `FeatureCollection` (`console.warn`
  only) — a failed fetch and a legitimately-empty layer are
  indistinguishable in `#layerList`, both showing a bare count "0".
  Minted as OBJ-frontend-engineer-4.

### Docket

**frontend-engineer**
- OBJ-frontend-engineer-2 | Add a visible loading state to `#layerList`/`#kpiGrid` while `loadAllData()` awaits its fetches — reaffirmed, unchanged
- OBJ-frontend-engineer-3 | Give `.topbar` explicit overflow handling at ≤375px — reaffirmed, unchanged
- OBJ-frontend-engineer-4 | Distinguish "layer failed to load" from "layer has zero features" in `#layerList` row rendering (`docs/app.js:248-259,289`) — new this sitting
  unlocks:  a DC developer looking at a layer showing count "0" can tell whether that infrastructure genuinely doesn't exist for this country, or the fetch silently failed
  evidence: a deliberately broken fetch path (e.g. 404 the file) renders a distinguishable "failed to load" state in that layer's row, not a bare "0"
  size:     S
  risk:     low — additive UI flag on the existing catch block; no change to fetch/render logic

**coord-validator**
- OBJ-coord-validator-1 | Add `vintage` field to all 13 `industrial.geojson` features — reaffirmed, unchanged
- OBJ-coord-validator-2 | Sample-verify `national-hv.geojson` (947) + `transmission-lines.geojson` (541) — reaffirmed, unchanged
- OBJ-coord-validator-3 | Remove or mark deprecated `grid-lines.geojson` (11 features, unreferenced) — reaffirmed, unchanged

**map-debugger**
- OBJ-map-debugger-1 | Fix dead mailto targets (`index.html:122`, `app.js:968,997`, still `example.com`) — reaffirmed, re-verified live, unchanged
- OBJ-map-debugger-4 | Fix light-theme topbar button contrast in `docs/brand.css` — reaffirmed, re-verified live, **ruled SHIP**
- OBJ-map-debugger-3 | Surface a visible error state for mid-session MapLibre runtime failures — reaffirmed, unchanged
  (OBJ-map-debugger-2, L, stays on the Board but out of today's active three — still blocked on OBJ-coord-validator-2's report, not re-ranked this sitting)

**map-tester**
- OBJ-map-tester-1 | Orphan-data check across `docs/data/morocco/*.geojson` vs load-path references — reaffirmed, unchanged
- OBJ-map-tester-2 | Write down the required evidence bar for a release-gate GO — reaffirmed, unchanged
- OBJ-map-tester-3 | Audit popup field-name mapping against each source file's actual keys — reaffirmed, unchanged
- OBJ-map-tester-4 | Establish a network-unrestricted browser-check path as a standing prerequisite before any GO claiming full-site coverage — new, filed during this sitting's gate execution (see Outcome); not yet ruled, carried to next sitting

**platform-engineer**
- OBJ-platform-engineer-1 | Add a minimal CI check on push to `main` (no build step, no bundler) — reaffirmed, unchanged
- OBJ-platform-engineer-2 | Wire an uptime check against the live map URL — reaffirmed, unchanged
- OBJ-platform-engineer-3 | Add `docs/_headers` with cache-control for `docs/data/*.geojson` — reaffirmed, unchanged

**security-engineer**
- OBJ-security-engineer-1 | Add Subresource Integrity hashes to the MapLibre `<script>`/`<link>` tags — reaffirmed, unchanged
- OBJ-security-engineer-2 | Disclose the third-party requests the page makes on load — reaffirmed, unchanged
- OBJ-security-engineer-3 | Add a Content-Security-Policy via `docs/_headers` — reaffirmed, unchanged

### Ruling

SHIP:    OBJ-map-debugger-4 — no rung-1/rung-2 candidate exists this
         sitting (JS syntax clean, all GeoJSON valid, no incident), so
         nothing pre-empts the docket. Of the ready S-sized rung-3/4
         candidates, this is the clearest, already-verified rung-4 hit:
         "the audience cannot read what is already there." Confirmed live
         this sitting by direct source read — `docs/brand.css:32-36` still
         unconditionally set topbar button text to `rgba(255,255,255,.85)`
         with no `[data-theme="light"]` variant, loaded after
         `docs/style.css` so it won the cascade and shadowed
         `style.css:113`'s light-theme override — leaving the
         Solaire/Methodology/GitHub/theme-toggle controls white-on-white
         and illegible for every light-mode visitor. It is S, isolated to
         one existing shared file, reversible, and does not touch
         data/schema. Today's SHIP budget was unspent — first sitting
         since 2026-08-04 14:15. Handed to map-debugger for execution,
         with security-engineer and map-tester gates run before commit.

REPORT:  OBJ-coord-validator-2, OBJ-platform-engineer-1 — reaffirmed, not
         re-litigated: both remain the only M-sized items on the Board,
         and both still block a future S. coord-validator-2
         (sample-verifying 1,488 unchecked ONEE/WBG line features) must
         resolve before OBJ-map-debugger-2 (L) can be split into anything
         shippable. platform-engineer-1 (CI check) still needs its scope
         pinned to pure validation before it can split into an S without
         risking the build-step/bundler structural veto. Neither has been
         executed this sitting; dispatch is handed back the same as the
         SHIP.

Vetoed:  none — every reaffirmed objective sits inside the standing
         structural and truth vetoes; nothing changed to require
         relitigating any of them. The two new objectives
         (OBJ-frontend-engineer-4, OBJ-map-tester-4) are additive-only,
         touch no schema, no data, no dependency — no veto applies.

Docketed to council/BOARD.md: OBJ-frontend-engineer-2, OBJ-frontend-engineer-3,
OBJ-frontend-engineer-4, OBJ-coord-validator-1, OBJ-coord-validator-2,
OBJ-coord-validator-3, OBJ-map-debugger-1, OBJ-map-debugger-2,
OBJ-map-debugger-3, OBJ-map-tester-1, OBJ-map-tester-2, OBJ-map-tester-3,
OBJ-map-tester-4, OBJ-platform-engineer-1, OBJ-platform-engineer-2,
OBJ-platform-engineer-3, OBJ-security-engineer-1, OBJ-security-engineer-2,
OBJ-security-engineer-3

New IDs minted this sitting: OBJ-frontend-engineer-4 (frontend-engineer
ledger → 5), OBJ-map-tester-4 (map-tester ledger → 5, minted during gate
execution — see Outcome). All other ledgers unchanged (coord-validator 4,
map-debugger 5, platform-engineer 4, security-engineer 4).

### Outcome
Commit:  `f97dabd` — "front-end: fix light-theme topbar button contrast
         in brand.css". Executed by map-debugger after this ruling: added
         a `[data-theme="light"] .topbar .ghost-btn,.topbar
         .icon-btn{color:#18181a;border-color:#b3b0a8}` override (base +
         `:hover` variant) in `docs/brand.css`, placed after the existing
         unconditional rule so its higher selector specificity (0,3,0 vs
         0,2,0) wins deterministically in light theme, without touching
         the base rule that governs dark theme (default) or the
         `.topbar{background:navy !important}` rule shared by both
         themes. Confirmed `solar/brand.css` is a physically separate,
         non-shared file, and `solar/index.html`'s topbar applies neither
         `.ghost-btn` nor `.icon-btn` — this diff structurally cannot
         reach Solar.
Gates:   desktop ✓ (dark theme, default, measured unchanged before/after:
         `color: rgba(255,255,255,.85)` on `background: rgb(37,37,42)`
         for all four topbar buttons — the regression check) | light ✓
         (`getComputedStyle` on all four topbar buttons: `color:
         rgb(24,24,26)` on `background: rgb(255,255,255)`, ≈17.7:1
         contrast by WCAG relative-luminance formula, independently
         re-measured by map-tester, not just claimed by the executing
         seat) | 375px ✓ (`document.documentElement.scrollWidth >
         clientWidth` returns `false` in both themes, re-verified
         independently) | data n/a (no `docs/data/**` touched) | secrets
         ✓ (security-engineer PASS: diff is exactly the two described CSS
         rule blocks, 15 insertions/0 deletions, only `docs/brand.css`
         touched, zero secret-pattern matches, no new
         script/URL/dependency, recent history clean) | map-tester **GO**
         — scoped explicitly to this objective: this sandbox's outbound
         proxy blocks unpkg/CARTO/Google Fonts (`ERR_TUNNEL_CONNECTION_FAILED`
         / `ERR_CONNECTION_RESET`, pre-existing and unrelated to this
         change), so MapLibre itself never initializes here and the map
         canvas/tile/popup rendering is unverifiable from this
         environment. map-tester substituted a local-asset-only Chromium
         session (all local requests 200) to independently re-measure the
         same computed-style evidence map-debugger reported, which is
         sound for a CSS-only chrome fix that doesn't depend on
         MapLibre/tiles. map-tester was explicit this GO does not cover
         full-site rendering and flagged that gap as a new objective
         (OBJ-map-tester-4) rather than silently rendering `GO-STATIC`.
Carried: OBJ-map-debugger-4 is now Shipped — see `council/BOARD.md`.
         OBJ-map-debugger-1 (dead mailto) is next-ranked for the SHIP slot
         at the next sitting — today's slot is spent. OBJ-map-debugger-2
         (L) stays blocked on OBJ-coord-validator-2's report;
         OBJ-platform-engineer-1 (M) stays blocked on its own scoping
         report. New this sitting, unranked, for the next sitting to
         evaluate: OBJ-frontend-engineer-4 (layer load-failure vs
         empty-layer distinction) and OBJ-map-tester-4 (close the
         network-unrestricted-browser-check gap this sitting's GO had to
         work around — may come back vetoed to "document, don't fix" if
         it requires a proxy-allowlist decision outside the Council's
         remit).
