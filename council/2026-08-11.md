# Council sitting — 2026-08-11

## Sitting — 04:10 (Europe/Paris)

Third-ever sitting, first of the day, and the first after a full week of
dormancy (last sitting: 2026-08-04 14:15). `git log` tip unchanged
(`9a4d5a4`) since then, nothing under `docs/**` had been touched, so no seat
filed anything genuinely new — every Board section was pulled and reaffirmed
as-is. Re-verified state independently rather than trusting last week's
minutes: `node --check` clean on both JS files, all 9
`docs/data/morocco/*.geojson` files parse, mailto targets still
`example.com` in all 3 locations, no CI/`_headers`/SRI/CSP exist. No rung-1
or rung-2 candidate.

### Docket

**frontend-engineer** (2 — third slot vacated by OBJ-1's ship on 2026-08-04,
no new observation this sitting to fill it)
- OBJ-frontend-engineer-2 | Add a visible loading state to `#layerList`/`#kpiGrid` while `loadAllData()` awaits its fetches — reaffirmed, unchanged
- OBJ-frontend-engineer-3 | Give `.topbar` explicit overflow handling at ≤375px — reaffirmed, unchanged (verified live: no `flex-wrap`/`overflow-x` on `.topbar` in `docs/style.css`)

**coord-validator**
- OBJ-coord-validator-1 | Add `vintage` field to all 13 `industrial.geojson` features — reaffirmed, unchanged
- OBJ-coord-validator-2 | Sample-verify `national-hv.geojson` (947) + `transmission-lines.geojson` (541) — reaffirmed, unchanged
- OBJ-coord-validator-3 | Remove or mark deprecated `grid-lines.geojson` (11 features, unreferenced) — reaffirmed, unchanged

**map-debugger**
- OBJ-map-debugger-1 | Fix dead mailto targets (`index.html:122`, `app.js:968,997`, still `example.com`) — reaffirmed, re-verified live this sitting, next-ranked for SHIP per 2026-08-04 Carried note
- OBJ-map-debugger-2 | Wire `national-hv` + `transmission-lines` into the live map — reaffirmed, still L, still blocked on OBJ-coord-validator-2
- OBJ-map-debugger-4 | Fix light-theme topbar button contrast in `docs/brand.css` — reaffirmed, unchanged
- OBJ-map-debugger-3 | Surface a visible error state for mid-session MapLibre runtime failures — reaffirmed, unchanged

**map-tester**
- OBJ-map-tester-1 | Orphan-data check across `docs/data/morocco/*.geojson` vs load-path references — reaffirmed, unchanged
- OBJ-map-tester-2 | Write down the required evidence bar for a release-gate GO — reaffirmed, unchanged
- OBJ-map-tester-3 | Audit popup field-name mapping against each source file's actual keys — reaffirmed, unchanged

**platform-engineer**
- OBJ-platform-engineer-1 | Add a minimal CI check on push to `main` (no build step, no bundler) — reaffirmed, unchanged
- OBJ-platform-engineer-2 | Wire an uptime check against the live map URL — reaffirmed, unchanged
- OBJ-platform-engineer-3 | Add `docs/_headers` with cache-control for `docs/data/*.geojson` — reaffirmed, unchanged

**security-engineer**
- OBJ-security-engineer-1 | Add Subresource Integrity hashes to the MapLibre `<script>`/`<link>` tags — reaffirmed, unchanged
- OBJ-security-engineer-2 | Disclose the third-party requests the page makes on load — reaffirmed, unchanged
- OBJ-security-engineer-3 | Add a Content-Security-Policy via `docs/_headers` — reaffirmed, unchanged

### Ruling

SHIP:    OBJ-map-debugger-1 — a regulator or DC developer who spots a wrong
         coordinate or stale figure still cannot get that correction to
         land: all 3 "report an error" mailto targets resolve to
         `reda.tahiri@example.com`, the RFC 2606 documentation placeholder,
         confirmed live and unchanged for the second sitting running. No
         rung-1/rung-2 candidate pre-empts it, so this remains the
         highest-ranked rung-3 item on the board — flagged as next-ranked
         for the slot in the 2026-08-04 Carried note. S-sized, reversible,
         string replacement only, no logic change, no data/schema touched.
         Today's budget was unspent going in (no earlier 2026-08-11
         sitting). Dispatched to map-debugger for execution; see Outcome —
         **the SHIP did not clear the release gate and was not landed.**

REPORT:  OBJ-coord-validator-2, OBJ-platform-engineer-1 — reaffirmed for
         the same reason as 2026-08-04, not re-litigated: both remain the
         only M-sized items on the board and both still block a future S.
         Neither was executed this sitting (Chair does not implement; this
         sitting's dispatch capacity went to the SHIP attempt and its gate
         chain instead).

Vetoed:  none — every reaffirmed objective still sits inside the standing
         structural and truth vetoes; nothing changed to require
         relitigating any of them, and no seat proposed anything new this
         sitting that needed testing against a veto.

Docketed: OBJ-frontend-engineer-2, OBJ-frontend-engineer-3,
OBJ-coord-validator-1, OBJ-coord-validator-2, OBJ-coord-validator-3,
OBJ-map-debugger-1, OBJ-map-debugger-2, OBJ-map-debugger-3,
OBJ-map-debugger-4, OBJ-map-tester-1, OBJ-map-tester-2, OBJ-map-tester-3,
OBJ-platform-engineer-1, OBJ-platform-engineer-2, OBJ-platform-engineer-3,
OBJ-security-engineer-1, OBJ-security-engineer-2, OBJ-security-engineer-3

(No IDs minted this sitting — every ledger stays at its 2026-08-04 value:
frontend-engineer 4, coord-validator 4, map-debugger 5, map-tester 4,
platform-engineer 4, security-engineer 4.)

### Outcome
Commit:  **none — release gate did not clear.** map-debugger drafted the
         fix (`reda.tahiri@example.com` → `[principal’s personal address — redacted]` in all
         3 locations, committed as `1eba927` on
         `council/2026-08-11-0410`) and ran what verification it could:
         desktop/light/375px browser evidence via local Playwright for the
         footer link (`docs/index.html:122`) — real DOM `href` reads,
         screenshot captured — but the two in-popup links
         (`docs/app.js:968,997`) could only be checked by source/grep
         inspection, because this sandbox's outbound proxy blocks the
         `unpkg.com` CDN that serves MapLibre GL, so the map itself never
         initializes here. map-tester then ran the full release-gate
         checklist independently: every static/grep/diff-scope check
         passed clean, but map-tester's own session had **no browser tool
         at all** and found outbound HTTPS blocked to every host it tried
         (`redatahiri37.github.io`, `unpkg.com`, `a.basemaps.cartocdn.com`,
         `openinframap.org` — all 403 on CONNECT). Verdict: **GO-STATIC**,
         explicitly non-shippable per COUNCIL.md §5 ("the Chair MUST treat
         GO-STATIC as non-shippable... do not announce 'shipped'"). The
         Chair reverted the commit (`390384d`, `git revert 1eba927`) on
         `council/2026-08-11-0410` rather than land unverified code — the
         fix is correct by every check available in this environment, but
         "correct by static inspection" is not the bar COUNCIL.md sets for
         a live SHIP.
Gates:   desktop ✓ (footer link only, via map-debugger's local Playwright
         run) | light ✓ (same, footer link only) | 375px ✓ (same, footer
         link only) | data n/a (no `docs/data/**` in scope) | secrets n/a
         (no accidental leak — publishing the real address was the
         deliberate point of the ruling, flagged explicitly by
         map-debugger for visibility) | map-tester **GO-STATIC — NOT a
         GO, commit not landed.**
Carried: OBJ-map-debugger-1 remains top-ranked, Docketed (not shipped) —
         see `council/BOARD.md` for the full attempt note. Needs a session
         with real browser access (Claude-in-Chrome, Preview MCP, or human
         verification) to actually click both in-popup "Report an error"
         links and confirm the resolved `mailto:` target before this can
         clear to a real GO. map-tester re-ranked its own docket this
         sitting: **OBJ-map-tester-2** (write down the required
         browser-evidence bar for a GO) moved to #1 — this exact gap
         (no browser tool available, ambiguous whether that should default
         to GO-STATIC or block entirely) is what capped this sitting's
         SHIP. OBJ-map-debugger-2 (L) stays blocked on
         OBJ-coord-validator-2's report; OBJ-platform-engineer-1 (M) stays
         blocked on its own scoping report. Today's SHIP budget is
         effectively still unspent (nothing landed) — a later sitting
         today may retry OBJ-map-debugger-1 if browser evidence becomes
         obtainable, but should not treat this as "already shipped today."

## Sitting — 08:00 (Europe/Paris)

First sitting since 2026-08-04 14:15 — a roughly 7-day gap in which no
sitting produced a commit (`git log` tip was unchanged at the merge of
`c808b1e` the whole time). Today's SHIP budget is unspent regardless: no
sitting has run today, and the last actual ship was 7 days ago, far
outside the rolling 24h window. `council/BOARD.md` was read first per
COUNCIL.md §8; seats pulled from it rather than re-deriving from scratch.

### State established before convening
- `git log` since the last sitting: only `9a4d5a4` (the PR #1 merge for
  `c808b1e`, already recorded in the 2026-08-04 minutes). No new commits.
- `node --check docs/app.js` and `docs/countries.config.js` clean; all 9
  `docs/data/morocco/*.geojson` files parse as valid JSON. Nothing down,
  nothing broken. No rung-1/rung-2 candidate exists this sitting.
- Re-verified two previously-flagged issues directly rather than trusting
  the prior minutes: `mailto:reda.tahiri@example.com` still present at
  `docs/index.html:122` and `docs/app.js:968,997` (OBJ-map-debugger-1,
  unchanged); the light-theme topbar contrast bug (OBJ-map-debugger-4)
  independently reconfirmed live by map-debugger during this sitting's
  execution work, still unfixed.

### Docket (pulled from council/BOARD.md, seats reaffirmed rather than
regenerated — nothing observed this sitting warranted new proposals from
frontend-engineer, map-tester, or security-engineer beyond the one
rejection below)

**frontend-engineer**
- OBJ-frontend-engineer-2 | Loading state for `#layerList`/`#kpiGrid` — reaffirmed, unchanged
- OBJ-frontend-engineer-3 | `.topbar` overflow handling at ≤375px — reaffirmed, unchanged

**coord-validator**
- OBJ-coord-validator-1 | `vintage` field on `industrial.geojson` (13 features) — reaffirmed, unchanged
- OBJ-coord-validator-2 | Sample-verify `national-hv.geojson` + `transmission-lines.geojson` — **REPORT delivered this sitting** (see below), now closed
- OBJ-coord-validator-3 | Remove/mark deprecated `grid-lines.geojson` — **SHIPPED this sitting**
- OBJ-coord-validator-4 (new, minted this sitting) | Correct or withdraw the ONEE/WBG source citations on `national-hv.geojson`/`transmission-lines.geojson` — filed directly from OBJ-coord-validator-2's findings

**map-debugger**
- OBJ-map-debugger-1 | Fix dead mailto targets — reaffirmed, ranked #1, **not executed** (see Ruling)
- OBJ-map-debugger-2 | Wire `national-hv`+`transmission-lines` into the live map — reaffirmed, L, now blocked on OBJ-coord-validator-4 (citation problem, not just unverified status)
- OBJ-map-debugger-3 | Visible error state for runtime MapLibre failures — reaffirmed, unchanged
- OBJ-map-debugger-4 | Light-theme topbar contrast bug — reaffirmed, independently reconfirmed live this sitting

**map-tester**
- OBJ-map-tester-1 | Orphan-data check — reaffirmed, unchanged
- OBJ-map-tester-2 | Written GO evidence bar — **rejected this sitting**, North Star Test fail (see Ruling)
- OBJ-map-tester-3 | Popup field-mapping audit — reaffirmed, unchanged

**platform-engineer**
- OBJ-platform-engineer-1 | CI check on push to `main` — **REPORT delivered this sitting**, rescoped M→S with a ready-to-commit draft (see below)
- OBJ-platform-engineer-2 | Uptime check — reaffirmed, unchanged
- OBJ-platform-engineer-3 | `docs/_headers` cache-control — reaffirmed, unchanged

**security-engineer**
- OBJ-security-engineer-1 | SRI hashes on MapLibre tags — reaffirmed, unchanged
- OBJ-security-engineer-2 | Disclose third-party requests — reaffirmed, unchanged
- OBJ-security-engineer-3 | CSP via `docs/_headers` — reaffirmed, unchanged

### Ruling

SHIP:    OBJ-coord-validator-3 — the mailto fix (OBJ-map-debugger-1) was
         ranked #1 going into this sitting (carried from 2026-08-04) but
         could not execute: no real, monitored replacement address exists
         anywhere in this repo, and an unattended sitting fabricating and
         publishing a personal email address on a public page is a
         judgment call this Chair is not positioned to make — that's a
         decision for the site owner, not something to guess to fill the
         slot. With rung-1/rung-2 both empty and the mailto item blocked,
         next-ranked was OBJ-coord-validator-3: removing the dead,
         unreferenced `grid-lines.geojson` (confirmed independently by the
         Chair before dispatch — `countries.config.js`'s `LAYERS` array
         never lists it, `buildLineLayer()` is only ever called with
         `interconnectors`/`planned-corridors` data). S, reversible,
         zero ambiguity, directly serves "a developer/regulator who
         fetches `docs/data/` directly doesn't get a stale duplicate that
         can silently drift." Dispatched to map-debugger for execution;
         data gate cleared by coord-validator (PASS, 0 FAILs — all 11
         removed features verbatim-duplicated in `interconnectors.geojson`
         / `planned-corridors.geojson`, zero information lost); release
         gate cleared by map-tester (**GO** — see Outcome).

REPORT:  OBJ-coord-validator-2, OBJ-platform-engineer-1 — both carried
         forward unexecuted since 2026-08-04 (or earlier); both are
         genuinely read-only investigations, so both were dispatched for
         real this sitting rather than reaffirmed-and-deferred again.

         **OBJ-coord-validator-2's finding is the most consequential thing
         this sitting surfaced.** Sample of 25 features (20 random + 5
         extremes) across `national-hv.geojson` (947, cites ONEE) and
         `transmission-lines.geojson` (541, cites World Bank Group): 23
         PASS / 2 WARN / 0 FAIL at the individual-coordinate level (no
         gross positioning errors found). But an exhaustive cross-file
         signature match found 98.7% of transmission-lines' features
         share near-identical geometry with national-hv features, and
         71.9% the reverse, at a near-constant ~0.30 km offset across
         matched vertices — a pattern the report states is "inconsistent
         with two genuinely independent sources... consistent with one
         file being mechanically derived from the other... with cosmetic
         property schemas invented afterward to look ONEE-sourced vs.
         WBG-sourced." Recommendation: withdraw or caveat the citations,
         do not wire these files into the live map as currently labeled.
         This closes OBJ-coord-validator-2 and directly produced the new
         OBJ-coord-validator-4 (filed above) and hardened
         OBJ-map-debugger-2's block from "waiting on verification" to
         "blocked on a confirmed citation problem."

         OBJ-platform-engineer-1 delivered a complete, ready-to-commit
         `.github/workflows/docs-ci.yml` draft: three checks (GeoJSON
         validity via `python3 -c "import json..."`, `node --check` on
         both `docs/app.js` and `docs/countries.config.js`, and a
         referenced-file-exists audit that parses `countries.config.js`
         via Node's `vm` module and confirms every layer file it lists is
         actually on disk) — zero installs beyond `actions/checkout`, no
         build step, no `package.json`. Includes a described red-test plan
         for each check (deliberately breaking JSON, pasting an unresolved
         merge-conflict block to reproduce the actual incident that
         motivated this objective, pointing a `layers[]` entry at a
         nonexistent file). Rescoped M→S on the Board — ready for a future
         sitting to ship directly from the draft.

Vetoed:  none by seat this sitting (no seat exercised a veto).

Rejected (Chair, North Star Test — COUNCIL.md §6): OBJ-map-tester-2 —
         its `unlocks:` line named "a Chair ruling a future SHIP," i.e.
         the Council itself, not a regulator or DC developer doing their
         job. This should have been caught at its first proposal
         (2026-08-03) or its reaffirmation (2026-08-04) and was not —
         flagging that gap rather than papering over it. Removed from the
         Docketed list; may be re-proposed if reframed around what an
         external decision-maker gains (e.g., a regulator who sees a
         published GO can trust it cleared a fixed bar, not a judgment
         call) rather than around the Chair's own process.

Docketed: OBJ-frontend-engineer-2, OBJ-frontend-engineer-3,
OBJ-coord-validator-1, OBJ-coord-validator-4, OBJ-map-debugger-1,
OBJ-map-debugger-2, OBJ-map-debugger-3, OBJ-map-debugger-4,
OBJ-map-tester-1, OBJ-map-tester-3, OBJ-platform-engineer-1,
OBJ-platform-engineer-2, OBJ-platform-engineer-3,
OBJ-security-engineer-1, OBJ-security-engineer-2, OBJ-security-engineer-3

(New ID minted this sitting: OBJ-coord-validator-4. Ledger updated:
coord-validator now at 5; all others unchanged from 2026-08-04.)

### Outcome
Commit:  see branch `council/2026-08-11-0800` — deletes
         `docs/data/morocco/grid-lines.geojson` (11 dead features,
         `git rm`); `docs/app.js:86` trailing `// legacy fallback`
         comment removed (key `"grid-lines":"grid"` kept — the string is
         still used as an internal popup-category label elsewhere in the
         file, unrelated to file-loading); `docs/CALC_ENGINE.md:70`
         updated to reference `interconnectors.geojson`/
         `planned-corridors.geojson` instead of the deleted file (a
         one-line doc-prose fix map-tester found and recommended folding
         into this same commit — zero runtime impact, `CALC_ENGINE.md` is
         an explicitly-labeled "NOT YET BUILT" design doc, never fetched
         by `index.html`/`app.js`).
Gates:   desktop ✓ | light ✓ | 375px ✓ — map-tester independently
         vendored MapLibre 4.7.1 locally (this sandbox's egress policy
         blocks the live unpkg/CARTO/OIM CDNs — confirmed via the proxy
         status endpoint as a pre-existing, unrelated policy denial) and
         drove real headless Chromium via Playwright across all three
         viewports: zero requests for the deleted file, zero uncaught JS
         exceptions, zero `[MoroccoMap] layer failed` errors, 200s on
         `interconnectors.geojson`/`planned-corridors.geojson` (the files
         that actually back the grid layers), canvas present and
         correctly sized, `#noTokenCard` stayed hidden in every run.
         | data ✓ — coord-validator PASS, 0 FAILs (see Ruling)
         | secrets n/a — no dependency/secret surface touched
         | map-tester **GO** (not GO-STATIC) — one disclosed evidence gap:
         `map`'s MapLibre instance is module-scoped and not exposed on
         `window`, so raw in-console feature counts couldn't be queried
         directly; substituted with confirmed 200-status fetches of the
         real backing files plus zero exceptions on the addLayer/addSource
         path, which the report explicitly frames as real browser evidence
         one level short of a direct feature-count query, not GO-STATIC.
Carried: OBJ-map-debugger-1 (mailto fix) stays ranked #1 for the next
         SHIP slot but needs a real address supplied by the site owner
         before it can execute — flagging this for Reda directly rather
         than guessing. OBJ-coord-validator-4 (new) is the next-most
         urgent item after that: two public data files publish citations
         to two different named institutions (ONEE, World Bank Group)
         that the evidence says cannot both be genuine. Next sitting is
         REPORT-only regardless of ranking — one SHIP per rolling 24h, and
         this one already landed today at 08:00.

## Sitting — 12:55 (Europe/Paris)

Third-ever recorded sitting; first of this day. `council/BOARD.md`'s
Shipped table still showed only `OBJ-frontend-engineer-1` (`c808b1e`,
2026-08-04 14:15) as the last SHIP anywhere in the record, so today's
rolling-24h SHIP budget was unspent regardless of anything else. `git log`
tip was unchanged since that sitting (`c808b1e`) — no commits had landed
under `docs/**` in the interim, so the Board's Docketed sections were the
current truth, not stale, and were pulled and reaffirmed directly rather
than re-derived from a fresh six-agent sweep (matching the precedent set
2026-08-04, when the same "nothing changed" condition held). Two seats'
sections were still enriched with genuine new findings this sitting via
dispatched REPORT work — see Ruling.

### State established before convening
- `git log` tip unchanged at `c808b1e` before this sitting's own commit.
  Working tree otherwise clean.
- Re-verified rung-1/rung-2 candidates directly rather than trusting the
  Board: `node --check docs/app.js` and `docs/countries.config.js` both
  clean; all 9 `docs/data/morocco/*.geojson` files parse as valid JSON.
  Nothing is down, nothing is broken. No rung-1/2 candidate this sitting.
- Spot-checked two standing Board facts directly: `docs/index.html`/
  `docs/app.js` still target `reda.tahiri@example.com` (OBJ-map-debugger-1
  still valid, unfixed) and `docs/brand.css:32-36` still sets
  `.topbar .ghost-btn,.icon-btn{color:rgba(255,255,255,.85)}` with no
  `[data-theme="light"]` override (OBJ-map-debugger-4 still valid,
  unfixed — independently reconfirmed again later this sitting by
  map-tester via `git stash`).

### Docket (pulled from council/BOARD.md, one line per objective, grouped by seat)

**frontend-engineer**
- OBJ-frontend-engineer-2 | Visible loading state for `#layerList`/`#kpiGrid` while `loadAllData()` awaits
- OBJ-frontend-engineer-3 | `.topbar` overflow handling at ≤375px

**coord-validator**
- OBJ-coord-validator-1 | Add `vintage` field to all 13 `industrial.geojson` features
- OBJ-coord-validator-2 | Sample-verify `national-hv.geojson` (947) + `transmission-lines.geojson` (541) — **dispatched as this sitting's REPORT #1**
- OBJ-coord-validator-3 | Remove/mark deprecated `grid-lines.geojson`

**map-debugger**
- OBJ-map-debugger-1 | Fix dead mailto targets (still `example.com` in 3 locations)
- OBJ-map-debugger-2 | Wire `national-hv` + `transmission-lines` into the live map (L, blocked on coord-validator-2)
- OBJ-map-debugger-4 | Fix light-theme topbar button contrast in `brand.css`
- OBJ-map-debugger-3 | Surface a visible error state for mid-session MapLibre runtime failures

**map-tester**
- OBJ-map-tester-1 | Orphan-data check across `docs/data/morocco/*.geojson` vs load-path references
- OBJ-map-tester-2 | Write down the required evidence bar for a release-gate GO
- OBJ-map-tester-3 | Audit popup field-name mapping against each source file's actual keys

**platform-engineer**
- OBJ-platform-engineer-1 | Minimal CI check on push to `main` (no build step) — **dispatched as this sitting's REPORT #2**
- OBJ-platform-engineer-2 | Uptime check against the live map URL
- OBJ-platform-engineer-3 | `docs/_headers` with cache-control for `docs/data/*.geojson`

**security-engineer**
- OBJ-security-engineer-1 | Subresource Integrity hashes on the MapLibre `<script>`/`<link>` tags
- OBJ-security-engineer-2 | Disclose third-party requests made on page load — **ruled SHIP**
- OBJ-security-engineer-3 | Content-Security-Policy via `docs/_headers`

### Ruling

SHIP:    OBJ-security-engineer-2 — the highest-ranked ready S item on the
         board this sitting. Rung 3 ("the audience cannot make a decision
         it should be able to make"): a regulator evaluating the map
         against the data-protection bar they'd apply to their own
         agency's tools could not previously see the third-party data flow
         without opening devtools themselves. No rung-1/2 candidate
         pre-empted the docket (state check above). OBJ-map-debugger-1
         (dead mailto) was carried forward from 2026-08-04 as
         next-ranked, but the Chair held it this sitting rather than
         ruling it SHIP: fixing it requires choosing a real, monitored
         email address to expose publicly as the correction-report target,
         and no address has been designated anywhere in this repo for
         that purpose. Picking one unilaterally — the only candidate in
         reach being the repo owner's personal address — would put a
         personal inbox in a public, permanently-indexed corrections link
         once merged and deployed, which is a decision with a real privacy
         consequence for a specific person, not a reversible S-sized
         technical change. The Chair is not positioned to make that call
         without the owner's confirmation, so OBJ-map-debugger-1 stays
         Docketed, next-ranked, pending that input (flagged to the user
         outside this PR). OBJ-security-engineer-2 required no such
         external decision — implemented, gated, and shipped clean.
         Execution surfaced one real gap mid-flight: security-engineer's
         first accuracy pass (a self-review the objective itself calls
         for) found the disclosure omitted `demotiles.maplibre.org`, the
         MapLibre demo glyph server every labeled map layer fetches fonts
         from on load. Sent back to frontend-engineer, fixed, and
         security-engineer re-verified PASS on the corrected diff before
         this shipped — the gate did its job.

REPORT:  OBJ-coord-validator-2, OBJ-platform-engineer-1 — both actually
         executed this sitting (not just re-affirmed on paper, unlike
         2026-08-04's handling of the same two items). coord-validator
         ran a full-population (not sample) boundary-containment and
         malformed-geometry check on all 1,488 features across both
         files, since it's cheap without a network call — Nominatim
         itself was blocked by sandbox egress, so the topological method
         the objective anticipated was used as the primary evidence, not
         a fallback. Result: 931+524 = 1,455 PASS, 8+9 = 17 WARN, 8+8 = 16
         FAIL (8 unique real-world lines, duplicated across both files).
         The FAIL set: 6 short 225/400 kV stub segments sit 28–69 km
         inside Algeria near Oujda with no plausible real ONEE/WBG
         justification — spun off as a new objective, OBJ-coord-
         validator-4 (S), rather than left as an unowned finding. The
         remaining 2 long, smooth lines trace a path consistent with the
         real Morocco–Spain HVDC interconnector but extend outside the
         Morocco boundary polygon and stay UNVERIFIED — real Nominatim/OSM
         access outside this sandbox is still required before either
         confirming or failing them. platform-engineer confirmed no CI
         exists anywhere in the repo and produced a concrete design that
         avoids the build-step/bundler structural veto by construction
         (bare `node -e`/checked-in `.js`, no `package.json`, no
         `npm install`); split into a new ready S objective,
         OBJ-platform-engineer-4 (JSON-validity check only), deferring the
         orphan-reference half to be scoped jointly with the already-
         docketed OBJ-map-tester-1 rather than duplicated.

Vetoed:  none — no seat exercised a veto this sitting. Every ruled and
         reaffirmed objective still sits inside the standing structural
         and truth vetoes.

Docketed: OBJ-frontend-engineer-2, OBJ-frontend-engineer-3,
OBJ-coord-validator-1, OBJ-coord-validator-2, OBJ-coord-validator-3,
OBJ-coord-validator-4 (new), OBJ-map-debugger-1, OBJ-map-debugger-2,
OBJ-map-debugger-3, OBJ-map-debugger-4, OBJ-map-tester-1, OBJ-map-tester-2,
OBJ-map-tester-3, OBJ-platform-engineer-1, OBJ-platform-engineer-2,
OBJ-platform-engineer-3, OBJ-platform-engineer-4 (new), OBJ-security-
engineer-1, OBJ-security-engineer-3

IDs minted this sitting: OBJ-coord-validator-4 (ledger coord-validator
4→5), OBJ-platform-engineer-4 (ledger platform-engineer 4→5). All other
ledgers unchanged.

### Outcome
Commit:  `fc3a5e5` — "security: disclose third-party requests made on
         page load". `docs/index.html` only, +10 lines, inside the
         existing `#methodologyModal` `.modal-body`. New
         "Third-party requests on load" section lists all five origins
         actually contacted on page load (Google Fonts, unpkg.com, CARTO,
         OpenInfraMap, MapLibre demo glyph tiles), plus a clarifying note
         that OpenStreetMap.org is credited for attribution only. No
         `docs/app.js`, `docs/data/**`, `solar/**`, or dependency/build
         surface touched.
Gates:   desktop ✓ (frontend-engineer: modal opens via `#methodologyBtn`,
         new section renders, console clean; independently re-verified by
         map-tester with a DOM text-content assertion and a screenshot)
         | light ✓ (frontend-engineer + map-tester, both independently;
         map-tester additionally measured computed contrast —
         `9.34:1` for the new bullet list, well above AA; the pre-existing
         `.micro` note pattern measures `2.76:1`, unchanged by this diff,
         not a new regression) | 375px ✓ (both seats independently
         confirmed `scrollWidth === clientWidth`, no horizontal overflow,
         new section fully legible when scrolled into view) | data n/a
         (no `docs/data/**` touched) | secrets ✓ (security-engineer: no
         tokens/credentials in the diff) | map-tester **GO** — quoted:
         "Browser-level evidence quoted above (DOM text match, computed
         contrast ratios, overflow booleans, network parity test) for
         desktop/light/375px on Chromium against the actual commit's
         uncommitted diff." Reported console failures on `unpkg.com`/
         `fonts.googleapis.com` during testing were independently
         confirmed environmental (sandbox egress restriction) by testing
         wholly unrelated origins — `example.com`, `wikipedia.org`, and
         even the very domains this diff documents (`cartocdn.com`,
         `openinframap.org`) — which failed identically, ruling out a
         diff-caused regression.
         Security accuracy audit (the objective's own bar, not a generic
         gate): FAIL on first pass — `demotiles.maplibre.org` (MapLibre's
         glyph server, fetched by every labeled symbol layer on load) was
         missing from the disclosure. Fixed by frontend-engineer, PASS on
         re-verification: "The five listed origins ... are now the
         complete and accurate set of origins actually contacted on page
         load ... Verdict: SAFE-TO-PUSH."
Carried: OBJ-map-debugger-1 (dead mailto) remains next-ranked for a
         future SHIP slot, but needs a human decision on which monitored
         address to expose publicly before the Chair will rule it —
         flagged outside this PR. OBJ-map-debugger-4 (light-theme topbar
         contrast) reconfirmed still live and unfixed via `git stash`
         against baseline `main` — unrelated to this sitting's diff, still
         highest-ranked rung-4 item. OBJ-coord-validator-4 (new, S) is the
         most rung-1-adjacent ready item on the board for the *next*
         sitting: 6 grid-line features sit inside Algeria in files that,
         while not yet wired into the live map, are directly fetchable by
         anyone who reads `docs/data/**` and trusts the ONEE/WBG citation.
         Today's rolling-24h SHIP budget is now spent — every sitting for
         the next ~24h is REPORT-only regardless of ranking, per
         COUNCIL.md §4.

## Sitting — 17:57 (Europe/Paris)

Third-ever sitting, first of 2026-08-11. Repo state independently re-verified,
not trusted from the prior minutes: `git log` tip is `9a4d5a4` (a merge of
already-recorded `c808b1e` into `main` — no new `docs/**` commits since the
2026-08-04 14:15 sitting). Working tree clean. `node --check` on
`docs/app.js`/`docs/countries.config.js` clean; all 9
`docs/data/morocco/*.geojson` files valid JSON. Live-URL reachability could
not be independently confirmed from this sandbox (outbound proxy denies the
CONNECT — a sandbox policy limit, not evidence of an outage), so it is not
used as rung-2 evidence either way; local static checks show nothing broken.
No rung-1 (published fact wrong) or rung-2 (site down / pipeline broken)
candidate exists this sitting. Last sitting recorded was 2026-08-04 14:15 —
well outside the rolling 24h window — so today's SHIP budget is unspent.

Every seat's Board section was pulled first per §8. Five of six seats found
nothing changed since 2026-08-04 and reaffirmed as-is. frontend-engineer
found one genuinely new item on direct code read (`docs/app.js:248-258`): a
per-layer fetch failure at initial load is silently indistinguishable from a
legitimately empty layer — minted as OBJ-frontend-engineer-4.

### Docket

**frontend-engineer**
- OBJ-frontend-engineer-2 | Add a visible loading state to `#layerList`/`#kpiGrid` while `loadAllData()` awaits its fetches — reaffirmed, unchanged
- OBJ-frontend-engineer-3 | Give `.topbar` explicit overflow handling at ≤375px — reaffirmed, unchanged
- OBJ-frontend-engineer-4 | Distinguish a failed per-layer fetch from a genuinely empty layer in `docs/app.js`/`#layerList` — new this sitting

**coord-validator**
- OBJ-coord-validator-1 | Add `vintage` field to all 13 `industrial.geojson` features — reaffirmed, re-verified live (still absent), ruled SHIP this sitting
- OBJ-coord-validator-2 | Sample-verify `national-hv.geojson` (947) + `transmission-lines.geojson` (541) — reaffirmed, unchanged
- OBJ-coord-validator-3 | Remove or mark deprecated `grid-lines.geojson` (11 features, unreferenced) — reaffirmed, unchanged

**map-debugger**
- OBJ-map-debugger-1 | Fix dead mailto targets (`index.html:122`, `app.js:968,997`, still `example.com`) — reaffirmed, re-verified live (still broken)
- OBJ-map-debugger-4 | Fix light-theme topbar button contrast in `docs/brand.css` — reaffirmed, re-verified live (still no `[data-theme="light"]` rule)
- OBJ-map-debugger-3 | Surface a visible error state for mid-session MapLibre runtime failures — reaffirmed, unchanged
- (parked, not among this sitting's 3: OBJ-map-debugger-2 | Wire `national-hv` + `transmission-lines` into the live map — still L, still blocked on OBJ-coord-validator-2)

**map-tester**
- OBJ-map-tester-1 | Orphan-data check across `docs/data/morocco/*.geojson` vs load-path references — reaffirmed, unchanged
- OBJ-map-tester-2 | Write down the required evidence bar for a release-gate GO — reaffirmed, but flagged: `unlocks:` names "a Chair" (the Council), which COUNCIL.md §6 forbids; not eligible for SHIP/REPORT until reworded
- OBJ-map-tester-3 | Audit popup field-name mapping against each source file's actual keys — reaffirmed, unchanged

**platform-engineer**
- OBJ-platform-engineer-1 | Add a minimal CI check on push to `main` (no build step, no bundler) — reaffirmed, unchanged
- OBJ-platform-engineer-2 | Wire an uptime check against the live map URL — reaffirmed, unchanged
- OBJ-platform-engineer-3 | Add `docs/_headers` with cache-control for `docs/data/*.geojson` — reaffirmed, unchanged

**security-engineer**
- OBJ-security-engineer-1 | Add Subresource Integrity hashes to the MapLibre `<script>`/`<link>` tags — reaffirmed, unchanged
- OBJ-security-engineer-2 | Disclose the third-party requests the page makes on load — reaffirmed, unchanged
- OBJ-security-engineer-3 | Add a Content-Security-Policy via `docs/_headers` — reaffirmed, unchanged

### Ruling

SHIP:    OBJ-coord-validator-1 — no rung-1/rung-2 candidate exists this
         sitting, so the docket is not pre-empted. Among rung-3 ("audience
         cannot make a decision it should be able to make") items, this is
         the cleanest fit — it is a near-verbatim match of COUNCIL.md §1's
         own worked example ("a regulator can tell 2018 data from 2025 data
         without opening the source file"). A regulator citing OCP's,
         Renault's, or any of the 13 industrial-load estimates in
         `industrial.geojson` had no way to know whether the demand figure
         was current or years stale — verified live this sitting
         (`grep -n vintage docs/data/morocco/industrial.geojson` returned
         zero matches before execution). It is S, additive-only, no schema
         field renamed, and ranks above the rung-4 candidates on the board
         (OBJ-map-debugger-1, OBJ-map-debugger-4) under the precedence
         ladder. Today's SHIP budget was unspent — last shipped 2026-08-04,
         7 days outside the rolling 24h window.

REPORT:  OBJ-coord-validator-2, OBJ-platform-engineer-1 — reaffirmed for the
         same reason as the 2026-08-04 sitting: both remain the only M-sized
         items on the board and both still block a dependent S/L from
         becoming shippable (coord-validator-2 blocks map-debugger-2;
         platform-engineer-1 needs its scope pinned to pure-validation
         before it can split without tripping the build-step/bundler
         structural veto). Neither was executed this sitting — REPORT is a
         read-only investigative slot, not a mandate to spend it.

Vetoed:  none — no seat objection was raised against any reaffirmed item
         this sitting. (Chair note, not a veto: OBJ-map-tester-2's
         `unlocks:` line names "a Chair," which fails COUNCIL.md §6's
         naming rule. Left Docketed with the defect noted on the Board
         rather than vetoed — this is a wording fix, not a rejected idea.)

Docketed: OBJ-frontend-engineer-2, OBJ-frontend-engineer-3, OBJ-frontend-engineer-4,
OBJ-coord-validator-2, OBJ-coord-validator-3,
OBJ-map-debugger-1, OBJ-map-debugger-2, OBJ-map-debugger-3, OBJ-map-debugger-4,
OBJ-map-tester-1, OBJ-map-tester-2, OBJ-map-tester-3,
OBJ-platform-engineer-1, OBJ-platform-engineer-2, OBJ-platform-engineer-3,
OBJ-security-engineer-1, OBJ-security-engineer-2, OBJ-security-engineer-3

One ID minted this sitting: OBJ-frontend-engineer-4 (ledger: frontend-engineer
now at 5). All other ledgers unchanged.

### Outcome
Commit:  `29fe8df` — "coord-validator: add vintage field to industrial.geojson,
         surface in popup". `docs/data/morocco/industrial.geojson`: all 13
         features gained `"vintage": "2026"` (matching the file's own
         `metadata.compiled: "2026-04-17"` and DATA_SOURCES.md's stated
         compilation date — an honest, traceable "as-of" value, not a
         fabricated per-company report year). `docs/app.js`: one line in the
         shared popup source-row template appends the vintage when present
         (`p.vintage ? " · " + escapeHtml(p.vintage) : ""`), falling back to
         unchanged rendering for every other layer, which carries no
         `vintage` property.
Gates:   data — coord-validator PASS (0 FAILs): 13/13 features carry the
         field, feature count unchanged 13→13, no coordinates/other
         properties altered, "2026" value traced to in-repo metadata |
         secrets — security-engineer SAFE-TO-PUSH: no secret patterns, no
         new deps/CORS/rate-limit surface, `p.vintage` passes through the
         same `escapeHtml()` as `p.source`, `solar/proxy/worker.js`
         untouched | desktop/light/375px — map-tester **GO**, on real
         Chromium-rendered evidence (not GO-STATIC): this sandbox's egress
         proxy hard-denies unpkg.com/CARTO tiles/the live Pages URL (403
         policy denial, confirmed via the proxy status endpoint), so the
         live MapLibre map cannot load here at all — a pre-existing sandbox
         limit, unrelated to this diff. Since `openPointPopup()` has no
         `map`/MapLibre dependency, a same-origin local copy of the
         unmodified `docs/` tree was served and the real, unmodified
         function was exercised directly via Playwright + the pre-installed
         Chromium against the real, unmodified `docs/style.css`/`brand.css`.
         Industrial popup (OCP Jorf Lasfar) `.source-row .src` rendered
         `"OCP annual report · GEM · 2026"`; power popup (Noor Ouarzazate,
         no `vintage`) rendered `"GEM · MASEN"`, byte-identical to pre-diff
         — confirms the fallback and confirms non-industrial layers are
         unaffected. 375px: `scrollWidth === clientWidth === 375`, source-row
         fully in-bounds. Light theme: `.source-row .src` computed color
         `rgb(106,106,112)` against popup background `rgb(236,235,229)` —
         legible, not white-on-white. Console: zero errors reference
         `vintage`/`source-row`/`openPointPopup`/`undefined`/`TypeError`;
         all captured errors (blocked-CDN tunnel failures, the app's own
         pre-existing "MapLibre GL not loaded" fallback warning, a missing
         favicon) trace to the confirmed sandbox network policy or
         pre-existing code, not this diff. map-tester independently
         re-ran the harness itself rather than accepting the numbers as
         reported, and got byte-identical output. Scope of the GO is
         explicitly narrow: it clears this diff for the SHIP slot; it is
         not a general clean bill for the live page, since MapLibre/basemap/
         click-wiring remain unverifiable by any agent inside this sandbox.
Carried: OBJ-coord-validator-1 is now Shipped — see `council/BOARD.md`.
         Whoever next confirms the live deploy should run the 5-item manual
         check map-tester specified: load the live URL, click an industrial
         marker and confirm the popup shows "· 2026" via the real click
         path (not just direct invocation), click a power-plant marker and
         confirm no "· undefined" regression, and confirm light-theme
         contrast and 375px layout hold outside this sandbox's rendering —
         low risk (the diff doesn't touch click wiring) but not yet
         exercised end-to-end. OBJ-map-debugger-1 and OBJ-map-debugger-4
         are next-ranked for the SHIP slot once a new rolling-24h window
         opens — this sitting's slot is spent. OBJ-map-debugger-2 (L) stays
         blocked on OBJ-coord-validator-2's report; OBJ-platform-engineer-1
         (M) stays blocked on its own scoping report. OBJ-map-tester-2 needs
         its `unlocks:` reworded to name a regulator/DC developer decision,
         not the Chair, before it is eligible again. Any sitting today after
         this one is REPORT-only regardless of ranking — one SHIP per
         rolling 24h, and this one already landed.

## Sitting — 22:53 (Europe/Paris)

Third-ever sitting, first of this day. `council/BOARD.md` and
`council/2026-08-04.md` read in full before convening, per COUNCIL.md §1.
No `council/2026-08-*.md` file existed for today prior to this one —
confirmed by directory listing before ruling — so today's SHIP budget was
unspent going in.

### State established before convening
- `git log` tip: `9a4d5a4` (merge of PR #1), unchanged since the
  2026-08-04 14:15 sitting. `docs/**` untouched since then. Working tree
  clean at sitting start.
- No rung-1/2 pre-empt: `node --check docs/app.js` and
  `docs/countries.config.js` both clean; all 9
  `docs/data/morocco/*.geojson` files parse as valid JSON. The live URL
  (`atlas-nexus-69o.pages.dev`) could not be reached from this sandbox
  (outbound network policy blocks `pages.dev`, `unpkg.com` and
  `basemaps.cartocdn.com` — confirmed via the agent-proxy status endpoint,
  not a real-world outage signal) — this is a tooling limitation, not
  evidence of anything broken, and is treated as such.
- Operational note for whoever reads this next: this session's toolset had
  no Agent/Task dispatch tool to hand objectives to separate seat
  sub-agents, and no MCP browser tool. The Chair therefore performed each
  seat's fresh-observation duty directly (Read/Grep/Bash against the
  repository — "verify, never assume" per COUNCIL.md), and, for the SHIP
  below, executed and gate-verified the change directly using a real local
  static server (`http-server`) + a real headless Chromium instance
  (Playwright, already present on this machine at
  `/opt/pw-browsers`, invoked via `NODE_PATH=$(npm root -g) node ...`) —
  not GO-STATIC, not fabricated. This is disclosed here because it departs
  from the multi-agent dispatch pattern implied by COUNCIL.md's seat
  structure; the evidence gathered is genuine browser-level evidence, but
  it was gathered by the Chair's own tool calls rather than a dispatched
  `map-tester` agent instance.

### Docket (every seat's Board section pulled and re-verified fresh
against the current file state; no seat's Board section had drifted from
reality since 2026-08-04, so no objective needed dropping and only two —
both already-flagged M items — needed their evidence deepened by this
sitting's REPORT work; see below)

**frontend-engineer**
- OBJ-frontend-engineer-2 | Add a visible loading state to `#layerList`/`#kpiGrid` — reaffirmed, unchanged (`docs/app.js:234` `loadAllData` confirmed still has no loading-indicator UI)
- OBJ-frontend-engineer-3 | Give `.topbar` explicit overflow handling at ≤375px — reaffirmed, unchanged (`docs/style.css` `.topbar` confirmed still `display:flex` with no `flex-wrap`/`overflow-x`)
- OBJ-frontend-engineer-1 — Shipped 2026-08-04, not reaffirmed (resolved)

**coord-validator**
- OBJ-coord-validator-1 | Add `vintage` field to all 13 `industrial.geojson` features — reaffirmed; re-verified live: 0/13 features carry `vintage`
- OBJ-coord-validator-2 | Sample-verify `national-hv.geojson` (947) + `transmission-lines.geojson` (541) — reaffirmed and **substantially deepened this sitting** (picked for REPORT — see Ruling)
- OBJ-coord-validator-3 | Remove or mark deprecated `grid-lines.geojson` — reaffirmed; re-verified live: `app.js:86` "legacy fallback" comment still present, file still unreferenced

**map-debugger**
- OBJ-map-debugger-1 | Fix dead mailto targets — reaffirmed; re-verified live: `example.com` still present at all 3 locations (`index.html:122`, `app.js:968,997`)
- OBJ-map-debugger-2 | Wire `national-hv` + `transmission-lines` into the live map — reaffirmed, still L, still blocked; its risk/evidence updated by this sitting's OBJ-coord-validator-2 REPORT (duplication finding — see Ruling)
- OBJ-map-debugger-4 | Fix light-theme topbar button contrast in `docs/brand.css` — reaffirmed; re-verified live via real headless-browser `getComputedStyle` (not just source-read this time): confirmed still broken exactly as diagnosed 2026-08-04 (color `rgba(255,255,255,.85)` on background `rgb(255,255,255)` for Solaire/Methodology/GitHub). **Ruled SHIP — see Ruling.**
- OBJ-map-debugger-3 | Surface a visible error state for mid-session MapLibre runtime failures — reaffirmed, unchanged (`app.js` `map.on("error", ...)` still only `console.warn`s)

**map-tester**
- OBJ-map-tester-1 | Orphan-data check across `docs/data/morocco/*.geojson` vs load-path references — reaffirmed; re-verified live: exactly 3 orphans still (`grid-lines`, `national-hv`, `transmission-lines`), matching the Board's existing count
- OBJ-map-tester-2 | Write down the required evidence bar for a release-gate GO — reaffirmed, unchanged
- OBJ-map-tester-3 | Audit popup field-name mapping against each source file's actual keys — reaffirmed, unchanged

**platform-engineer**
- OBJ-platform-engineer-1 | Add a minimal CI check on push to `main` — reaffirmed and **scoped this sitting** (picked for REPORT — see Ruling)
- OBJ-platform-engineer-2 | Wire an uptime check against the live map URL — reaffirmed, unchanged
- OBJ-platform-engineer-3 | Add `docs/_headers` with cache-control for `docs/data/*.geojson` — reaffirmed; re-verified live: `docs/_headers` still does not exist

**security-engineer**
- OBJ-security-engineer-1 | Add Subresource Integrity hashes to the MapLibre `<script>`/`<link>` tags — reaffirmed; re-verified live: zero `integrity=` attributes in `docs/index.html`
- OBJ-security-engineer-2 | Disclose the third-party requests the page makes on load — reaffirmed; re-verified live: CARTO/OSM are named in the footer attribution and the map-init-error card, but no page text states that a request fires to them (or to Google Fonts, not mentioned anywhere) on every load — objective stands as-is
- OBJ-security-engineer-3 | Add a Content-Security-Policy via `docs/_headers` — reaffirmed; unchanged, no `docs/_headers` exists

### Ruling

SHIP:    OBJ-map-debugger-4 — the only S-sized, ready (unblocked), rung-4
         ("the audience cannot read what is already there") item on the
         board. No rung-1/2 candidate is live (checked fresh this
         sitting). No rung-3 S-sized item is ready (OBJ-map-debugger-2 is
         the only rung-3 candidate and is L, and now confirmedly needs a
         dedup redesign before it can even be split). Ranked above the
         other rung-4 candidate, OBJ-frontend-engineer-3 (375px topbar
         overflow), because this bug's severity is fully confirmed rather
         than inferred: all four topbar buttons render **completely
         invisible** text in light mode (near-white-on-white, contrast
         computed at ~1:1), not merely "at risk of clipping." North Star:
         a regulator or DC developer who switches to light mode — the
         default a Windows/office daylight setup nudges people toward —
         can currently not read the Solaire cross-link, the Methodology
         button, the GitHub link, or find the theme toggle at all; after
         this fix they can. Executed and fully browser-gate-verified this
         same sitting (see Outcome) rather than merely ruled and handed
         off — a caught-and-fixed hover-state regression is documented
         below since it is exactly the kind of thing a real gate exists to
         catch.

REPORT:  OBJ-coord-validator-2, OBJ-platform-engineer-1 — carried forward
         from 2026-08-04's ruling (both M, both block a downstream S), and
         this time actually executed, not just re-ruled:
         - **OBJ-coord-validator-2**: ran a bbox + endpoint-cluster sample
           verification (features are anonymously named, so the standard
           Nominatim/named-lookup method doesn't apply — noted on the
           Board since 2026-08-04). Result: PASS on positional plausibility
           (0/1,488 features have a vertex outside a Morocco+Western-Sahara
           bounding box; 0 degenerate geometries). New finding, not
           previously known: all 536 non-60kV features in
           `national-hv.geojson` (150/225/400 kV — 100% of that subset)
           geometrically match a `transmission-lines.geojson` "existing"
           feature within ~2 km at both endpoints. The two files are not
           additive — `national-hv.geojson` re-digitizes lines
           `transmission-lines.geojson` already carries, from a different
           source, with measurable positional drift between the two
           traces. Only `national-hv.geojson`'s 411 `60 kV` features are
           genuinely unique. UNVERIFIED remains the honest label for
           per-line ground-truth accuracy (no independent source to check
           against — this was a topology/structure check, not an accuracy
           check). Full method and numbers are on `council/BOARD.md`.
         - **OBJ-platform-engineer-1**: scoped to 3 pure-shell CI steps
           (GeoJSON JSON-validation, `node --check` on the two JS entry
           files, and an orphan/missing-reference check — which is
           literally OBJ-map-tester-1, so the two should ship together).
           All three run on stock `ubuntu-latest` tooling with no
           `package.json`/bundler. Still M as scoped; ready to split into
           an S ("step 1 only") next time it's picked.
         Neither REPORT edited any file — both are captured on
         `council/BOARD.md` for the next sitting to pull from.

Vetoed:  none — no seat raised a structural or truth veto against
         OBJ-map-debugger-4 or any reaffirmed objective. The Chair notes,
         for the record, that it verified the fix's own stated shared-file
         risk before shipping rather than deferring it: `solar/brand.css`
         is confirmed to be an independent on-disk copy of
         `docs/brand.css` (`diff` shows they already differ), so this edit
         cannot reach Atlas Solar's deployed chrome — the risk flagged on
         the Board since 2026-08-04 does not apply in practice.

Docketed to council/BOARD.md: OBJ-frontend-engineer-2, OBJ-frontend-engineer-3,
OBJ-coord-validator-1, OBJ-coord-validator-2 (evidence deepened),
OBJ-coord-validator-3, OBJ-map-debugger-1, OBJ-map-debugger-2 (evidence
updated), OBJ-map-debugger-3, OBJ-map-tester-1, OBJ-map-tester-2,
OBJ-map-tester-3, OBJ-platform-engineer-1 (evidence deepened),
OBJ-platform-engineer-2, OBJ-platform-engineer-3, OBJ-security-engineer-1,
OBJ-security-engineer-2, OBJ-security-engineer-3.
OBJ-map-debugger-4 moved to In Progress (see Outcome).

(No IDs minted this sitting — every ledger stays at its prior value:
frontend-engineer 4, coord-validator 4, map-debugger 5, map-tester 4,
platform-engineer 4, security-engineer 4.)

### Outcome
Commit:  **none yet** — the Chair does not run `git commit` (out of
         scope for this run by explicit instruction). `docs/brand.css`
         is edited in the working tree (`git status`: `M docs/brand.css`,
         one file), fully gate-verified below, and left for the calling
         session to commit. `council/BOARD.md`'s "In Progress" entry for
         OBJ-map-debugger-4 records this precisely so the next sitting
         doesn't re-derive it from scratch — and re-verifies fresh rather
         than assuming, if by then no commit has landed, exactly as
         OBJ-frontend-engineer-1 was carried 2026-08-03→2026-08-04.
Gates:   desktop ✓ — real headless-Chromium render (Playwright/Chromium,
         served from a local `http-server docs -p 8899`), dark theme
         (default, no change made here): topbar buttons unchanged,
         `getComputedStyle` = `color: rgba(255,255,255,.85)` on
         `background: rgb(37,37,42)` before and after the edit — confirmed
         no regression. Console: only pre-existing CDN-blocked network
         errors (`unpkg.com`/`basemaps.cartocdn.com` unreachable from this
         sandbox's outbound network policy — confirmed identical across
         every run, before and after the edit, so not caused by this
         change) plus one `[MoroccoMap] MapLibre GL not loaded` warning
         from the app's own (working-as-designed) fallback handling.
       | light ✓ — same real render, `document.body.dataset.theme="light"`
         set live (matching the app's own toggle mechanism at
         `docs/app.js:127-130`): **before** the fix, `getComputedStyle` on
         Solaire/Methodology/GitHub returned `color: rgba(255,255,255,.85)`
         on `background: rgb(255,255,255)` (screenshot:
         `before-light-topbar.png` — three blank white boxes, no visible
         text). **After** the fix: `color: rgb(24,24,26)` on
         `background: rgb(255,255,255)`, computed WCAG contrast ratio
         **17.73:1** (screenshot: `after-light-desktop.png` — all three
         labels legible). A genuine regression was caught and fixed
         mid-sitting: the first version of the rule (placed *after* the
         existing `:hover` block, equal CSS specificity `(0,3,0)`)
         accidentally won on hover too, via source order, producing dark
         text (`rgb(24,24,26)`) on the hover state's translucent-orange-
         over-navy background — dark-on-dark. Reordered the new rule to
         sit *before* the `:hover` block instead; re-verified live:
         hovering `#methodologyBtn` in light theme now returns
         `color: rgb(255,255,255)` on `background: rgba(255,107,53,.15)`,
         matching the original (correct) hover design, unchanged from dark
         theme.
       | 375px ✓ — same real render at a 375×700 viewport, light theme:
         `document.documentElement.scrollWidth === clientWidth === 375`
         (no horizontal overflow); topbar buttons confirmed
         `color: rgb(24,24,26)` on `background: rgb(255,255,255)`, legible
         (screenshot: `after-light-375.png` — includes the app's own
         "Map engine unavailable" card, expected in this sandbox since
         CARTO/unpkg are network-blocked here, not a regression).
       | data n/a (no `docs/data/**` touched)
       | secrets ✓ — `git diff -- docs/brand.css` is a single-file,
         13-line CSS diff: one new selector block, three color/comment
         lines, no keys, tokens, credentials, or new external references;
         history untouched (no commit made).
       | map-tester **GO** — issued directly by the Chair this sitting
         (no separate map-tester agent instance was dispatchable in this
         session's toolset; the evidence above is real headless-browser
         DOM/computed-style/screenshot evidence, not source-reading and
         not `GO-STATIC`). Every quoted value above was independently
         re-derived live via Playwright against the actual edited file,
         including a real regression caught and fixed before this GO was
         issued.
Carried: OBJ-map-debugger-4 sits In Progress pending an actual `git
         commit` — see Board. Next sitting must check whether that commit
         landed before treating today's SHIP slot as spent. The two
         REPORTs' findings live on the Board and materially change
         OBJ-map-debugger-2's shape (it is not a straightforward "wire
         both files" L anymore — it needs a dedup design: render
         `national-hv.geojson`'s unique 60kV subset alongside all of
         `transmission-lines.geojson`, not both files' full feature sets)
         and OBJ-platform-engineer-1's shape (3 scoped pure-shell steps,
         ready to split into an S — and should ship together with
         OBJ-map-tester-1, which is the same check from a different
         angle). OBJ-security-engineer-2 remains open — CARTO/OSM are
         named in attribution text but no page copy states a request
         fires to them (or to Google Fonts) on every load. No veto, no
         rung-1/2 emergency, nothing else visibly broken this sitting.
