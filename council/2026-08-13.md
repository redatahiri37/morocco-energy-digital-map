# Council sitting — 2026-08-13

## Sitting — 09:23 (Europe/Paris)

First sitting since 2026-08-04 (14:15). Read the Board first, then the
2026-08-04 minutes, per the standing order — nothing was regenerated from
scratch. `git log` tip since that sitting: only `4100942` (council-minutes
commit) and merge `9a4d5a4` (PR #1) landed, both paperwork — `git diff
c808b1e..HEAD --stat` touches zero files under `docs/`. No commit has
shipped since `c808b1e` (2026-08-04, keyboard-focusable layer toggles).

### State established before convening
- `node --check docs/app.js` / `docs/countries.config.js`: clean. All 9
  `docs/data/morocco/*.geojson` files parse as valid JSON. No rung-1
  (published fact wrong) or rung-2 (site down / broken pipeline) candidate
  exists this sitting.
- No `council/2026-08-13.md` existed before this sitting — today's 24h SHIP
  budget was unspent at convening time.
- Both bugs carried from the last sitting's "Carried" note were re-verified
  live in source before convening: the 3 dead `mailto:reda.tahiri@example.com`
  links (`docs/index.html:122`, `docs/app.js:968,997`) and the light-theme
  topbar contrast bug (`docs/brand.css:32-36`, no `[data-theme="light"]`
  override) are both still present, unchanged.

### Docket (pulled from council/BOARD.md; each seat verified against
current source before reaffirming — see individual seat reports for detail)

**frontend-engineer**
- OBJ-frontend-engineer-2 | Loading state for `#layerList`/`#kpiGrid` — reaffirmed, unchanged (`app.js:234-262`, `265-267`, `324-325`)
- OBJ-frontend-engineer-3 | `.topbar` overflow handling at ≤375px — reaffirmed, unchanged (`style.css:77-84`, `551-555`)
- OBJ-frontend-engineer-4 | Escape-key handler to close the open feature popup — **new**, minted this sitting (zero keydown handling anywhere in `app.js`, confirmed by whole-file grep)

**coord-validator**
- OBJ-coord-validator-1 | Add `vintage` field to all 13 `industrial.geojson` features — reaffirmed, unchanged
- OBJ-coord-validator-2 | Sample-verify `national-hv.geojson` (947) + `transmission-lines.geojson` (541) — reaffirmed; **REPORT executed this sitting**, see Ruling
- OBJ-coord-validator-3 | Remove/mark deprecated `grid-lines.geojson` (11 features, unreferenced) — reaffirmed, unchanged

**map-debugger**
- OBJ-map-debugger-1 | Fix dead mailto targets — reaffirmed, ranked #1 by the seat itself; **ruled SHIP this sitting, gate failed**, see Ruling
- OBJ-map-debugger-4 | Fix light-theme topbar contrast (`brand.css`) — reaffirmed, unchanged; cross-app risk noted (shared with Atlas Solar)
- OBJ-map-debugger-3 | Visible error state for mid-session MapLibre runtime failures — reaffirmed, unchanged (`app.js:224` still only `console.warn`s)
- OBJ-map-debugger-2 | Wire `national-hv` + `transmission-lines` into the live map (L) — reaffirmed, stays blocked pending a splittable slice; OBJ-coord-validator-2's REPORT this sitting clears the *data-trust* half of that block (0/1488 structural FAIL) but the wiring/perf work itself remains unscoped and L

**map-tester**
- OBJ-map-tester-1 | Orphan-data check across `docs/data/morocco/*.geojson` — reaffirmed, unchanged (3 orphans reconfirmed: `grid-lines`, `national-hv`, `transmission-lines`)
- OBJ-map-tester-2 | Write down the release-gate GO evidence bar — reaffirmed, unchanged
- OBJ-map-tester-3 | Audit popup field-name mapping against source keys — reaffirmed, unchanged (`app.js:936,985` still read `p.precision` against files that don't carry that key)

**platform-engineer**
- OBJ-platform-engineer-1 | Minimal CI check on push to `main` (M) — reaffirmed; **REPORT executed this sitting, split into 2 S-slices**, see Ruling
- OBJ-platform-engineer-2 | Uptime check against the live map URL — reaffirmed, unchanged; seat's own read: genuinely S and ready to ship, just not ranked highest this sitting
- OBJ-platform-engineer-3 | `docs/_headers` cache-control for `docs/data/*.geojson` — reaffirmed, unchanged

**security-engineer**
- OBJ-security-engineer-1 | SRI hashes on the MapLibre `<script>`/`<link>` tags — reaffirmed, unchanged
- OBJ-security-engineer-2 | Disclose third-party requests made on load — reaffirmed, unchanged
- OBJ-security-engineer-3 | CSP via `docs/_headers` — reaffirmed, unchanged; seat flags this needs full 3-gate testing before it can be an unattended SHIP, given the rung-2 risk of an overly strict policy

### Ruling

SHIP:    OBJ-map-debugger-1 — ruled SHIP. Highest-ranked live rung-3 item
         (a broken correction channel undermines the trust §1 requires of
         the whole map), S-sized, zero structural-veto exposure, and
         security-engineer cleared the actual diff with no veto ("an email
         address in a mailto link is contact info, not a credential").
         **Execution ran, but the Release gate failed**: map-debugger
         implemented the fix (`reda.tahiri@example.com` →
         `reda.tahiri1@gmail.com` in `docs/index.html:122`,
         `docs/app.js:968,997`; `node --check` clean; zero remaining
         `example.com` occurrences in `docs/`). map-tester's independent
         verdict was **GO-STATIC, not GO**: this sandbox's outbound proxy
         returns `403 Forbidden` on `unpkg.com` (confirmed independently
         via `curl`), which blocks MapLibre GL from loading at all, so the
         two dynamic-popup mailtos (only rendered inside a clicked
         feature's popup) could not be click-tested live — only
         source-grep-verified against an unmodified template literal. The
         static `docs/index.html:122` link *was* DOM-verified live across
         desktop/light/375px with zero horizontal overflow and no new
         console errors. Per COUNCIL.md §5, "`GO-STATIC` is not shippable;
         it is a receipt that only outside checks ran" — this is a Truth
         gate, not a seat veto, and the Chair cannot waive it. **The diff
         was reverted; nothing committed.** Today's SHIP budget remains
         unspent.

REPORT:  OBJ-coord-validator-2, OBJ-platform-engineer-1 — both executed in
         full this sitting (read-only, no edits):
         — **coord-validator-2**: full-population bbox/NaN/degenerate-geometry
           scan across all 1,488 `national-hv.geojson` + `transmission-lines.geojson`
           features: 0 FAIL. Endpoint-plausibility spot-check (n=30,
           haversine against `power-plants.geojson`/`industrial.geojson`):
           30/30 PASS. Longest line (502 km) matches the known
           Agadir–Laâyoune 400kV corridor. Absolute position stays
           UNVERIFIED for all 1,488 features — generic names mean no
           independent geocoder applies; this is an inherent ceiling, not a
           gap in the method. Found 10 exact-geometry duplicate features (5
           pairs per file) — filed as new OBJ-coord-validator-4.
         — **platform-engineer-1**: split into two independent S-slices —
           new OBJ-platform-engineer-4 (GitHub Actions JSON-validation of
           `docs/data/morocco/*.geojson` on push/PR, one new file,
           `python3` preinstalled on `ubuntu-latest`, no build step) and
           new OBJ-platform-engineer-5 (deferred: verify every
           `layers[].file` in `docs/countries.config.js` resolves to a real
           file — the inverse of OBJ-map-tester-1's orphan check). Slice 4
           ships first; slice 5 needs config-parsing and is more surface.

Vetoed:  none — security-engineer reviewed the actual mailto diff and
         cleared it (no veto); the SHIP was blocked by a Release-gate
         failure, not a seat veto. No other objective was ranked into
         contention this sitting.

New objectives minted this sitting: OBJ-frontend-engineer-4,
OBJ-coord-validator-4, OBJ-platform-engineer-4, OBJ-platform-engineer-5.

Docketed to council/BOARD.md: OBJ-frontend-engineer-2, OBJ-frontend-engineer-3,
OBJ-frontend-engineer-4, OBJ-coord-validator-1, OBJ-coord-validator-2,
OBJ-coord-validator-3, OBJ-coord-validator-4, OBJ-map-debugger-1,
OBJ-map-debugger-2, OBJ-map-debugger-3, OBJ-map-debugger-4,
OBJ-map-tester-1, OBJ-map-tester-2, OBJ-map-tester-3,
OBJ-platform-engineer-2, OBJ-platform-engineer-3, OBJ-platform-engineer-4,
OBJ-platform-engineer-5, OBJ-security-engineer-1, OBJ-security-engineer-2,
OBJ-security-engineer-3. (OBJ-platform-engineer-1 (M) kept on the Board,
marked superseded by its split — IDs are never renumbered or removed.)

### Outcome
Commit:  none — gate failed. The only change this sitting is to
         `council/BOARD.md` and this minutes file; no product code
         (`docs/**`) changed on `main` or on this branch.
Gates:   desktop n/a | light n/a | 375px n/a | data n/a (no `docs/data/**`
         committed) | secrets ✓ (diff reviewed and cleared, but the diff
         itself was reverted, not committed) | map-tester **GO-STATIC**
         (not shippable — quoted: "the sandbox's outbound proxy blocks
         `unpkg.com` wholesale... this caps the verdict... I cannot
         honestly produce clean-console evidence for the two dynamic
         popups")
Carried: OBJ-map-debugger-1 remains the top-ranked SHIP candidate — retry
         it from an environment where `unpkg.com` is reachable (the live
         GitHub Pages deploy, or a machine with normal internet egress), or
         consider vendoring `maplibre-gl` locally if this sandbox
         limitation recurs across sittings (a future objective for a seat
         to file, not decided here). OBJ-coord-validator-2's core question
         is now answered (structurally sound, 0 FAIL) — the new
         OBJ-coord-validator-4 (dedup) and the still-unscoped wiring half
         of OBJ-map-debugger-2 (L) are what's left before that data can
         reach the live map. OBJ-platform-engineer-4 (S) is ready to be a
         future sitting's SHIP once map-debugger-1 or another item takes
         this slot. Next sitting: re-attempt OBJ-map-debugger-1 first: if
         it clears a real GO, ship it; today's budget is still unspent
         either way since nothing landed today.
