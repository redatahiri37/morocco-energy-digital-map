# Council sitting — 2026-08-13

## Sitting — 18:05 (Europe/Paris)

Third sitting overall, first of 2026-08-13 — and the first sitting in **9
days**: the last recorded sitting was 2026-08-04 15:18 (Europe/Paris) per
`council/2026-08-04.md`, last commit landed same day (`c808b1e`, merged as
`9a4d5a4`). No incident and no explanation on record for the gap — noted
here plainly rather than papered over; carried below as an open question
for the user.

### State established before convening
- `git log` tip was `9a4d5a4` at sitting start; `git status` clean, HEAD
  tracking `origin/main`, working tree matched `main`. No commits, code, or
  data changes since the 2026-08-04 sitting.
- Rung-1/rung-2 pre-empt check, verified fresh rather than assumed:
  `node --check docs/app.js` and `docs/countries.config.js` both clean; all
  9 `docs/data/morocco/*.geojson` files parse as valid JSON. No published
  fact was found wrong and no local evidence of breakage.
- Attempted a live reachability check against the deployed map URL; the
  sandbox's outbound proxy denied the connection before reaching the host —
  an environment restriction, not evidence the site is down or up. Recorded
  as an open gap, not treated as a rung-2 finding either way.
- `council/BOARD.md`'s Shipped table held exactly one row
  (OBJ-frontend-engineer-1, 2026-08-04, `c808b1e`) — confirmed today's
  rolling-24h SHIP budget was unspent at sitting start.
- Board content totalled 18 objectives across 6 seats (frontend-engineer 2,
  coord-validator 3, map-debugger 4, map-tester 3, platform-engineer 3,
  security-engineer 3). Every seat pulled from its own Board section first
  (COUNCIL.md §8) and reaffirmed as-is; nothing changed since 2026-08-04, so
  no seat filed anything genuinely new.

### Docket (all 18, reaffirmed unchanged from `council/BOARD.md` — no new proposals)

**frontend-engineer**
- OBJ-frontend-engineer-2 | Add a visible loading state to `#layerList`/`#kpiGrid` while `loadAllData()` awaits its fetches — S
- OBJ-frontend-engineer-3 | Give `.topbar` explicit overflow handling at ≤375px — S

**coord-validator**
- OBJ-coord-validator-1 | Add `vintage` field to all 13 `industrial.geojson` features — S
- OBJ-coord-validator-2 | Sample-verify `national-hv.geojson` (947) + `transmission-lines.geojson` (541) — M
- OBJ-coord-validator-3 | Remove or mark deprecated `grid-lines.geojson` (11 features, unreferenced) — S

**map-debugger**
- OBJ-map-debugger-1 | Fix dead mailto targets (`index.html:122`, `app.js:968,997`) — S — **SHIPPED this sitting**
- OBJ-map-debugger-2 | Wire `national-hv` + `transmission-lines` into the live map — L (blocked on OBJ-coord-validator-2)
- OBJ-map-debugger-4 | Fix light-theme topbar button contrast in `docs/brand.css` — S
- OBJ-map-debugger-3 | Surface a visible error state for mid-session MapLibre runtime failures — S

**map-tester**
- OBJ-map-tester-1 | Orphan-data check across `docs/data/morocco/*.geojson` vs load-path references — S
- OBJ-map-tester-2 | Write down the required evidence bar for a release-gate GO — S
- OBJ-map-tester-3 | Audit popup field-name mapping against each source file's actual keys — S

**platform-engineer**
- OBJ-platform-engineer-1 | Add a minimal CI check on push to `main` (no build step, no bundler) — M
- OBJ-platform-engineer-2 | Wire an uptime check against the live map URL — S
- OBJ-platform-engineer-3 | Add `docs/_headers` with cache-control for `docs/data/*.geojson` — S

**security-engineer**
- OBJ-security-engineer-1 | Add Subresource Integrity hashes to the MapLibre `<script>`/`<link>` tags — S
- OBJ-security-engineer-2 | Disclose the third-party requests the page makes on every load — S
- OBJ-security-engineer-3 | Add a Content-Security-Policy via `docs/_headers` — S (risk: medium — must not break tile loading)

### Ruling

SHIP:    OBJ-map-debugger-1 — Fix the dead `mailto:` correction-report
         targets. No rung-1 (wrong published fact) or rung-2 (site down /
         broken pipeline) candidate existed this sitting — clean JS syntax,
         all GeoJSON valid, no incident on record; live reachability was
         inconclusive (sandbox proxy denial), not treated as evidence
         either way. This was the highest-ranked S-sized item on the board:
         a completely non-functional feature — all three "report an error"
         entry points pointed at `reda.tahiri@example.com`, an
         IANA-reserved documentation domain (RFC 2606) that cannot receive
         mail — meaning a regulator or DC developer who spotted a wrong
         coordinate had no way to get it corrected through the page's only
         offered channel. Directly serves the map's truth obligation to its
         regulator audience. S, zero risk (plain string replacement, no
         logic/schema/data change), and was already next-ranked in the
         2026-08-04 minutes' Carried section — reaffirmed, not re-derived.
         Today's SHIP budget was unspent (Shipped table held only the
         2026-08-04 row; 9 days had passed).

REPORT:  OBJ-coord-validator-2, OBJ-platform-engineer-1 — reaffirmed for the
         same reason as 2026-08-04, not re-litigated: nothing changed to
         alter either need. coord-validator-2 (sample-verifying 1,488
         unchecked ONEE/WBG line features) must resolve before
         OBJ-map-debugger-2 (L) can split into a shippable S.
         platform-engineer-1 (CI check) needs its scope pinned to a
         pure-validation checklist before it can split into an S without
         risking the build-step/bundler structural veto. Neither has been
         executed yet — carried forward again.

Vetoed:  none — every reaffirmed objective still sits inside the standing
         structural and truth vetoes; nothing new was proposed that needed
         testing against one, and no seat exercised a veto.

Docketed to council/BOARD.md: OBJ-frontend-engineer-2, OBJ-frontend-engineer-3,
OBJ-coord-validator-1, OBJ-coord-validator-2, OBJ-coord-validator-3,
OBJ-map-debugger-2, OBJ-map-debugger-3, OBJ-map-debugger-4,
OBJ-map-tester-1, OBJ-map-tester-2, OBJ-map-tester-3,
OBJ-platform-engineer-1, OBJ-platform-engineer-2, OBJ-platform-engineer-3,
OBJ-security-engineer-1, OBJ-security-engineer-2, OBJ-security-engineer-3

(No IDs minted this sitting — every ledger stays at its 2026-08-04 value:
frontend-engineer 4, coord-validator 4, map-debugger 5, map-tester 4,
platform-engineer 4, security-engineer 4.)

### Outcome
Commit:  `9de5a15` — "map: point correction-report mailto links at a real
         address". Executed by frontend-engineer after this ruling:
         `docs/index.html:122`, `docs/app.js:968`, `docs/app.js:997` — the
         placeholder `mailto:reda.tahiri@example.com` (RFC 2606
         documentation domain, non-deliverable) replaced with
         `mailto:reda.tahiri1@gmail.com` (the Council operator's real
         account address, sourced from session context since the objective
         as filed named no replacement). No other bytes changed in either
         file; repo-wide sweep confirmed zero remaining occurrences of the
         old address in `docs/`. `docs/data/**` and `solar/**` untouched.
Gates:   desktop ✓ (console clean of app-attributable errors; footer link
         DOM-read as `mailto:reda.tahiri1@gmail.com?subject=MoroccoMap%20data%20correction`;
         the exact `app.js:968`/`:997` template-literal expressions
         independently re-executed with sample feature data by map-tester,
         confirming both popup mailto targets resolve correctly with
         properly percent-encoded subject/body) | light ✓ (theme toggled
         via the real `#themeToggle` control, `body.dataset.theme==="light"`
         confirmed, no new console entries, no visual regression — expected
         for an href-only change) | 375px ✓ (`scrollWidth===clientWidth===375`,
         no horizontal overflow; footer link fully within viewport bounds
         and a real touch-tap succeeded) | data n/a (`docs/data/**`
         untouched) | secrets ✓ — security-engineer verdict: **SAFE-TO-PUSH
         (Secrets gate: PASS)** — diff scoped to exactly 3 mailto-string
         substitutions, zero secret-pattern matches; flagged non-blocking:
         a personal Gmail address is now served in plaintext to every
         visitor in perpetuity (intended, per the objective's own
         "real, monitored address" requirement) | release — map-tester
         verdict: **GO** — independently reproduced all browser-level
         evidence above rather than trusting the executing seat's report;
         one disclosed non-blocking caveat: automated tooling cannot
         confirm the new address is actually *monitored* (receiving mail),
         only that it is syntactically correct and wired correctly in all
         3 locations — full closure of "monitored" requires a real
         send-and-check, left as a manual step for the user | deploy n/a
         (this Council never deploys — PR only, per COUNCIL.md hard rules).
Carried: OBJ-map-debugger-1 is now Shipped — see `council/BOARD.md`.
         Next sitting is REPORT-only regardless of ranking — one SHIP per
         rolling 24h, and this one landed today at 18:05. OBJ-map-debugger-4
         (light-theme topbar contrast) is next-ranked for the SHIP slot
         once tomorrow's budget resets. OBJ-map-debugger-2 (L) stays
         blocked on OBJ-coord-validator-2's report; OBJ-platform-engineer-1
         (M) stays blocked on its own scoping report. The 9-day gap between
         sittings is carried as an open question for the user — not a
         ruling this Chair can resolve. One manual follow-up is owed to the
         user, not automatable: send a real test email to
         `reda.tahiri1@gmail.com` via the new links to confirm the address
         is actually monitored, closing the objective's "monitored" claim
         fully (map-tester's disclosed evidence gap).
