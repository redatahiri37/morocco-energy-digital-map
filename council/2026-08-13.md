# Council sitting — 2026-08-13

## Sitting — 22:53 (Europe/Paris)

**Headline finding, read before the docket below:** the Council's own
delivery pipeline is broken, and it is far more serious than a normal
sitting's business. Since the last commit that actually reached `main`
(`9a4d5a4`, merging PR #1 on 2026-08-04), every subsequent sitting —
**27 of them**, running faithfully every ~5 hours exactly as scheduled —
has produced an open pull request that nobody has merged. `main` is
frozen at the 2026-08-04 state. Full detail in the "State" and "Escalation"
sections below; this is not a Board objective (it fails the North Star
Test — the beneficiary is the Council's operator, not a regulator or DC
developer — a prior sitting, 2026-08-12 21:04, reasoned through this
exact question and reached the same conclusion), so it is recorded here
as Chair narrative and escalated directly instead.

### State established before convening
- `git log` tip: `9a4d5a4`, unchanged since 2026-08-04. Working tree
  clean, `HEAD` was tracking `origin/main` before this branch was cut.
- Rung-1/rung-2 pre-empt check, verified fresh: `node --check docs/app.js`
  and `docs/countries.config.js` both clean; all 9
  `docs/data/morocco/*.geojson` files parse as valid JSON/GeoJSON via a
  direct Python load, not assumed. A live reachability probe against
  `https://atlas-nexus-69o.pages.dev/` returned `HTTP 000` — the sandbox's
  outbound proxy denies the connection before reaching the host, same
  inconclusive result prior sittings recorded. Not treated as rung-2
  evidence either way; the map's own code and data are internally sound.
- **27 open, unmerged pull requests** found on `origin`
  (`council/2026-08-04-1303` … `council/2026-08-13-1805`, PR #2–#28),
  every one branched from the same stale `9a4d5a4` tip, every one
  containing a full sitting's worth of independent work — objectives
  filed, seats convened, gates run, commits made — that never reached
  `main`. Confirmed by reading several of these branches directly
  (not taking their PR titles on faith):
  - **`council/2026-08-13-0415`** (PR #26, this morning) ruled SHIP on
    `OBJ-map-debugger-4` (light-theme topbar contrast) and executed it.
  - **`council/2026-08-13-0923`** (PR #27) independently ruled SHIP on
    `OBJ-map-debugger-1` (dead mailto targets) — its own minutes record
    the release gate **failed** that attempt.
  - **`council/2026-08-13-1805`** (PR #28, this evening) independently
    ruled SHIP on `OBJ-map-debugger-1` again and this time it passed —
    commit `9de5a15` on that branch fixes the mailto targets.
  - Because every sitting starts from the same frozen `main` and has no
    visibility into any other unmerged branch, **three separate
    autonomous SHIP executions happened today alone** — each sitting
    correctly believed, from its own (blind) view of `main`, that
    today's budget was unspent. The rule in COUNCIL.md §4 was followed
    to the letter every time; it is the merge backlog that is defeating
    its intent.
  - Real, substantive work is trapped in this backlog, not just
    duplicate mailto fixes: `council/2026-08-12-2104` fully resolved
    `OBJ-coord-validator-2` (0 FAILs across the full 1,488-feature
    `national-hv`/`transmission-lines` population, with a corrected bbox
    and a documented ~952-distinct-corridor caveat) and fully scoped
    `OBJ-platform-engineer-1` down from M to a ready-to-ship S (a
    drafted, locally-verified `.github/workflows/docs-ci.yml`). Neither
    result is visible from `main` today. Different unmerged branches have
    also minted **colliding IDs** — two different `OBJ-frontend-engineer-4`
    proposals exist on different branches (tooltip viewport clamping vs.
    modal Escape-key handling), which will conflict the moment more than
    one of these branches is merged.
- Given the above, **this sitting rules itself REPORT-only before even
  reaching the ranking step**: multiple sittings today already exercised
  the SHIP slot (successfully, in at least one case). Re-shipping
  already-shipped-elsewhere work would only add a fourth near-duplicate
  PR to an already-unmanageable pile.
- One genuinely new finding, verified directly against the current
  `docs/app.js` on `main` (not inherited from any other branch): see
  `OBJ-map-debugger-5` below. It is filed to the Board as a normal
  Chair-verified seat objective, not executed (REPORT-only sitting).

### Docket (pulled from `council/BOARD.md`, reaffirmed unless marked)

**frontend-engineer**
- OBJ-frontend-engineer-2 | Visible loading state for `#layerList`/`#kpiGrid` during `loadAllData()` — reaffirmed, unchanged
- OBJ-frontend-engineer-3 | `.topbar` overflow handling at ≤375px — reaffirmed, unchanged

**coord-validator**
- OBJ-coord-validator-1 | `vintage` field on all 13 `industrial.geojson` features — reaffirmed, unchanged
- OBJ-coord-validator-2 | Sample-verify `national-hv.geojson` (947) + `transmission-lines.geojson` (541) — reaffirmed; **note: already answered in full on `council/2026-08-12-2104` (0 FAILs) — not re-run today to avoid duplicating that work; the report needs merging, not repeating**
- OBJ-coord-validator-3 | Remove or mark deprecated `grid-lines.geojson` (11 features, unreferenced) — reaffirmed, unchanged

**map-debugger**
- OBJ-map-debugger-1 | Fix dead mailto targets (`index.html:122`, `app.js:968,997`) — reaffirmed on `main` (still `example.com` there); **note: already fixed and gate-passed on `council/2026-08-13-1805` (commit `9de5a15`) — that fix needs merging, not re-executing**
- OBJ-map-debugger-2 | Wire `national-hv` + `transmission-lines` into the live map — reaffirmed, L, still blocked on OBJ-coord-validator-2's report reaching `main`
- OBJ-map-debugger-3 | Visible error state for mid-session MapLibre runtime failures — reaffirmed, unchanged
- OBJ-map-debugger-4 | Light-theme topbar button contrast in `docs/brand.css` — reaffirmed on `main` (bug still present there); **note: already fixed on `council/2026-08-13-0415` — needs merging, not re-executing**
- OBJ-map-debugger-5 | *(new — minted this sitting, Chair-verified)* Give `"interconnectors"` and `"planned-corridors"` their own MapLibre source ids in `buildLineLayer()` (`docs/app.js:527-553`)
  unlocks: a DC developer checking whether Morocco already has operational cross-border interconnection capacity (the 2×400 kV Spain–Morocco links) sees them on the map, instead of nothing — confirmed by direct source read and cross-check against both GeoJSON files: `buildMapLayers()` calls `buildLineLayer("interconnectors", …)` then `buildLineLayer("planned-corridors", …)` (`app.js:488-494`); both hardcode `srcId = "src-grid"` (`app.js:528`) and `addOrReplace()` (`app.js:522-525`) removes-and-replaces the *source*, so the second call overwrites the first's data before any paint occurs — the `src-grid` source ends up holding only `planned-corridors.geojson`'s 8 features (all `status:"planned"`), and `interconnectors.geojson`'s 3 real features (2 `status:"operational"` 400 kV links + 1 `status:"idle"` link) are never in the source at all. Since `lyr-grid-hv/mv/lv` filter on `status:"operational"`, those layers now always render zero features — the "Interconnectors" checkbox visibly does nothing, in either theme, at any zoom.
  evidence: after the fix, toggling "Interconnectors" renders the 2 operational Spain–Morocco lines and the 1 idle Algeria–Morocco line; toggling "Planned corridors" independently renders its own 8 features; both can be on simultaneously without either overwriting the other's source
  size: S
  risk: low — give each call its own `srcId` (e.g. derived from `dataLayerId`) and register both sets of layer ids in `layersFor()`'s `"grid"` branch instead of one shared list; no schema change, no new dependency
  **Not yet on `council/BOARD.md`'s copy on any other branch under this exact framing** — two other unmerged branches independently found the same root cause under different IDs/wording (`council/2026-08-12-2104`'s abandoned `OBJ-map-debugger-5` draft, tooltip-clamping's sibling proposal). Whoever reconciles the backlog should treat these as duplicates of one bug, not three.

**map-tester**
- OBJ-map-tester-1 | Orphan-data check across `docs/data/morocco/*.geojson` vs. load-path references — reaffirmed, unchanged
- OBJ-map-tester-2 | Written evidence bar for a release-gate GO — reaffirmed, unchanged
- OBJ-map-tester-3 | Popup field-name mapping audit against each source file's real keys — reaffirmed, unchanged

**platform-engineer**
- OBJ-platform-engineer-1 | Minimal CI check on push to `main` — reaffirmed; **note: already fully scoped and downgraded M→S on `council/2026-08-12-2104` (a drafted, locally-verified `.github/workflows/docs-ci.yml`) — needs merging, not re-scoping**
- OBJ-platform-engineer-2 | Uptime check against the live map URL — reaffirmed, unchanged
- OBJ-platform-engineer-3 | `docs/_headers` cache-control for `docs/data/*.geojson` — reaffirmed, unchanged

**security-engineer**
- OBJ-security-engineer-1 | Subresource Integrity hashes on the MapLibre `<script>`/`<link>` tags — reaffirmed, unchanged
- OBJ-security-engineer-2 | Disclose third-party requests made on page load — reaffirmed, unchanged
- OBJ-security-engineer-3 | Content-Security-Policy via `docs/_headers` — reaffirmed, unchanged

### Ruling

SHIP:    none — **this sitting is REPORT-only.** Per COUNCIL.md §4, the
         Chair must check today's minutes for a prior SHIP before
         approving one; today's minutes (recoverable only by reading the
         unmerged branches directly, since `main` carries no
         `council/2026-08-13.md`) show not one but three separate
         autonomous SHIP executions already today
         (`council/2026-08-13-0415`, `-0923` [gate-failed],
         `-1805` [gate-passed, commit `9de5a15`]). The budget is spent for
         the rolling 24h several times over. Approving a fourth today,
         even for the newly-verified `OBJ-map-debugger-5`, would be
         indiscipline dressed up as thoroughness — it would sit as PR #29,
         a fifth near-duplicate branch, solving nothing about *why* none
         of the first 27 have landed.

REPORT:  none dispatched this sitting. Both standing REPORT-eligible
         M-items (`OBJ-coord-validator-2`, `OBJ-platform-engineer-1`) were
         already executed in full on `council/2026-08-12-2104` with real,
         usable findings (see State, above). Re-running either today would
         be pure duplicate compute for an answer that already exists,
         unlanded. This sitting's capacity went to verifying the backlog
         itself and to `OBJ-map-debugger-5`, which needed a direct source
         read the Chair does not normally do but which the scale of the
         backlog problem justified today.

Vetoed:  none — nothing proposed today collides with a structural or
         truth veto in COUNCIL.md §5.

Docketed to council/BOARD.md: OBJ-frontend-engineer-2, OBJ-frontend-engineer-3,
OBJ-coord-validator-1, OBJ-coord-validator-2, OBJ-coord-validator-3,
OBJ-map-debugger-1, OBJ-map-debugger-2, OBJ-map-debugger-3,
OBJ-map-debugger-4, OBJ-map-debugger-5 (new — minted this sitting),
OBJ-map-tester-1, OBJ-map-tester-2, OBJ-map-tester-3,
OBJ-platform-engineer-1, OBJ-platform-engineer-2, OBJ-platform-engineer-3,
OBJ-security-engineer-1, OBJ-security-engineer-2, OBJ-security-engineer-3

(One ID minted this sitting: OBJ-map-debugger-5, ledger map-debugger 5 → 6.
No other ledger changed. Caution for whoever reconciles the backlog: at
least one other unmerged branch may have already minted its own
`OBJ-map-debugger-5` for a different bug — check before trusting the
ledger number alone once branches start merging.)

### Outcome
Commit:  none — REPORT-only sitting, no code changes, no data changes.
Gates:   n/a (nothing executed against `docs/**` this sitting)
Carried: **For the user, not the Board — this Chair cannot resolve it:**
         the map has received zero shipped changes on `main` in 9+ days
         despite the Council sitting on schedule every ~5 hours the whole
         time and doing real, correct work each time. 27 open PRs
         (`redatahiri37/morocco-energy-digital-map` #2–#28) are stacked on
         the same stale base and need a human decision about how to
         reconcile them — likely: pick the most complete/recent branch
         per objective (e.g. `council/2026-08-13-1805` for the mailto fix,
         `council/2026-08-12-2104` for the coord-validator-2 report and
         the platform-engineer-1 CI spec), merge those forward, and close
         the rest as superseded, rather than merging all 27 in sequence
         (many touch the same lines and will conflict). Until that
         happens, every future sitting will keep re-discovering the same
         ~18 Board items and occasionally re-executing work that already
         exists, unlanded, elsewhere. This sitting did not attempt that
         reconciliation itself — picking which of several divergent
         branches is authoritative is an editorial/ownership decision,
         not a Council ruling, and several of the candidate branches
         contain real but *different* code (e.g. two different
         `OBJ-frontend-engineer-4` proposals) that a human should look at
         before anything is merged.
