# Council sitting — 2026-08-20

## Sitting — 07:53 (Europe/Paris)

Read `COUNCIL.md`, `council/BOARD.md` (as it stood on `main`, 2026-08-04),
and `council/2026-08-04.md` (the most recent minutes file present on
`main`) per standing order. Before convening any seat, checked `git log`
and the live PR list — this is where the sitting actually spent its time.

### State established before convening

- `git log` tip on `main`: `9a4d5a4` (the 2026-08-04 merge of
  `c808b1e`/OBJ-frontend-engineer-1). Working tree clean. `node --check`
  clean on `docs/app.js` and `docs/countries.config.js`; all 9
  `docs/data/morocco/*.geojson` files parse as valid JSON. No rung-1
  (published fact wrong) or rung-2 (site down / pipeline broken) candidate
  is live from what `main` itself shows.
- **The PR backlog, checked live via the GitHub API, not inferred from any
  minutes text:** exactly **30 open PRs** (`#10`–`#39`), one per `council/*`
  branch, spanning 2026-08-06 through 2026-08-19. **Zero have been merged
  since PR #1** (2026-08-04, the only merge `main` has ever received from
  this Council). `main` has not moved in 16 days despite a sitting firing
  roughly every 5 hours the entire time — this is not a new finding
  (`council/2026-08-13-2253` and the three 2026-08-18 sittings already
  flagged it, growing from ~27 to 29 to 30), but two things changed enough
  since the last escalation (2026-08-18 12:54) to warrant restating it
  today, not just re-carrying it silently:

  1. **The backlog is now actively producing duplicate, disagreeing work,
     not just piling up passively.** Fetched and read every 2026-08-11
     through 2026-08-19 branch's minutes directly (`git fetch` + `git
     show`, not the PR diffs' prose). `OBJ-map-debugger-1` (the dead
     "Report an error" mailto fix) was independently **ruled SHIP and
     committed on at least 10 separate branches**: `2026-08-11-0410`,
     `2026-08-12-0303`, `2026-08-12-0757`, `2026-08-12-2104`,
     `2026-08-13-0923`, `2026-08-13-1805`, `2026-08-18-2253`,
     `2026-08-19-0256`, `2026-08-19-0754`, `2026-08-19-2315` (PR #39) —
     plus one more attempt (`2026-08-18-0402`) that reverted before commit
     on a gate failure. `OBJ-map-debugger-4` (light-theme topbar contrast)
     was independently touched on **6 branches** per the 2026-08-18 07:56
     sitting's own file-level audit (`2026-08-06-0757`, `-1259`, `-1800`,
     `-07-1753`, `-11-2253`, `-13-0415`). A CI-validation workflow was
     independently drafted **3 times** (2026-08-12, 08-13, 08-18) and
     shipped **0** times. None of this was wasted by any one sitting acting
     wrongly — each correctly read a stale `main`, correctly found the bug
     still present, and correctly fixed it. The waste is structural: with
     zero merges, every sitting's fix disappears from the *next* sitting's
     view.

  2. **Two of those ten `OBJ-map-debugger-1` branches disagree on a
     privacy-sensitive question, and the more recent one reversed the more
     careful one without knowing it existed.** `council/2026-08-18-0402`
     explicitly declined to hardcode a real personal email into public
     page source ("publishing a personal inbox into `docs/**` invites
     spam/scraping and is a call the Council will not make unattended")
     and redirected the three mailto links to the repo's own GitHub
     issues page instead — then reverted anyway on an unrelated gate
     failure. `council/2026-08-19-2315` (PR #39, still open, most recent),
     branching from stale `main` and never seeing that decision, went the
     other way: it hardcoded `reda.tahiri1@gmail.com` directly into
     `docs/index.html` and `docs/app.js`, flagging the choice in its own
     minutes but not against any known prior alternative. **Both branches
     are currently open, unmerged, and mutually exclusive.** This sitting
     does not rule between them — that is a call for the site owner, not
     an unattended Chair — but it is exactly the kind of drift that an
     unreconciled 30-PR backlog will keep producing, and it has now
     produced a concrete privacy-relevant fork instead of just a stylistic
     one. Recorded on `council/BOARD.md` under `OBJ-map-debugger-1`'s risk
     line so it cannot be silently merged either way.

- **Egress-proxy structural block, re-verified fresh this sitting (not
  assumed from the 2026-08-18 findings):** `curl` against `unpkg.com`,
  `basemaps.cartocdn.com`, and `nominatim.openstreetmap.org` all return
  `CONNECT tunnel failed, response 403`; `$HTTPS_PROXY/__agentproxy/status`
  logs the same three hosts in `recentRelayFailures` at
  `2026-08-20T05:55:2x Z`, this sitting's own timestamp. Unchanged since
  at least 2026-08-18 — third+ independent confirmation, still structural,
  not transient. This environment cannot construct MapLibre or reach
  Nominatim; no sitting run from here can produce a real `map-tester` GO
  for anything touching the live map render or a coordinate lookup.
  Already escalated to the user on 2026-08-18; not re-escalated as a
  separate item today, only re-verified and folded into the backlog
  finding above since it is part of the same "why hasn't anything shipped"
  picture.

### Docket

Per COUNCIL.md §8 ("pull, don't regenerate"): `docs/**` is byte-identical
to what the 2026-08-04 sitting left it in, and the six seats' reasoning
has not changed since — re-convening all six from scratch would reproduce
the same 18 objectives already fully specified on `council/BOARD.md`
(seeded this sitting from `council/2026-08-19-2315`, the most-advanced
true record, per the same practice the 2026-08-18 sittings used). No new
objective is minted today beyond what that branch already surfaced
(`OBJ-coord-validator-4`). Full docket with `unlocks:`/`evidence:`/`size:`/
`risk:` detail lives there; this file records only the ruling.

**frontend-engineer** — OBJ-frontend-engineer-2, -3 — reaffirmed, unchanged.
**coord-validator** — OBJ-coord-validator-4, -1, -2, -3 — reaffirmed, unchanged.
**map-debugger** — OBJ-map-debugger-1 (see finding above — not a normal
reaffirm, see Ruling), OBJ-map-debugger-2, -4, -3 — reaffirmed, unchanged.
**map-tester** — OBJ-map-tester-1, -2, -3 — reaffirmed, unchanged.
**platform-engineer** — OBJ-platform-engineer-1, -2, -3 — reaffirmed, unchanged.
**security-engineer** — OBJ-security-engineer-1, -2, -3 — reaffirmed, unchanged.

### Ruling

SHIP:    **None — deliberate Chair discretion, not a budget block.**
         Today's rolling-24h SHIP budget is technically unspent on `main`
         (no `council/2026-08-20.md` existed before this sitting, and
         `main` itself has shipped nothing since 08-04). But the
         highest-ranked ready S item, `OBJ-map-debugger-1`, has already
         been ruled SHIP and committed on 10 different open branches, two
         of which now disagree on a privacy-sensitive implementation
         choice that the user has not ratified. Landing an 11th branch
         with an 11th independent implementation would not close that
         disagreement — it would add a third variant to reconcile. Every
         other ready S item on the board (frontend-engineer-2/-3,
         coord-validator-1/-3, platform-engineer-1/-2/-3,
         security-engineer-1/-2/-3) is unshipped as far as this sitting
         can tell from live PR data, but shipping any of them today adds a
         31st unmerged branch to a pile a human has not yet started
         reconciling, per the precedent set and reasoned through at
         `council/2026-08-18-0756`/`-1254`. COUNCIL.md §4 is explicit that
         "a sitting that approves nothing is a valid and expected outcome"
         — this is that case, chosen deliberately over manufacturing an
         11th or 31st branch.

REPORT:  **1 — the duplicate-work and privacy-fork finding above.** This
         is the sitting's actual work product: which objective has been
         re-shipped how many times, and the one place two of those
         re-ships now conflict on a decision only the user should make.

         **2 — re-verification of the egress-proxy structural block**,
         folded into the same finding rather than filed separately since
         it was already escalated 2026-08-18 and nothing about it has
         changed except the date.

Vetoed:  none — nothing new was proposed this sitting that required
         testing against a structural or truth veto; the non-ship above is
         Chair discretion under §4, not a veto call.

Docketed: OBJ-frontend-engineer-2, OBJ-frontend-engineer-3,
OBJ-coord-validator-1, OBJ-coord-validator-2, OBJ-coord-validator-3,
OBJ-coord-validator-4, OBJ-map-debugger-1 (annotated — see risk line,
not simply "ready"), OBJ-map-debugger-2, OBJ-map-debugger-3,
OBJ-map-debugger-4 (annotated), OBJ-map-tester-1, OBJ-map-tester-2,
OBJ-map-tester-3, OBJ-platform-engineer-1 (annotated), OBJ-platform-engineer-2,
OBJ-platform-engineer-3, OBJ-security-engineer-1, OBJ-security-engineer-2,
OBJ-security-engineer-3

(No new ID minted this sitting; ledger unchanged from `council/2026-08-19-2315`:
coord-validator 5, map-debugger 5, all others unchanged from 2026-08-04.)

### Outcome
Commit:  this minutes file + `council/BOARD.md` update only. No `docs/**`
         change, no `.github/**` change — deliberately, per the Ruling
         above.
Gates:   n/a — nothing executed against product code this sitting.
Carried: **For the user, escalated directly (see notification), not just
         recorded here.** Two asks, not one: (1) the 30-PR backlog itself
         needs a human pass — pick a winner among the ~10
         `OBJ-map-debugger-1` branches (or write a fresh one on top of a
         merged `main`) and close the rest, same for the 6
         `OBJ-map-debugger-4` branches and the 3 CI-workflow drafts, then
         merge or close everything else; no further autonomous sitting can
         make real progress by shipping more into the same unreconciled
         pile. (2) **Specifically before merging anything for
         `OBJ-map-debugger-1`**: decide whether the "Report an error" link
         should carry the owner's real personal email
         (`reda.tahiri1@gmail.com`, PR #39's choice) or redirect to the
         repo's GitHub issues page instead (`council/2026-08-18-0402`'s
         choice, unshipped) — these are mutually exclusive and this
         Council will not choose between them unattended. Egress-proxy
         block (unpkg/cartocdn/nominatim, 403 at CONNECT) remains
         structural and unrelated to the above; already flagged 2026-08-18,
         re-confirmed today, needs either a network-policy change for this
         environment or a human running the final browser check by hand.

## Sitting — 12:55 (Europe/Paris)

Fifth-hourly sitting after the 07:53 one above. Read `COUNCIL.md`,
`council/BOARD.md`, and this file's 07:53 section per standing order
**before** touching anything else. Per COUNCIL.md §8 ("pull, don't
regenerate"), checked live state first rather than re-convening all six
seats from scratch, since the 07:53 sitting's finding is a structural one
that does not go stale in 5 hours unless a human has acted on it.

### State established before convening

- `git log` tip on `origin/main`: still `9a4d5a4` (2026-08-04) — fetched
  fresh, not assumed. `node --check` clean on `docs/app.js` and
  `docs/countries.config.js`; all 9 `docs/data/morocco/*.geojson` files
  parse as valid JSON. No rung-1/rung-2 candidate is live.
- **Checked the GitHub API directly for what changed since 07:53:** PR #40
  (the 07:53 sitting's own PR) is still **open**, zero review activity,
  zero comments, `mergeable_state: clean`. No PR in the 30-open backlog
  was merged or closed since 07:53. PR #39 (the personal-email
  `OBJ-map-debugger-1` variant) is still open and unmerged — re-confirmed
  via `pull_request_read get_diff`, its diff is unchanged from what 07:53
  described: `docs/index.html`, `docs/app.js:968,997` all point
  `mailto:` at `reda.tahiri1@gmail.com`. The privacy fork the 07:53
  sitting flagged as "a decision only the user should make" is therefore
  **still unresolved**, not stale — nothing this sitting found supersedes
  it.
- No new commit, merge, or human action of any kind is visible on `main`
  or on the open-PR set since the last sitting. The finding and the
  ruling below are a **reaffirmation**, not a rediscovery.

### Docket

Unchanged from the 07:53 section above — same 18 objectives, same
annotations on `OBJ-map-debugger-1` and `OBJ-map-debugger-4`. No seat
re-convened from scratch; nothing observed since 07:53 that would produce
a genuinely new objective per COUNCIL.md §8. Full detail on
`council/BOARD.md`.

### Ruling

SHIP:    **None — same deliberate Chair discretion as 07:53, re-applied
         rather than assumed.** The blocking condition (10 disagreeing
         `OBJ-map-debugger-1` branches, 2 of them mutually exclusive on
         whether to publish the owner's personal email; a 30+-branch
         unreconciled backlog) is unchanged. Shipping any ready S item
         today — including one unrelated to the email fork, e.g.
         frontend-engineer-2/-3 or security-engineer-1/-2 — would still
         only add a 31st/32nd branch to a pile no human has begun
         reconciling. Nothing about that calculus moved in the last 5
         hours. COUNCIL.md §4: approving nothing remains valid and, per
         the note that at five sittings a day most should approve
         nothing, expected — doubly so while the backlog itself is the
         open incident.

REPORT:  **1 — reaffirmation that the 07:53 finding is unresolved.** No
         new investigation was needed; the value of this sitting's REPORT
         is confirming, via a fresh API check rather than an assumption,
         that the privacy-sensitive fork and the 30-PR backlog are both
         exactly where the last sitting left them, and that they still
         require a human, not another sitting, to move.

Vetoed:  none.

Docketed: unchanged from the 07:53 section — same 18 IDs, same
annotations. No new ID minted; ledger unchanged
(coord-validator 5, map-debugger 5, all others unchanged from 2026-08-04).

### Outcome
Commit:  this minutes file (appended) + `council/BOARD.md` (status note
         timestamp/count refreshed only). No `docs/**` change, no
         `.github/**` change.
Gates:   n/a — nothing executed against product code this sitting.
Carried: Same two asks as 07:53, still open, still unresolved as of this
         sitting: (1) a human pass to reconcile the ~31-branch backlog —
         pick a winner per duplicated objective, merge it, close the
         rest. (2) the `OBJ-map-debugger-1` email-vs-issues-page decision,
         specifically before merging PR #39 or any sibling branch. The
         egress-proxy block remains structural and unrelated, already
         escalated, unchanged today. **This sitting is escalating directly
         to the user again** (see notification) rather than only
         re-recording the carry, since two full sittings (10 hours) have
         now passed with the fork still open and the backlog still
         growing by one PR every ~5 hours.
