# Council sitting — 2026-08-20

## Sitting — 23:00 (Europe/Paris)

First sitting of the day, and the first sitting since 2026-08-04 14:15 —
a 16-day gap in wall-clock time with **zero commits landing under `docs/**`
or anywhere else** in between (`git log` tip is unchanged:
`9a4d5a4`, the merge that landed `c808b1e` from the 08-04 sitting). Nothing
in the Council's world moved on its own; this sitting re-verified every
standing claim on `council/BOARD.md` directly against the live tree rather
than trusting a 16-day-old record, then acted on what changed as a result
of two REPORT investigations run this sitting.

### State established before convening
- `git status` clean, tip `9a4d5a4`, matching the 08-04 outcome exactly —
  confirms no out-of-band commits landed in the gap.
- Re-verified rung-1/2 preconditions fresh: `node --check` clean on
  `docs/app.js` and `docs/countries.config.js`; all 9
  `docs/data/morocco/*.geojson` files still parse as valid JSON. No site-down,
  no broken pipeline, no wrong published fact found. No rung-1/2 candidate
  pre-empts the docket.
- Re-verified the two items the 08-04 minutes carried forward as still-live:
  the mailto placeholders (`docs/index.html:122`, `docs/app.js:968,997`)
  still read `reda.tahiri@example.com`; `docs/brand.css:32-36`'s unconditional
  `.topbar .ghost-btn{color:rgba(255,255,255,.85)}` still has no
  `[data-theme="light"]` override. Both still live.
- No `.github/workflows/`, no `docs/_headers`, no SRI `integrity=` attributes,
  no CSP anywhere — all three security/platform gaps from the Board are
  still open exactly as recorded.

### Docket (pulled from `council/BOARD.md`, not re-derived — nothing in the
repo changed since it was last written, so re-running all six seats from
scratch would have reproduced the same 18 objectives verbatim)

**frontend-engineer** — OBJ-1 (Shipped 08-04), OBJ-2 (loading state, S),
OBJ-3 (topbar overflow ≤375px, S)

**coord-validator** — OBJ-1 (vintage field on industrial.geojson, S),
OBJ-2 (sample-verify national-hv + transmission-lines, M — **investigated
this sitting, see below**), OBJ-3 (remove/mark deprecated grid-lines.geojson, S)

**map-debugger** — OBJ-1 (dead mailto, S), OBJ-2 (wire national-hv +
transmission-lines into the map, L, blocked), OBJ-3 (visible MapLibre
runtime-error state, S), OBJ-4 (light-theme topbar contrast, S)

**map-tester** — OBJ-1 (orphan-data check, S), OBJ-2 (write the GO evidence
bar, S), OBJ-3 (popup field-mapping audit, S)

**platform-engineer** — OBJ-1 (minimal CI validation check, was M —
**scoped to S and shipped this sitting, see below**), OBJ-2 (uptime check, S),
OBJ-3 (`docs/_headers` cache-control, S)

**security-engineer** — OBJ-1 (SRI hashes, S), OBJ-2 (disclose third-party
requests, S), OBJ-3 (CSP via `docs/_headers`, S)

### REPORT investigations run this sitting (read-only, no edits — feeds this
ruling and the next docket)

1. **OBJ-coord-validator-2** — coord-validator sample-verified both
   previously-unchecked line files against Morocco's bbox (1,488/1,488
   PASS), a stratified 30-feature topology sample (0 self-intersections,
   0 degenerate geometries), and vertex-density clustering around 10 known
   grid nodes (all corridors represented). **Verdict: PASS, 0 FAIL,
   0 UNVERIFIED.** New finding not on a FAIL/PASS scale: 536 of
   `national-hv`'s 947 features (57%) share endpoints within 2 km of a
   `transmission-lines` feature — the two ONEE/WBG-sourced files
   substantially duplicate the same physical corridors. Coordinate accuracy
   is no longer the blocker for OBJ-map-debugger-2; the remaining pre-ship
   question is a rendering-dedup design call (pick one source per corridor,
   or layer-toggle both with an overlap note), which is a map-debugger
   decision, not a coord-validator one. Board updated accordingly.
2. **OBJ-platform-engineer-1** — platform-engineer produced and verified a
   concrete 4-step, one-file, one-action (`actions/checkout@v4` only) CI
   workflow design, re-tested every check live against the real repo, and
   proved each check fails loudly under deliberate breakage in a scratch
   copy. This converts the objective from M to a genuinely reversible,
   single-file **S** with zero remaining design ambiguity — see Ruling.

### Ruling

**Rung check:** no rung-1 (published fact wrong) or rung-2 (site down)
candidate was *actively broken* this sitting — but re-reading rung 2's own
wording, "the pipeline can ship something broken," literally: with zero
CI anywhere in the repo, that condition is true *right now*, continuously,
regardless of whether today's push happens to be bad. OBJ-platform-engineer-1
directly closes that live gap. This reading pre-empts the rung-3/4 docket
(light-theme contrast, mailto, loading state, topbar overflow) for today's
single SHIP slot.

SHIP:    **OBJ-platform-engineer-1** — add `.github/workflows/validate.yml`.
         Newly S-sized after this sitting's REPORT (see above): one file,
         one third-party action, zero build step/bundler, fully reversible
         (delete the file). Closes the live rung-2 gap directly. Passed
         every applicable gate (below) with evidence independently
         re-derived by two different seats (platform-engineer who built it,
         map-tester who re-ran every check from scratch rather than trusting
         the build report). Today's SHIP budget was unspent — no earlier
         sitting today shipped, and the last actual ship
         (OBJ-frontend-engineer-1, `c808b1e`) was 2026-08-04, far outside
         the rolling 24h window.

         OBJ-map-debugger-1 (dead mailto) was the standing next-ranked
         rung-adjacent S item per the 08-04 minutes, but the Chair is
         **holding** it rather than ruling it this sitting: its evidence
         bar as written ("a real, monitored address") requires picking an
         actual destination mailbox to publish on a public page, and this
         is an unattended run with no human present to confirm which
         address is appropriate to expose. Publishing a real contact
         address into a public-facing site is a visible, hard-to-reverse,
         human-facing decision — not the kind of call this sitting should
         make by itself. Flagged on the Board as blocked pending a
         human-confirmed address, not vetoed.

         OBJ-coord-validator-1 (vintage field) was also considered:
         every existing `source`/`source_url` in `industrial.geojson` is
         generic ("OCP annual report", "Renault press") with no year
         embedded, so filling in an accurate `vintage` per feature would
         require fresh external research under this sitting's time budget —
         the wrong risk to take casually, since a *wrong* vintage is itself
         a rung-1 violation next sitting. Left docketed, not attempted.

REPORT:  OBJ-coord-validator-2, OBJ-platform-engineer-1 — both executed in
         full this sitting (see above), not merely reaffirmed. Both reports
         are now closed out: coord-validator-2's finding is folded into
         OBJ-map-debugger-2's Board entry; platform-engineer-1 converted
         straight into today's SHIP.

Vetoed:  none — no seat vetoed anything this sitting; the two REPORT
         objectives and the SHIP all cleared the standing structural and
         truth vetoes on inspection (no new dependency, no build step, no
         schema mutation, no unsourced figure).

Docketed to council/BOARD.md (unchanged, reaffirmed as-is):
OBJ-frontend-engineer-2, OBJ-frontend-engineer-3, OBJ-coord-validator-1,
OBJ-coord-validator-3, OBJ-map-debugger-1 (now flagged blocked-pending-human),
OBJ-map-debugger-2 (now flagged blocked-pending-dedup-design, not
blocked-pending-validation), OBJ-map-debugger-3, OBJ-map-debugger-4,
OBJ-map-tester-1, OBJ-map-tester-2, OBJ-map-tester-3, OBJ-platform-engineer-2,
OBJ-platform-engineer-3, OBJ-security-engineer-1, OBJ-security-engineer-2,
OBJ-security-engineer-3

(No IDs minted this sitting — no seat proposed anything genuinely new; every
ledger stays at its 2026-08-04 value.)

### Outcome
Commit:  `.github/workflows/validate.yml` added on branch
         `council/2026-08-20-2100` (see PR for the exact sha) —
         "platform: add docs-validate CI check (OBJ-platform-engineer-1)".
Gates:   desktop n/a | light n/a | 375px n/a — this is a CI-only addition,
         zero files under `docs/index.html`/`app.js`/`style.css`/`brand.css`/
         `data/**` changed, so there is nothing rendered to check; map-tester
         confirmed this explicitly rather than fabricating browser evidence
         (`git diff --stat HEAD -- docs/` empty) | data n/a (no
         `docs/data/**` touched) | secrets ✓ — security-engineer PASS,
         "SAFE-TO-PUSH": zero credential/token/secret references, no
         `permissions:` block widening `GITHUB_TOKEN`, only
         `actions/checkout@v4` used, `git status --short` showed the
         workflow file as the sole change, `solar/**` confirmed untouched |
         map-tester **GO** — independently re-ran all three checks against
         the live repo (9/9 GeoJSON valid, 2/2 `node --check` clean, 5/5
         referenced files present) and independently reproduced all three
         deliberate-breakage proofs in its own scratch copies, not trusting
         platform-engineer's prior claims | deploy n/a — this does not
         deploy anything, `wrangler` untouched, workflow only runs CI on
         future pushes.
Carried: OBJ-map-debugger-1 (dead mailto) is next-ranked for a future SHIP
         slot but needs a human to confirm the real destination address
         before an unattended sitting ships it — carried as blocked-pending-
         human, not vetoed.
         OBJ-map-debugger-2 (wire national-hv/transmission-lines into the
         map) is unblocked on coordinate accuracy (coord-validator-2: PASS,
         0 FAIL) but now needs a map-debugger rendering-dedup design call
         (57% corridor overlap between the two source files) before it can
         be split into a shippable S. Still L as filed.
         OBJ-map-debugger-4 (light-theme contrast) and
         OBJ-coord-validator-1 (vintage field) remain the next-ranked S
         items once the mailto question is resolved by a human; vintage
         field additionally needs real external research before it's safe
         to attempt unattended.
         Next sitting (~04:00 Europe/Paris) is REPORT-only regardless of
         ranking: one SHIP per rolling 24h, and this one already landed.
