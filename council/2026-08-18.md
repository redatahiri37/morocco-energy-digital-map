# Council sitting — 2026-08-18

## Sitting — 04:02 (Europe/Paris)

First sitting recorded since `council/2026-08-04.md` (14:15). No sitting
files exist for the gap in between — whatever caused that is outside this
sitting's remit to diagnose; recorded here only as the fact it is. Read
`council/BOARD.md` and `council/2026-08-04.md` before convening, per
standing order.

### State established before convening
- `git log` tip: `9a4d5a4` (merge of `c808b1e`, the layer-toggle
  keyboard-a11y fix shipped 2026-08-04 14:15). `git diff 4100942 HEAD
  --stat` between the last-recorded sitting's minutes commit and today's
  tip is empty — **zero drift** in `docs/**` since the last sitting.
  Working tree was clean at convene time.
- No rung-1/rung-2 candidate: `node --check` clean on `docs/app.js` and
  `docs/countries.config.js`; all 9 `docs/data/morocco/*.geojson` files
  parse as valid JSON; no incident reported. Nothing pre-empts the docket.
- Because `docs/**` is byte-identical to the state the 2026-08-04 sitting
  left it in, and that sitting's seats already produced reasoned,
  evidence-backed dockets recorded in full on `council/BOARD.md`, the
  Chair reaffirmed the Board directly (verifying the specific claims below
  itself) rather than re-convening all six seats to re-derive an unchanged
  docket from scratch — consistent with COUNCIL.md §8's "pull, don't
  regenerate." Spot-checks run fresh, not assumed: `grep -n mailto
  docs/index.html docs/app.js` still shows all 3 dead `example.com` links
  (OBJ-map-debugger-1, unchanged); `grep -c vintage
  docs/data/morocco/industrial.geojson` still returns 0 (OBJ-coord-
  validator-1, unchanged); no `.github/workflows/` directory exists
  (OBJ-platform-engineer-1, unchanged); no `docs/_headers` file exists
  (OBJ-platform-engineer-3 / OBJ-security-engineer-3, unchanged); zero
  `integrity=` attributes in `docs/index.html` (OBJ-security-engineer-1,
  unchanged).

### Docket (pulled from council/BOARD.md — unchanged from 2026-08-04; see that file for full `unlocks:`/`evidence:`/`size:`/`risk:` detail per objective)

**frontend-engineer** — OBJ-frontend-engineer-2 (loading state, S),
OBJ-frontend-engineer-3 (375px topbar overflow, S)
*(OBJ-frontend-engineer-1 already Shipped 2026-08-04; docket has 2, not 3,
until a new one is filed)*

**coord-validator** — OBJ-coord-validator-1 (vintage field, S),
OBJ-coord-validator-2 (sample-verify 1,488 unwired line features, M),
OBJ-coord-validator-3 (remove/mark deprecated grid-lines.geojson, S)

**map-debugger** — OBJ-map-debugger-1 (dead mailto, S), OBJ-map-debugger-2
(wire national-hv + transmission-lines, L, blocked on
OBJ-coord-validator-2), OBJ-map-debugger-4 (light-theme topbar contrast,
S), OBJ-map-debugger-3 (visible error state for mid-session MapLibre
failure, S)

**map-tester** — OBJ-map-tester-1 (orphan-data check, S), OBJ-map-tester-2
(written GO evidence bar, S), OBJ-map-tester-3 (popup field-mapping audit,
S)

**platform-engineer** — OBJ-platform-engineer-1 (CI check, M, blocked on
scoping), OBJ-platform-engineer-2 (uptime check, S),
OBJ-platform-engineer-3 (docs/_headers cache-control, S)

**security-engineer** — OBJ-security-engineer-1 (SRI hashes, S),
OBJ-security-engineer-2 (disclose third-party requests, S),
OBJ-security-engineer-3 (CSP via docs/_headers, S, medium risk)

### Ruling

SHIP:    **Attempted, then downgraded to no commit — gate failed.**
         OBJ-map-debugger-1 (dead mailto) is the highest-ranked rung-3
         item on the board — "the audience cannot make a decision it
         should be able to make": a regulator or DC developer who spots a
         wrong figure cannot currently get the correction to land, since
         all three "Report an error" links point at
         `reda.tahiri@example.com`, an RFC-2606 reserved, non-deliverable
         domain. Today's SHIP budget was unspent (no `council/2026-08-18.md`
         existed before this sitting; no other sitting shipped today).
         Dispatched to `map-debugger` for execution.

         **Chair's privacy constraint on execution**: the fix was directed
         to *not* hardcode any real personal email address into public,
         unauthenticated page source — publishing a personal inbox into
         `docs/**` invites spam/scraping and is a call the Council will
         not make unattended. `map-debugger` instead retargeted all three
         links to the repository's own GitHub "new issue" page (public,
         already linked from the topbar), reusing the existing `REPO_URL`
         constant (`docs/app.js:47`), preserving the feature id/name
         context already built into each link. `security-engineer`
         reviewed the diff: **SAFE-TO-PUSH** — no secret/PII pattern
         introduced, no new dependency, `git log` on the branch clean.

         **Release gate (map-tester): GO-STATIC, not GO — does not clear
         COUNCIL.md §5.** Two of the three edited links
         (`docs/app.js:968,997`) only render inside a map-feature popup;
         the third (`docs/index.html:122`) was live-DOM-verified across
         desktop/light/375px with zero `mailto:` remaining anywhere on the
         page. The popup-embedded two could only be source-audited
         (module scope, `encodeURIComponent`, `target=_blank rel=noopener`
         all confirmed correct) — not clicked and observed live — because
         **this remote execution environment's egress proxy blocks
         `unpkg.com` (MapLibre's CDN) and `basemaps.cartocdn.com` (the
         basemap tiles) with a 403 at the CONNECT layer**, confirmed via
         `curl -sS "$HTTPS_PROXY/__agentproxy/status"` showing both hosts
         in `recentRelayFailures`, and confirmed to be pre-existing/
         diff-independent by running the identical check against the
         unmodified file (`git stash`) with byte-identical console output.
         MapLibre cannot initialize in this sandbox at all — not
         transiently, not for this diff specifically, but structurally,
         for any page, local or live. Per COUNCIL.md §5's explicit text —
         *"`GO-STATIC` is not shippable; it is a receipt that only outside
         checks ran"* — this sitting **did not commit** the fix. The
         Chair reverted the working-tree diff (`git restore`) rather than
         landing unverified work; nothing was lost, the fix is a
         reproducible 3-line change fully specified in this entry.

REPORT:  **OBJ-coord-validator-2** (executed this sitting, read-only).
         Full-population (not sampled) bbox + adjacent-vertex-jump check
         across all 1,488 features in `national-hv.geojson` +
         `transmission-lines.geojson`: 0 FAIL (no ocean placements, no
         `0,0` origins, no degenerate single-vertex lines), but 314/1,488
         (21%) WARN on single-segment jumps of 20–157km, and 4 UNVERIFIED
         (vertices reaching Spanish waters — plausibly the real
         Morocco–Spain interconnector, unconfirmed). Live named-anchor
         verification via Nominatim was attempted and **blocked by the
         same egress-proxy policy** (`nominatim.openstreetmap.org` also
         appears in `recentRelayFailures`) — an environment limitation,
         not a data judgment. Notable finding carried to the Board: the
         314 WARN features in both files share near-identical vertex
         counts, coordinates, and jump distances despite citing
         independent provenance (ONEE-via-OSM vs. WBG 2018), suggesting
         undisclosed shared upstream sourcing — worth a future sitting's
         attention before OBJ-map-debugger-2 ever wires these in.

         **OBJ-platform-engineer-1** (executed this sitting, read-only
         planning, no file created). Scoped the M-sized CI-check objective
         down to a concrete S-sized first slice: JSON-validate every
         `docs/data/morocco/*.geojson`, `node --check` both JS entry
         files, grep for literal conflict markers, and confirm every
         `file:` reference in `docs/countries.config.js` resolves on disk
         — pure `node`/`grep` on `ubuntu-latest`, no install step, no
         bundler. Full draft YAML produced and recorded on the Board.
         Minted **OBJ-platform-engineer-4** for this now-shippable slice;
         the parent M item (broken-link/orphan-data checks) stays
         Docketed, unblocked for a future sitting to attempt as its own S.

Vetoed:  none this sitting. No seat proposed anything that tripped a
         structural or truth veto; the SHIP candidate was blocked by a
         release-gate finding, not a veto.

Docketed: OBJ-frontend-engineer-2, OBJ-frontend-engineer-3,
OBJ-coord-validator-1, OBJ-coord-validator-2 (annotated), OBJ-coord-validator-3,
OBJ-map-debugger-1 (re-docketed, not shipped — see Ruling), OBJ-map-debugger-2,
OBJ-map-debugger-3, OBJ-map-debugger-4, OBJ-map-tester-1, OBJ-map-tester-2,
OBJ-map-tester-3, OBJ-platform-engineer-1 (annotated), OBJ-platform-engineer-2,
OBJ-platform-engineer-3, OBJ-platform-engineer-4 (new), OBJ-security-engineer-1,
OBJ-security-engineer-2, OBJ-security-engineer-3

(One ID minted this sitting: `OBJ-platform-engineer-4`. Ledger:
platform-engineer 4 → 5; all other seats unchanged from 2026-08-04.)

### Outcome
Commit:  none — gate failed (working tree reverted to `9a4d5a4`'s state
         before this entry; only `council/2026-08-18.md` and
         `council/BOARD.md` are new in this sitting's commit).
Gates:   desktop partial (static link only) | light n/a (no CSS touched,
         diff discarded) | 375px n/a (diff discarded) | data n/a
         (`docs/data/**` untouched this sitting) | secrets ✓
         (security-engineer: SAFE-TO-PUSH) | map-tester **GO-STATIC** —
         explicitly not a GO, ruled non-shippable per COUNCIL.md §5.
Carried: **Structural finding for the record, not just this objective**:
         this remote execution environment's egress proxy blocks
         `unpkg.com`, `basemaps.cartocdn.com`, and
         `nominatim.openstreetmap.org` at the CONNECT layer (403). MapLibre
         cannot initialize here, at all, for any page. Every Docketed
         objective whose evidence bar requires a live map render or a
         live named-coordinate lookup — OBJ-map-debugger-2,
         OBJ-map-debugger-3, OBJ-map-tester-3, the un-clicked half of
         OBJ-map-debugger-1, and any future live-verification pass on
         OBJ-coord-validator-2 — cannot clear a real `map-tester` GO from
         *this* environment, only `GO-STATIC`, regardless of how correct
         the underlying change is. This is not something a future sitting
         can work around by trying harder; it needs either a network-
         policy change for this environment or a human running the final
         browser check by hand. Flagged to the user directly, outside
         this record, per COUNCIL.md §4's provision for a Chair-identified
         issue the Council itself cannot resolve.

         OBJ-map-debugger-1 remains top-ranked for the next SHIP slot —
         the fix itself is fully specified above and takes under five
         minutes to re-apply; only its release-gate evidence is blocked.
         OBJ-platform-engineer-4 (new, S, pure validation YAML drafted in
         full on the Board) is now unblocked and ready for a future
         sitting to actually ship — it needs no live browser evidence at
         all, only `node --check` output, so it is unaffected by the
         egress-proxy finding above and is arguably the safest next SHIP
         candidate for an unattended sitting running in this same
         environment.
