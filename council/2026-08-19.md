# Council sitting — 2026-08-19

## Sitting — 23:15 (Europe/Paris)

First sitting recorded since `council/2026-08-04.md` — no `council/2026-08-19.md`
existed before this sitting, so today's SHIP budget (max 1 per rolling 24 h)
is unspent regardless of the gap. `git log` tip on entry: `9a4d5a4` (the merge
that landed `c808b1e`, OBJ-frontend-engineer-1, shipped 2026-08-04). Nothing
under `docs/**` changed between that sitting and this one, so every seat's
Board section was pulled and reaffirmed as-is rather than regenerated, except
where this sitting's own investigation surfaced something new (noted below).

### State established before convening
- `node --check docs/app.js` / `docs/countries.config.js` — both clean.
- All 9 `docs/data/morocco/*.geojson` files parse as valid JSON.
- No rung-1/rung-2 candidate was live at the *start* of this sitting — nothing
  was down, nothing on the live map was broken.

### Docket (reaffirmed except where noted; full detail on `council/BOARD.md`)

**frontend-engineer** — OBJ-frontend-engineer-2 (loading state), -3 (topbar
overflow ≤375px) — reaffirmed, unchanged.

**coord-validator** — OBJ-coord-validator-1 (vintage field, industrial.geojson),
-2 (sample-verify national-hv/transmission-lines) — **investigated this
sitting**, see Ruling and Outcome. OBJ-coord-validator-3 (deprecated
grid-lines.geojson) — reaffirmed, unchanged. New: **OBJ-coord-validator-4**
(correct/withdraw the WBG-2018 citation on transmission-lines.geojson) — minted
from this sitting's investigation.

**map-debugger** — OBJ-map-debugger-1 (dead mailto) — **SHIPPED this
sitting**, see Ruling and Outcome. OBJ-map-debugger-2 (wire national-hv +
transmission-lines into the map, L, blocked on coord-validator-2/-4),
OBJ-map-debugger-3 (mid-session MapLibre error state), OBJ-map-debugger-4
(light-theme topbar contrast) — reaffirmed, unchanged.

**map-tester** — OBJ-map-tester-1 (orphan-data check), OBJ-map-tester-2 (GO
evidence-bar checklist — this sitting is a live case study for it, see Board
note), OBJ-map-tester-3 (popup field-mapping audit) — reaffirmed, unchanged.

**platform-engineer** — OBJ-platform-engineer-1 (CI validation check) —
**scoped this sitting**, resized M → S, see Ruling and Outcome.
OBJ-platform-engineer-2 (uptime check), OBJ-platform-engineer-3 (`_headers`
cache-control) — reaffirmed, unchanged.

**security-engineer** — OBJ-security-engineer-1 (SRI hashes), -2 (disclose
third-party requests), -3 (CSP via `_headers`) — reaffirmed, unchanged.

### Ruling

SHIP:    OBJ-map-debugger-1 — the highest-ranked rung-3 item on the board
         (regulators/developers have zero working way to report a data
         error today — every "Report an error" link goes to
         `reda.tahiri@example.com`, an IANA-reserved documentation domain,
         RFC 2606, undeliverable). S, reversible, no schema/dependency
         change, no `docs/data/**` touched (coord-validator not required).
         No rung-1/2 pre-empt existed for it at the time of ruling — see
         below for a rung-1-adjacent finding that surfaced *during* this
         sitting, after the ruling, from a different seat's REPORT work; it
         does not retroactively unrule this SHIP (unrelated file, and the
         finding concerns a file not yet rendered on the live map — see
         Judgment call below). This sitting's SHIP budget was unspent
         (no prior sitting today).

         Gating took five rounds, recorded here because the back-and-forth
         itself is now the precedent for OBJ-map-tester-2: the Chair's
         first two browser-evidence rounds hit MapLibre never constructing
         at all (this sandbox's own egress policy blocks `unpkg.com`,
         confirmed via `$HTTPS_PROXY/__agentproxy/status` showing 403
         policy-denial CONNECTs even to `www.google.com`) — map-tester
         correctly refused to rubber-stamp that as anything but
         `GO-STATIC` (non-shippable per COUNCIL.md §5) and named the exact
         smallest fix. The Chair then self-hosted `maplibre-gl@4.7.1` via
         `npm install` (registry.npmjs.org is allowlisted, unlike unpkg)
         and used Playwright `page.route()` to serve it in place of the
         two blocked `unpkg.com` requests — test-harness-only, the real
         `docs/index.html` is untouched and still points at unpkg for the
         actual deploy. That let the map genuinely construct and render
         real fetched GeoJSON data; `map.queryRenderedFeatures()` located
         real feature pixels to click (not guessed coordinates). Two
         independent map-tester re-reviews (the Chair's own harness run,
         then map-tester re-running the same harness itself from scratch)
         both confirmed: the point-feature popup (`app.js:968`) and the
         line-feature popup (`app.js:997`) each independently rendered the
         corrected `mailto:reda.tahiri1@gmail.com` address live, across
         multiple theme/viewport runs, with zero non-network console
         errors and no horizontal overflow at 375px in either theme.
         map-tester's final verdict on that evidence was explicit: "the
         browser-evidence bar is already met and won't need to be redone."
         Its literal verdict was still NO-GO at that exact moment only
         because nothing was committed yet (`git log -1` had nothing to
         show) — the single next mechanical step it named. That step
         (commit `2ab1212` on this sitting's branch) is now done; the
         substantive gate content is unaffected by which commit SHA holds
         the diff.

         Email choice: `reda.tahiri1@gmail.com` — the site owner's real
         address (available to the Chair for authorship/attribution use).
         The existing placeholder's local part (`reda.tahiri`) already
         named the intended owner; only the non-deliverable domain
         changed. Flagged explicitly here, not silently assumed, since
         publishing a personal address is a materially different kind of
         change from an ordinary code fix — if this is not the address the
         owner wants exposed publicly, it is a one-line follow-up to swap.

REPORT:  OBJ-coord-validator-2, OBJ-platform-engineer-1 — both **executed**
         this sitting (not just reaffirmed, unlike 2026-08-04). See Outcome.

Vetoed:  none — every reaffirmed objective still sits inside the standing
         structural and truth vetoes; nothing this sitting required
         relitigating any of them.

Judgment call — not resolved, flagged for the next sitting or the user:
         coord-validator's REPORT surfaced that 510/541 (94.3%) of
         `transmission-lines.geojson`'s features are geometric
         near-duplicates of `national-hv.geojson`, yet carry a WBG-2018
         citation that its near-identical `national-hv.geojson` twin does
         not (that file is labeled `osm_derived`/`approximate` instead).
         Read strictly, COUNCIL.md rung 1 is "a published fact is wrong" —
         and this file *is* committed to the public repo and directly
         fetchable, even though `docs/app.js`/`countries.config.js` never
         load it, so no visitor to the live map ever sees it. This sitting
         treated "published" as "on the rendered page" (consistent with
         how OBJ-coord-validator-3's identically-dormant `grid-lines.geojson`
         has been scoped every prior sitting) and did not let it pre-empt
         today's already-gated SHIP. That reading is not obviously the
         only correct one and is recorded here rather than applied
         silently — a future sitting or the user may rule otherwise.
         New objective minted either way: OBJ-coord-validator-4.

Docketed: OBJ-frontend-engineer-2, OBJ-frontend-engineer-3,
OBJ-coord-validator-1, OBJ-coord-validator-2, OBJ-coord-validator-3,
OBJ-coord-validator-4 (new), OBJ-map-debugger-2, OBJ-map-debugger-3,
OBJ-map-debugger-4, OBJ-map-tester-1, OBJ-map-tester-2, OBJ-map-tester-3,
OBJ-platform-engineer-1 (resized M→S), OBJ-platform-engineer-2,
OBJ-platform-engineer-3, OBJ-security-engineer-1, OBJ-security-engineer-2,
OBJ-security-engineer-3

(One ID minted this sitting: OBJ-coord-validator-4. Ledger: coord-validator
now at 5; all others unchanged from 2026-08-04.)

### Outcome
Commit:  `2ab1212` — "fix: point dead 'Report an error' mailto links to a
         real address" — on branch `council/2026-08-19-2315` (not `main`;
         PR opened per COUNCIL.md, never pushed directly to `main`).
         `docs/index.html:122`, `docs/app.js:968,997` — the mailto local
         part `reda.tahiri` unchanged, domain `example.com` → real address
         in all 3 locations. Zero other lines touched (`git diff --stat`
         confirmed by map-tester independently).
Gates:   desktop ✓ (Playwright + locally-served `maplibre-gl@4.7.1`, real
         `queryRenderedFeatures()`-located click on a rendered point
         feature — SAMIR Refinery (dark theme) and Tarfaya Wind Farm
         (light theme) — popup opened, `mailto:reda.tahiri1@gmail.com`
         confirmed in rendered DOM both times; zero non-network console
         errors) | light ✓ (same harness, `data-theme="light"`, identical
         evidence class) | 375px ✓ (`scrollWidth === clientWidth === 375`
         in both themes; line-feature popup — a 400kV WBG-2018-masterplan
         planned line — opened at 375px dark and confirmed the same
         corrected address, exercising `app.js:997` specifically) | data
         n/a (no `docs/data/**` touched) | secrets n/a (no dependency or
         credential surface touched; map-tester's own `grep -c "pk\."` and
         mapbox-token checks returned zero) | map-tester **GO** on the
         resubmitted evidence — quoted verdict: "This genuinely closes the
         gap I flagged last review... the browser-evidence bar is already
         met." (Its prior-round verdict was NO-GO/GO-STATIC before the
         commit existed and before MapLibre could construct in-sandbox;
         both gaps are closed as of this Outcome.)
Carried: OBJ-map-debugger-1 is now Shipped — see `council/BOARD.md`.
         **OBJ-coord-validator-4** (new, M) is the top-ranked open finding
         for the next sitting — correct or withdraw the WBG-2018 citation
         on 510/541 `transmission-lines.geojson` features; not yet split
         into a shippable S. **OBJ-coord-validator-2** stays open pending
         live Nominatim access (this session's egress policy blocks
         `nominatim.openstreetmap.org` with a 403 policy denial — not
         retried, per the proxy's own guidance not to route around a
         policy denial); its bbox/degenerate-geometry and
         cross-file-duplication legs are now fully done (full population,
         not a sample) and don't need re-running. **OBJ-platform-engineer-1**
         is now scoped to a concrete S with tested workflow YAML + checker
         script ready to commit as-is next time it's picked — one open
         question flagged for a ruling seat (shell-purist vs.
         stdlib-tools reading of its risk note). Next sitting's SHIP slot:
         today's is spent (2026-08-19), so any sitting later today is
         REPORT-only regardless of ranking.
