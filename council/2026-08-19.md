# Council sitting — 2026-08-19

## Sitting — 02:56 (Europe/Paris)

Third-ever sitting, first of 2026-08-19. `council/2026-08-19.md` did not
exist before this sitting. Today's SHIP budget (max 1 per rolling 24h) is
unspent regardless of the 15-day gap since the last sitting (2026-08-04
14:15) — confirmed independently: `council/BOARD.md`'s Shipped table holds
exactly one row (`c808b1e`, 2026-08-04), and `git log -- docs/` shows no
commit since.

### State established before convening
- `git log` tip: `9a4d5a4` (merge of PR #1). Nothing under `docs/**` has
  landed since `c808b1e` (2026-08-04) — a 15-day gap with zero council
  activity. Working tree clean.
- Checked for a live rung-1/rung-2 pre-empt: `node --check docs/app.js` and
  `docs/countries.config.js` both clean; all 9
  `docs/data/morocco/*.geojson` files parse as valid JSON. A live fetch of
  the deployed URL (`https://atlas-nexus-69o.pages.dev/`) could not be
  verified from this sandbox (outbound proxy returned a policy-level 403 on
  the CONNECT, not a site-level error) — recorded as unverifiable, not
  treated as evidence of an outage. No rung-1/2 candidate found by any
  in-repo check.
- Every Board item was re-verified directly against current file state
  rather than trusted from the 2026-08-04 minutes (see Docket below for
  what was re-confirmed). Nothing changed in 15 days; no seat had a
  genuinely new observation this sitting — all 18 objectives reaffirmed
  unchanged, no new IDs minted.

### Docket (reaffirmed, unchanged from council/BOARD.md — no new proposals)

**frontend-engineer**
- OBJ-frontend-engineer-2 | Add a visible loading state to `#layerList`/`#kpiGrid` while `loadAllData()` awaits its fetches — reaffirmed, unchanged
- OBJ-frontend-engineer-3 | Give `.topbar` explicit overflow handling at ≤375px — reaffirmed, unchanged
- (OBJ-frontend-engineer-1 shipped 2026-08-04; not reaffirmed, no longer on this seat's active docket)

**coord-validator**
- OBJ-coord-validator-1 | Add `vintage` field to all 13 `industrial.geojson` features — reaffirmed; re-verified `grep -c vintage docs/data/morocco/industrial.geojson` still returns 0
- OBJ-coord-validator-2 | Sample-verify `national-hv.geojson` (947) + `transmission-lines.geojson` (541) — reaffirmed, unchanged
- OBJ-coord-validator-3 | Remove or mark deprecated `grid-lines.geojson` (11 features, unreferenced) — reaffirmed; re-verified `app.js:86` still carries the "legacy fallback" comment

**map-debugger**
- OBJ-map-debugger-1 | Fix dead mailto targets (`index.html:122`, `app.js:968,997`) — reaffirmed; re-verified live, still `reda.tahiri@example.com` at all 3 sites — **ranked for SHIP**
- OBJ-map-debugger-2 | Wire `national-hv` + `transmission-lines` into the live map — reaffirmed, still L, still blocked on OBJ-coord-validator-2's report
- OBJ-map-debugger-3 | Surface a visible error state for mid-session MapLibre runtime failures — reaffirmed, unchanged
- OBJ-map-debugger-4 | Fix light-theme topbar button contrast in `docs/brand.css` — reaffirmed; re-verified live, `docs/brand.css:32-36` still sets `.topbar .ghost-btn{color:rgba(255,255,255,.85)}` unconditionally, no light-theme override anywhere in that file

**map-tester**
- OBJ-map-tester-1 | Orphan-data check across `docs/data/morocco/*.geojson` vs load-path references — reaffirmed; re-verified `national-hv`/`transmission-lines` still zero references in `app.js`/`countries.config.js`
- OBJ-map-tester-2 | Write down the required evidence bar for a release-gate GO — reaffirmed, unchanged
- OBJ-map-tester-3 | Audit popup field-name mapping against each source file's actual keys — reaffirmed, unchanged

**platform-engineer**
- OBJ-platform-engineer-1 | Add a minimal CI check on push to `main` (no build step, no bundler) — reaffirmed; re-verified `.github/workflows/` still does not exist
- OBJ-platform-engineer-2 | Wire an uptime check against the live map URL — reaffirmed, unchanged
- OBJ-platform-engineer-3 | Add `docs/_headers` with cache-control for `docs/data/*.geojson` — reaffirmed; re-verified `docs/_headers` still does not exist

**security-engineer**
- OBJ-security-engineer-1 | Add Subresource Integrity hashes to the MapLibre `<script>`/`<link>` tags — reaffirmed; re-verified zero `integrity=` attributes in `docs/index.html`
- OBJ-security-engineer-2 | Disclose the third-party requests the page makes on load — reaffirmed, unchanged
- OBJ-security-engineer-3 | Add a Content-Security-Policy via `docs/_headers` — reaffirmed; re-verified no CSP meta tag and no `docs/_headers` exists

### Ruling

SHIP:    OBJ-map-debugger-1 — highest-ranked, unshipped rung-3 item on the
         board ("the audience cannot make a decision it should be able to
         make" — specifically, cannot get a correction to land). Carried
         forward explicitly from the 2026-08-04 minutes as next-ranked once
         that day's slot was spent; re-verified fresh this sitting, not
         assumed: all 3 mailto targets (`index.html:122`, `app.js:968`,
         `app.js:997`) still resolve to `reda.tahiri@example.com`, an
         IANA-reserved documentation domain (RFC 2606) that cannot receive
         mail. No rung-1 or rung-2 candidate exists this sitting (JS
         syntax clean, all GeoJSON valid, no incident evidence — live URL
         check inconclusive due to sandbox network policy, not treated as
         a finding either way), so nothing pre-empts the docket. S-sized,
         reversible, a 3-site string replacement with no logic or schema
         change — clears every §5 gate requirement for autonomous SHIP.
         Today's SHIP budget is unspent (`council/BOARD.md` Shipped table
         has one row, dated 2026-08-04).

         **Execution held, not gated-fail:** the fix requires a real,
         monitored mailbox to replace the placeholder. The only candidate
         address on file is the repo owner's personal Gmail
         (reda.tahiri1@gmail.com). Publishing a person's personal email
         address on a public, deployed page is a one-way action —
         scrapers index it within hours and it cannot be meaningfully
         un-published once live — and no council seat has the authority to
         choose to expose the owner's personal contact details on their
         behalf. This is the kind of call the Chair's mandate does not
         cover: COUNCIL.md authorises unattended shipping of *map*
         changes, not unattended decisions about what personal information
         of the owner's gets published. Execution is therefore held for
         the owner to either supply a monitored non-personal address (a
         project alias, a form endpoint) or explicitly approve using the
         personal one. Nothing has been merged to `docs/**` this sitting.

REPORT:  OBJ-coord-validator-2, OBJ-platform-engineer-1 — reaffirmed for
         the same reason as the last sitting that ranked them: both are
         the only M-sized items on the board and both gate a currently
         un-shippable L. coord-validator-2 (sample-verifying 1,488
         unchecked ONEE/WBG line features) must resolve before
         OBJ-map-debugger-2 (wiring those features into the map) can be
         split into anything shippable. platform-engineer-1 (CI check)
         needs its scope pinned to a pure-validation checklist before it
         can split into an S without tripping the build-step/bundler
         structural veto. Neither was executed this sitting — no read-only
         agent runs were dispatched once the SHIP was held pending owner
         input, to keep this sitting's footprint to the ruling itself.

Vetoed:  none — every reaffirmed objective still sits inside the standing
         structural and truth vetoes; nothing changed in the 15-day gap
         that required relitigating any of them, and no seat proposed
         anything new this sitting that needed testing against a veto.

Docketed: OBJ-frontend-engineer-2, OBJ-frontend-engineer-3,
OBJ-coord-validator-1, OBJ-coord-validator-2, OBJ-coord-validator-3,
OBJ-map-debugger-2, OBJ-map-debugger-3, OBJ-map-debugger-4,
OBJ-map-tester-1, OBJ-map-tester-2, OBJ-map-tester-3,
OBJ-platform-engineer-1, OBJ-platform-engineer-2, OBJ-platform-engineer-3,
OBJ-security-engineer-1, OBJ-security-engineer-2, OBJ-security-engineer-3

(No IDs minted this sitting — every ledger stays at its 2026-08-04 value:
frontend-engineer 4, coord-validator 4, map-debugger 5, map-tester 4,
platform-engineer 4, security-engineer 4.)

### Outcome
Commit:  none — SHIP ruled but execution held pending owner decision on
         what monitored address to publish (see Ruling above); no code
         change on this branch, minutes and Board update only.
Gates:   n/a — not executed this sitting.
Carried: OBJ-map-debugger-1 stays ranked for SHIP and is NOT counted as
         spending today's 24h SHIP budget, since nothing shipped. The next
         sitting should re-check whether the owner has supplied a contact
         address; if so, that sitting may execute and ship it (subject to
         the normal one-per-24h budget check at that time). If not, it
         stays held and the next-ranked candidate (OBJ-map-debugger-4,
         light-theme contrast — the strongest remaining S, rung-4 item,
         now carried three sittings running) becomes the SHIP candidate
         instead, so the slot isn't wasted indefinitely on one blocked item.
