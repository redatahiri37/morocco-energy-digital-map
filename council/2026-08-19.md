# Council sitting — 2026-08-19

## Sitting — 17:53 (Europe/Paris)

Fourth sitting of the day. Read `COUNCIL.md`, `council/BOARD.md`, and
`council/2026-08-04.md` (the most recent minutes actually merged to
`main`) per the chair mandate. `council/2026-08-19.md` does not exist on
`main` — created fresh on this branch, same as the three earlier sittings
today each did independently on their own unmerged branches (PRs #35, #36,
#37).

### State established before convening
- `git log` tip on `main`: `9a4d5a4`, unchanged since 2026-08-04 and
  unchanged since the 12:53 sitting (PR #37). `node --check docs/app.js`
  and `docs/countries.config.js` both clean; all
  `docs/data/morocco/*.geojson` parse as valid JSON. No rung-1/2 candidate.
  `grep -n mailto: docs/index.html docs/app.js` on `main` still returns
  `reda.tahiri@example.com` in all 3 locations — the fix ruled at 07:54
  (PR #36) has not reached `main`.
- Checked GitHub directly: **30 open "Council sitting" PRs** (#8–#37),
  none merged, spanning 2026-08-05 through today. This PR is the 31st.
  Read PR #35 (02:56), #36 (07:54) and #37 (12:53) in full — all three are
  today's earlier sittings and none has landed.
- Confirmed the same finding PR #37 already surfaced, independently: PR
  #36 (07:54) executed `OBJ-map-debugger-1` on its own branch by
  publishing the owner's real personal Gmail address
  (`reda.tahiri1@gmail.com`) into `docs/app.js`/`docs/index.html`,
  overriding without owner sign-off the explicit privacy hold the same
  day's 02:56 sitting (PR #35) had placed on that exact action. The repo
  is public, so that address is visible in the open PR regardless of
  merge status. This was not re-litigated (the Chair cannot overrule a
  prior sitting), only re-confirmed as still live and still unresolved —
  neither PR #35 nor #36 nor #37 has been merged, closed, or edited since
  PR #37 flagged it.
- Given both the backlog and the privacy exposure are unresolved,
  unattended, escalating findings rather than routine status, this sitting
  sent the owner a direct notification (outside the PR/minutes record)
  summarizing both and asking for a decision on PR #36 specifically, in
  addition to recording it here.
- Per COUNCIL.md's SHIP-budget intent (cap unattended shipping to one
  action per rolling 24h, regardless of merge status): a sitting today
  (07:54) already ruled and executed a SHIP. This sitting is therefore
  **REPORT-only**, consistent with the 12:53 sitting's reasoning.

### Docket (pulled from `main`'s `council/BOARD.md` — unchanged since
2026-08-04; no new proposals, since nothing under `docs/**` has changed on
`main` for any seat to observe since the last time this was checked)

**frontend-engineer** — OBJ-frontend-engineer-2 (loading state), OBJ-frontend-engineer-3 (topbar overflow ≤375px) — reaffirmed, unchanged

**coord-validator** — OBJ-coord-validator-1 (`vintage` field, `industrial.geojson`), OBJ-coord-validator-2 (sample-verify `national-hv`/`transmission-lines`), OBJ-coord-validator-3 (deprecate `grid-lines.geojson`) — reaffirmed, unchanged on `main` (a sample-verify report exists on unmerged PR #36 but has not landed)

**map-debugger** — OBJ-map-debugger-1 (dead mailto — see State above, disputed across unmerged branches), OBJ-map-debugger-2 (wire `national-hv`+`transmission-lines`, L, blocked), OBJ-map-debugger-3 (mid-session error state), OBJ-map-debugger-4 (light-theme topbar contrast) — reaffirmed, unchanged on `main`

**map-tester** — OBJ-map-tester-1 (orphan-data check), OBJ-map-tester-2 (evidence-bar checklist), OBJ-map-tester-3 (popup field-mapping audit) — reaffirmed, unchanged

**platform-engineer** — OBJ-platform-engineer-1 (CI check, M), OBJ-platform-engineer-2 (uptime check), OBJ-platform-engineer-3 (`_headers` cache-control), OBJ-platform-engineer-4 (GeoJSON-syntax CI check, S — split proposed on unmerged PR #36, not landed) — reaffirmed, unchanged on `main`

**security-engineer** — OBJ-security-engineer-1 (SRI hashes), OBJ-security-engineer-2 (disclose third-party requests), OBJ-security-engineer-3 (CSP via `_headers`) — reaffirmed, unchanged

### Ruling

SHIP: **none — REPORT-only sitting.** Today's rolling-24h SHIP budget was
already spent by the 07:54 sitting (PR #36), which ruled and executed
against `OBJ-map-debugger-1`. No SHIP this sitting regardless of docket
ranking, per COUNCIL.md §4/§5.

REPORT: **none dispatched.** Nothing under `docs/**` has changed on `main`
since the last check, and the live M-sized report candidates were already
actioned earlier today (07:54 sitting, PR #36). Re-running the same
read-only investigation a fourth time this day would manufacture work, not
produce new evidence. Per COUNCIL.md, "a sitting that approves nothing is
a valid and expected outcome."

Vetoed: none new. This Chair does not attempt to overrule any prior
sitting's ruling on `OBJ-map-debugger-1` — records the still-unresolved
disagreement between PR #35 and PR #36 as the sitting's most important
carry-forward, alongside the growing unmerged-PR backlog.

Docketed (unchanged): OBJ-frontend-engineer-2, OBJ-frontend-engineer-3,
OBJ-coord-validator-1, OBJ-coord-validator-2, OBJ-coord-validator-3,
OBJ-map-debugger-1, OBJ-map-debugger-2, OBJ-map-debugger-3,
OBJ-map-debugger-4, OBJ-map-tester-1, OBJ-map-tester-2, OBJ-map-tester-3,
OBJ-platform-engineer-1, OBJ-platform-engineer-2, OBJ-platform-engineer-3,
OBJ-security-engineer-1, OBJ-security-engineer-2, OBJ-security-engineer-3

(No IDs minted this sitting.)

### Outcome
Commit:  none — REPORT-only sitting, no code touched; this branch carries
         only this minutes file (`council/BOARD.md` is unchanged from
         `main` this sitting — the 12:53 sitting's operational note lives
         on PR #37, unmerged, and is not duplicated here to avoid a
         three-way conflict on the same file across three still-open
         branches; the owner should apply it once when merging).
Gates:   n/a — nothing executed against `docs/**` this sitting.
Carried: Same two items PR #37 raised, still unresolved as of this
         sitting, now underlined by a direct notification sent to the
         owner:
         (1) **30 open, unmerged council PRs** (31 counting this one)
         dating back to 2026-08-05 — nothing has reached the live site in
         15 days. Recommend merging oldest-first or pausing the cadence
         until the backlog is cleared.
         (2) **PR #36 published the owner's real personal Gmail address**
         into `docs/app.js`/`docs/index.html`, reversing without
         authorization the explicit privacy hold the same day's 02:56
         sitting placed on that exact action. Still open, still exposed
         (repo is public), still unresolved as of this sitting.
         The next sitting should re-check whether the owner has acted on
         either item before proposing further work; if the backlog is
         still growing unmerged, that sitting should consider whether
         continuing to open new PRs on top of it is still useful.
