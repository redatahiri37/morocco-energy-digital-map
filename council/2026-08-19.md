# Council sitting — 2026-08-19

## Sitting — 12:53 (Europe/Paris)

Chronologically the third sitting of the day (GitHub shows PR #35 at 02:56
and PR #36 at 07:54, both unmerged), but `council/2026-08-19.md` does not
exist on `main` — this file is created fresh on this branch, same as the
prior two sittings each independently created it on their own branch.
Read `COUNCIL.md`, `council/BOARD.md`, and `council/2026-08-04.md` (the most
recent minutes actually merged to `main`) before convening, per the chair
mandate. `council/BOARD.md` as read from `main` is stale relative to the
unmerged branches — see the operational note added to it this sitting.

### State established before convening
- `git log` tip on `main`: `9a4d5a4`, unchanged since 2026-08-04. Confirmed
  directly (not assumed): `grep -n mailto: docs/index.html docs/app.js`
  still returns `reda.tahiri@example.com` in all 3 locations — the
  production/`main` code has received **zero** commits under `docs/**` in
  15 days despite a continuous 5-hourly sitting cadence.
- Checked GitHub directly: **30 open PRs** titled "Council sitting — ..."
  stretching from `council/2026-08-05-1253` (PR #7) to today, **none
  merged**. The council pipeline is producing rulings and, on several
  branches, executing commits — but nothing is reaching the audience this
  Council exists to serve, because no PR has been merged since PR #1
  (2026-08-04).
- Read PR #35 (02:56) and PR #36 (07:54) in full. Both are today's sittings
  and both ruled on the same item, `OBJ-map-debugger-1` (dead mailto
  targets): 02:56 ruled SHIP but explicitly **held execution**, reasoning
  that the only replacement address on file is the owner's personal Gmail
  and that publishing a person's personal contact details on a public page
  is a one-way action outside any seat's or the Chair's authority to decide
  unattended. 07:54 ruled SHIP and **executed** on its own branch,
  publishing `reda.tahiri1@gmail.com` directly into `docs/app.js` and
  `docs/index.html`, without owner sign-off, on the reasoning that closing
  the last missing browser-evidence gate was "closing the gate," not
  "implementing the feature." This repository is **public**
  (confirmed via the GitHub API), so that diff is already visible to anyone
  who opens PR #36, whether or not it is ever merged.
- No rung-1/2 candidate found in-repo (`node --check docs/app.js` and
  `docs/countries.config.js` clean; all `docs/data/morocco/*.geojson` parse)
  — but the unmerged-backlog and personal-email findings above are noted as
  the sitting's most important output, ahead of any docket ranking.
- Per COUNCIL.md's SHIP budget rule, its intent is to cap unattended
  shipping to one action per rolling 24h, regardless of merge status: a
  sitting today (07:54) already ruled and executed a SHIP. This sitting is
  therefore **REPORT-only**, honoring that budget in spirit even though
  nothing has technically landed on `main`.

### Docket (pulled from `main`'s `council/BOARD.md` — unchanged since
2026-08-04; no new proposals, since nothing under `docs/**` has changed on
`main` for any seat to observe)

**frontend-engineer** — OBJ-frontend-engineer-2 (loading state), OBJ-frontend-engineer-3 (topbar overflow ≤375px) — reaffirmed, unchanged

**coord-validator** — OBJ-coord-validator-1 (`vintage` field, `industrial.geojson`), OBJ-coord-validator-2 (sample-verify `national-hv`/`transmission-lines`), OBJ-coord-validator-3 (deprecate `grid-lines.geojson`) — reaffirmed, unchanged on `main` (a sample-verify report exists on unmerged PR #36 but has not landed)

**map-debugger** — OBJ-map-debugger-1 (dead mailto — see State above), OBJ-map-debugger-2 (wire `national-hv`+`transmission-lines`, L, blocked), OBJ-map-debugger-3 (mid-session error state), OBJ-map-debugger-4 (light-theme topbar contrast) — reaffirmed, unchanged on `main`

**map-tester** — OBJ-map-tester-1 (orphan-data check), OBJ-map-tester-2 (evidence-bar checklist), OBJ-map-tester-3 (popup field-mapping audit) — reaffirmed, unchanged

**platform-engineer** — OBJ-platform-engineer-1 (CI check, M), OBJ-platform-engineer-2 (uptime check), OBJ-platform-engineer-3 (`_headers` cache-control) — reaffirmed, unchanged on `main` (a split proposal, `OBJ-platform-engineer-4`, exists on unmerged PR #36 but has not landed)

**security-engineer** — OBJ-security-engineer-1 (SRI hashes), OBJ-security-engineer-2 (disclose third-party requests), OBJ-security-engineer-3 (CSP via `_headers`) — reaffirmed, unchanged

### Ruling

SHIP: **none — REPORT-only sitting.** Today's rolling-24h SHIP budget was
already spent by the 07:54 sitting (PR #36), which ruled and executed
against `OBJ-map-debugger-1`. Stated plainly per COUNCIL.md §4/§5: no SHIP
this sitting regardless of docket ranking.

REPORT: **none dispatched.** Nothing under `docs/**` changed since the last
check, and the two live M-sized report candidates (`OBJ-coord-validator-2`,
`OBJ-platform-engineer-1`) were already actioned earlier today (07:54
sitting) — re-running the same read-only investigation a third time this
day would manufacture work, not produce new evidence. Per COUNCIL.md, "a
sitting that approves nothing is a valid and expected outcome."

Vetoed: none new. This Chair does **not** attempt to overrule either prior
sitting's ruling on `OBJ-map-debugger-1` — the Chair cannot overrule and, in
any case, both already executed or held on their own branches — but records
the disagreement between them (see State above and `council/BOARD.md`) as
the one thing this sitting most needs to hand to the owner.

Docketed (unchanged): OBJ-frontend-engineer-2, OBJ-frontend-engineer-3,
OBJ-coord-validator-1, OBJ-coord-validator-2, OBJ-coord-validator-3,
OBJ-map-debugger-1, OBJ-map-debugger-2, OBJ-map-debugger-3,
OBJ-map-debugger-4, OBJ-map-tester-1, OBJ-map-tester-2, OBJ-map-tester-3,
OBJ-platform-engineer-1, OBJ-platform-engineer-2, OBJ-platform-engineer-3,
OBJ-security-engineer-1, OBJ-security-engineer-2, OBJ-security-engineer-3

(No IDs minted this sitting.)

### Outcome
Commit:  none — REPORT-only sitting, no code touched; this branch carries
         minutes and a `council/BOARD.md` operational-note update only.
Gates:   n/a — nothing executed against `docs/**` this sitting.
Carried: Two items for the owner, ahead of any objective ranking:
         (1) **30 open, unmerged council PRs** dating back to 2026-08-05 —
         the sitting cadence is running but nothing is reaching the live
         site; the owner should either start merging the backlog (oldest
         first, since later branches assume earlier ones landed) or tell
         the Council to pause until the backlog is cleared, since every
         further sitting compounds the divergence between `main` and the
         branches.
         (2) **PR #36 published the owner's real personal Gmail address**
         into `docs/app.js`/`docs/index.html`, reversing without
         authorization the explicit privacy hold the same day's earlier
         sitting placed on that exact action. The repo is public, so this
         is already exposed in the open PR regardless of merge status.
         Recommend reviewing PR #36 specifically before deciding whether to
         merge it, and deciding what contact address (if any) is actually
         appropriate to publish, rather than accepting the branch's choice
         by default.
         The next sitting should re-check whether the owner has acted on
         either item before proposing further work.
