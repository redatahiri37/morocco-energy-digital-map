# Council sitting — 2026-08-04

## Sitting — 14:15 (Europe/Paris)

Second-ever sitting. First sitting of the new day, so today's SHIP budget
(max 1 per rolling 24 h) is unspent regardless of what happened yesterday —
confirmed independently: `council/2026-08-03.md` shows one sitting (12:00)
that ruled a SHIP but recorded "Commit: none — Chair does not implement,"
and `council/BOARD.md`'s Shipped table is still `| — | — | — | — | — |`.
No seat filed anything genuinely new this sitting: `git log` tip is
unchanged since the last sitting (`71b900f`), nothing under `docs/**` has
been touched, so every seat's Board section was pulled and reaffirmed
as-is rather than regenerated. Full detail for every objective lives on
`council/BOARD.md`; this file records only the ruling.

### State established before convening
- `git log` tip unchanged: `71b900f`. No commits landed since the
  2026-08-03 12:00 sitting. Working tree carries the same uncommitted
  `council/BOARD.md` + untracked `council/2026-08-03.md` as before —
  expected, since only a shipped sitting commits.
- Re-verified the standing SHIP candidate directly rather than trusting
  yesterday's minutes: `docs/style.css:183` still reads
  `.layer-row input{display:none}`; `grep -n tabindex docs/app.js` returns
  zero matches. `OBJ-frontend-engineer-1` was ruled SHIP once already and
  never executed — this is its **second** sitting In Progress, unexecuted.
- Checked for a live rung-1/rung-2 pre-empt before ranking anything else:
  `node --check docs/app.js` and `docs/countries.config.js` both clean;
  all 9 `docs/data/morocco/*.geojson` files parse as valid JSON. Nothing
  is down, nothing is broken. No rung-1/2 candidate exists this sitting.

### Docket (reaffirmed, unchanged from council/BOARD.md — no new proposals)

**frontend-engineer**
- OBJ-frontend-engineer-1 | Make layer-toggle checkboxes keyboard-focusable/operable (`docs/style.css` + `docs/app.js`) — reaffirmed; ruled SHIP 2026-08-03 12:00, still unexecuted, re-verified fresh this sitting
- OBJ-frontend-engineer-2 | Add a visible loading state to `#layerList`/`#kpiGrid` while `loadAllData()` awaits its fetches — reaffirmed, unchanged
- OBJ-frontend-engineer-3 | Give `.topbar` explicit overflow handling at ≤375px — reaffirmed, unchanged

**coord-validator**
- OBJ-coord-validator-1 | Add `vintage` field to all 13 `industrial.geojson` features — reaffirmed, unchanged
- OBJ-coord-validator-2 | Sample-verify `national-hv.geojson` (947) + `transmission-lines.geojson` (541) — reaffirmed, unchanged
- OBJ-coord-validator-3 | Remove or mark deprecated `grid-lines.geojson` (11 features, unreferenced) — reaffirmed, unchanged

**map-debugger**
- OBJ-map-debugger-1 | Fix dead mailto targets (`index.html:122`, `app.js:968,997`, still `example.com`) — reaffirmed, unchanged
- OBJ-map-debugger-2 | Wire `national-hv` + `transmission-lines` into the live map — reaffirmed, still L, still blocked on OBJ-coord-validator-2
- OBJ-map-debugger-3 | Surface a visible error state for mid-session MapLibre runtime failures — reaffirmed, unchanged

**map-tester**
- OBJ-map-tester-1 | Orphan-data check across `docs/data/morocco/*.geojson` vs load-path references — reaffirmed, unchanged
- OBJ-map-tester-2 | Write down the required evidence bar for a release-gate GO — reaffirmed, unchanged
- OBJ-map-tester-3 | Audit popup field-name mapping against each source file's actual keys — reaffirmed, unchanged

**platform-engineer**
- OBJ-platform-engineer-1 | Add a minimal CI check on push to `main` (no build step, no bundler) — reaffirmed, unchanged
- OBJ-platform-engineer-2 | Wire an uptime check against the live map URL — reaffirmed, unchanged
- OBJ-platform-engineer-3 | Add `docs/_headers` with cache-control for `docs/data/*.geojson` — reaffirmed, unchanged

**security-engineer**
- OBJ-security-engineer-1 | Add Subresource Integrity hashes to the MapLibre `<script>`/`<link>` tags — reaffirmed, unchanged
- OBJ-security-engineer-2 | Disclose the third-party requests the page makes on load — reaffirmed, unchanged
- OBJ-security-engineer-3 | Add a Content-Security-Policy via `docs/_headers` — reaffirmed, unchanged

### Ruling

SHIP:    OBJ-frontend-engineer-1 — re-affirmed, not re-derived from
         scratch. Verified directly this sitting, not assumed from the
         prior minutes: the layer toggles — the map's primary interaction,
         choosing which infrastructure to look at — remain provably
         unreachable by keyboard or screen reader on the live `docs/`
         pipeline right now. No rung-1 (published fact wrong) or rung-2
         (site down / pipeline broken) candidate exists this sitting
         (JS syntax clean, all GeoJSON valid, no incident), so nothing
         pre-empts the docket, and this remains the highest-ranked rung-3
         item — "the audience cannot make a decision it should be able to
         make" — on the board. It is S, reversible, touches no
         data/schema, and the correct pattern already exists in-repo
         (commit `2ed6340`) to port from the file no visitor loads into
         the live pipeline. Today's SHIP budget is unspent: the only prior
         ruling (2026-08-03 12:00) produced no commit. Handed back to
         frontend-engineer for execution and all three browser gates plus
         a map-tester GO before any commit.

REPORT:  OBJ-coord-validator-2, OBJ-platform-engineer-1 — reaffirmed for
         the same reason as last sitting, not re-litigated: both are the
         only M-sized items on the board, and both block a future S.
         coord-validator-2 (sample-verifying 1,488 unchecked ONEE/WBG line
         features) must resolve before OBJ-map-debugger-2 (L, wiring
         those features into the map) can be split into anything
         shippable. platform-engineer-1 (CI check) needs its scope pinned
         down to a pure-validation checklist before it can split into an S
         without tripping the build-step/bundler structural veto. Neither
         report has actually been executed yet (Chair does not implement);
         dispatch is handed back the same as the SHIP.

Vetoed:  none — every reaffirmed objective still sits inside the standing
         structural and truth vetoes; nothing changed to require
         relitigating any of them, and no seat proposed anything new this
         sitting that needed testing against a veto.

Docketed to council/BOARD.md: OBJ-frontend-engineer-2, OBJ-frontend-engineer-3,
OBJ-coord-validator-1, OBJ-coord-validator-2, OBJ-coord-validator-3,
OBJ-map-debugger-1, OBJ-map-debugger-2, OBJ-map-debugger-3,
OBJ-map-tester-1, OBJ-map-tester-2, OBJ-map-tester-3,
OBJ-platform-engineer-1, OBJ-platform-engineer-2, OBJ-platform-engineer-3,
OBJ-security-engineer-1, OBJ-security-engineer-2, OBJ-security-engineer-3

(No IDs minted this sitting — every ledger stays at its 2026-08-03 value:
frontend-engineer 4, coord-validator 4, map-debugger 4, map-tester 4,
platform-engineer 4, security-engineer 4.)

### Outcome
Commit:  `c808b1e` — "front-end: make layer-toggle checkboxes
         keyboard-focusable and operable". Executed by frontend-engineer
         after this ruling: `docs/style.css` `.layer-row` — added
         `position:relative`; replaced `.layer-row input{display:none}`
         with a visually-hidden-but-focusable overlay
         (`position:absolute;opacity:0`, pixel-exact over the `.check`
         span); added `.layer-row input:focus-visible + .check{outline:2px
         solid var(--c-wind);outline-offset:2px}`, matching the existing
         `.ghost-btn:focus-visible` convention already in the file. Used
         the page's real native `<input type="checkbox">` (already present
         in `docs/app.js`'s markup) rather than porting the frozen
         prototype's div+`role="checkbox"` reimplementation — fewer moving
         parts, native Space-toggle/accessibility semantics for free, zero
         JS changed.
Gates:   desktop ✓ (real Tab-key walk from `#countrySelect` through the
         topbar lands on the first layer-row checkbox;
         `getComputedStyle(.check)` shows `outline: rgb(13,148,136) solid
         2px` on focus; console clean; mouse-click toggle regression-
         checked, unaffected) | light ✓ (same Tab walk under
         `data-theme="light"`, identical outline evidence, console clean)
         | 375px ✓ (`scrollWidth === clientWidth === 375`, no horizontal
         overflow; input/`.check` `getBoundingClientRect()` byte-identical;
         Tab reaches the checkbox; console clean) | data n/a (no
         `docs/data/**` touched) | secrets n/a (CSS-only diff, no
         dependency/secret surface touched) | map-tester **GO** — quoted
         verdict: "all three COUNCIL.md §5 browser gates (desktop / light
         / 375px) have real MCP-browser-derived evidence, quoted, and
         every quoted value is independently consistent with what I read
         directly in `docs/style.css:174-197`, `docs/app.js:265-303`, and
         `docs/index.html:27-76`... This clears the bar for a real GO, not
         `GO-STATIC`." One disclosed evidence gap: the MCP browser tool's
         synthetic Space-keypress does not register on *any* checkbox on
         this page (confirmed via a control test against a freshly
         injected, fully-visible native checkbox — tool limitation, not a
         page defect), so Space-toggle is closed by source audit (zero
         `keydown`/`keypress`/`preventDefault` anywhere in `docs/app.js`,
         confirmed by map-tester's own grep) rather than direct
         observation. Enter does not toggle the checkbox — expected,
         standard native-checkbox behavior, not a regression.
Carried: OBJ-frontend-engineer-1 is now Shipped — see `council/BOARD.md`.
         A new objective was surfaced during this execution and filed to
         the Board (not fixed here — out of scope, no sweep):
         **OBJ-map-debugger-4**, a pre-existing light-theme contrast bug
         where `docs/brand.css:32-36` (`.topbar .ghost-btn{color:
         rgba(255,255,255,.85)}`, unconditional, loaded after
         `docs/style.css` "so chrome rules win") silently overrides
         `docs/style.css`'s `[data-theme="light"]` text-color rule for the
         Solaire/Methodology/GitHub/theme-toggle buttons, leaving them
         white-on-white and unreadable. Confirmed live (screenshot) and
         confirmed pre-existing via `git stash` against the unedited file.
         OBJ-map-debugger-1 (dead mailto) remains next-ranked for the SHIP
         slot, now that today's slot is spent. OBJ-map-debugger-2 (L)
         stays blocked on OBJ-coord-validator-2's report;
         OBJ-platform-engineer-1 (M) stays blocked on its own scoping
         report. Next sitting is REPORT-only regardless of ranking — one
         SHIP per rolling 24h, and this one already landed today.

## Sitting — 17:53 (Europe/Paris)

Third sitting. Read `COUNCIL.md`, the 2026-08-04 14:15 minutes, and
`council/BOARD.md` before convening. `git log` tip has moved since 14:15
only by the merge of that sitting's own SHIP: `origin/main` now sits at
`9a4d5a4` ("Merge pull request #1 from redatahiri37/front-end/layer-toggle-
a11y-focus"), one commit ahead of the `71b900f` the 14:15 minutes recorded
as unchanged. No other commit has landed. `council/BOARD.md`'s Shipped
table already carries `OBJ-frontend-engineer-1 | c808b1e`, so this sitting
adds no new fact to the Board beyond re-confirming the merge actually
reached `main` (checked directly: `git fetch origin` then
`git log origin/main --oneline -5` shows the merge commit as tip;
`git rev-parse origin/main` = `9a4d5a4`).

### State established before convening
- `node --check docs/app.js` and `docs/countries.config.js` — both clean.
- All 9 `docs/data/morocco/*.geojson` files parse as valid JSON
  (re-verified individually with `python3 -c "json.load(...)"`, not
  assumed from the last sitting).
- Spot-re-checked every Board item's underlying fact directly rather than
  trusting the 14:15 minutes: `docs/style.css`'s `@media (max-width:375px)`
  block still has no `.topbar` overflow rule; `industrial.geojson` has
  `grep -c vintage` = 0; `national-hv`/`transmission-lines` still have zero
  references in `app.js`/`countries.config.js`; `mailto:reda.tahiri@
  example.com` still appears at `index.html:122` and `app.js:968,997`;
  no `integrity=` attribute anywhere in `index.html`; no
  `Content-Security-Policy` anywhere in `index.html`; `docs/_headers` and
  `.github/workflows/` both still absent. Every Board fact holds unchanged
  — nothing resolved itself, nothing new observed. No seat has anything
  genuinely new to file this sitting; every seat's docket below is pulled
  from `council/BOARD.md` and reaffirmed as-is, per COUNCIL.md §8's
  "pull, don't regenerate."
- Attempted a live-reachability check against
  `https://atlas-nexus-69o.pages.dev/` (the deployed map, per README.md)
  as an independent rung-2 check beyond local file validation. The
  sandbox's outbound proxy returned a 403 policy denial on the CONNECT to
  that host (`recentRelayFailures` in `$HTTPS_PROXY/__agentproxy/status`:
  `connect_rejected`, "gateway answered 403 to CONNECT"). This is an
  environment network-policy restriction on this specific run, not a
  signal from the site itself — no HTTP response was ever received to
  read a status code from — so it is **not** treated as evidence the site
  is down. Recorded for transparency, not acted on as a rung-2 finding.
- No rung-1 (published fact wrong) or rung-2 (site down / pipeline broken)
  candidate is live this sitting.

### Docket (reaffirmed, unchanged from `council/BOARD.md` — no new proposals; OBJ-frontend-engineer-1 shipped 14:15, no longer open)

**frontend-engineer**
- OBJ-frontend-engineer-2 | Add a visible loading state to `#layerList`/`#kpiGrid` while `loadAllData()` awaits its fetches — reaffirmed, unchanged
- OBJ-frontend-engineer-3 | Give `.topbar` explicit overflow handling at ≤375px — reaffirmed, unchanged; re-confirmed the media-query block still has no such rule

**coord-validator**
- OBJ-coord-validator-1 | Add `vintage` field to all 13 `industrial.geojson` features — reaffirmed, unchanged
- OBJ-coord-validator-2 | Sample-verify `national-hv.geojson` (947) + `transmission-lines.geojson` (541) — reaffirmed, unchanged
- OBJ-coord-validator-3 | Remove or mark deprecated `grid-lines.geojson` (11 features, unreferenced) — reaffirmed, unchanged

**map-debugger**
- OBJ-map-debugger-1 | Fix dead mailto targets (`index.html:122`, `app.js:968,997`, still `example.com`) — reaffirmed, unchanged; next-ranked for the SHIP slot once one is available
- OBJ-map-debugger-2 | Wire `national-hv` + `transmission-lines` into the live map — reaffirmed, still L, still blocked on OBJ-coord-validator-2
- OBJ-map-debugger-3 | Surface a visible error state for mid-session MapLibre runtime failures — reaffirmed, unchanged
- OBJ-map-debugger-4 | Fix light-theme topbar button contrast in `docs/brand.css` — reaffirmed, unchanged (surfaced 14:15, carried to the Board, not yet actioned)

**map-tester**
- OBJ-map-tester-1 | Orphan-data check across `docs/data/morocco/*.geojson` vs load-path references — reaffirmed, unchanged
- OBJ-map-tester-2 | Write down the required evidence bar for a release-gate GO — reaffirmed, unchanged
- OBJ-map-tester-3 | Audit popup field-name mapping against each source file's actual keys — reaffirmed, unchanged

**platform-engineer**
- OBJ-platform-engineer-1 | Add a minimal CI check on push to `main` (no build step, no bundler) — reaffirmed, unchanged
- OBJ-platform-engineer-2 | Wire an uptime check against the live map URL — reaffirmed, unchanged
- OBJ-platform-engineer-3 | Add `docs/_headers` with cache-control for `docs/data/*.geojson` — reaffirmed, unchanged

**security-engineer**
- OBJ-security-engineer-1 | Add Subresource Integrity hashes to the MapLibre `<script>`/`<link>` tags — reaffirmed, unchanged
- OBJ-security-engineer-2 | Disclose the third-party requests the page makes on load — reaffirmed, unchanged
- OBJ-security-engineer-3 | Add a Content-Security-Policy via `docs/_headers` — reaffirmed, unchanged

### Ruling

SHIP:    none — **REPORT-only sitting.** Today's `council/2026-08-04.md`
         14:15 sitting already shipped `OBJ-frontend-engineer-1`
         (`c808b1e`, merged to `main` at `9a4d5a4`), confirmed by reading
         that file before convening, per COUNCIL.md §4: "at most 1 [SHIP]
         per rolling 24 h, across all sittings... If an earlier sitting
         today already shipped, the current sitting is REPORT-only." No
         rung-1/rung-2 finding is live to pre-empt that limit regardless
         (state check above). OBJ-map-debugger-1 (dead mailto) remains
         next-ranked for the SHIP slot the next time one is open.

REPORT:  OBJ-coord-validator-2, OBJ-platform-engineer-1 — reaffirmed
         unchanged from the 14:15 ruling, not re-litigated: these remain
         the only two M-sized Board items, and both still block a future
         S from being split out (coord-validator-2 blocks
         map-debugger-2's L; platform-engineer-1's own scope must narrow
         before it can become an S without risking the build-step/
         bundler structural veto). Neither has been executed since being
         ruled REPORT at 14:15 — this Chair does not implement or
         investigate on a seat's behalf (COUNCIL.md: "You rule; seats
         execute"); dispatch is handed back to `coord-validator` and
         `platform-engineer` for actual execution before the next
         sitting, same as it was three hours ago.

Vetoed:  none — nothing new was proposed this sitting to test against a
         veto, and every reaffirmed Board item still sits inside the
         standing structural and truth vetoes without change.

Docketed: OBJ-frontend-engineer-2, OBJ-frontend-engineer-3,
OBJ-coord-validator-1, OBJ-coord-validator-2, OBJ-coord-validator-3,
OBJ-map-debugger-1, OBJ-map-debugger-2, OBJ-map-debugger-3,
OBJ-map-debugger-4, OBJ-map-tester-1, OBJ-map-tester-2, OBJ-map-tester-3,
OBJ-platform-engineer-1, OBJ-platform-engineer-2, OBJ-platform-engineer-3,
OBJ-security-engineer-1, OBJ-security-engineer-2, OBJ-security-engineer-3

(No IDs minted this sitting — every ledger stays at its 2026-08-04 14:15
value: frontend-engineer 4, coord-validator 4, map-debugger 5, map-tester
4, platform-engineer 4, security-engineer 4. `council/BOARD.md` is
otherwise unchanged by this sitting — no card moved, since nothing
shipped and nothing new was filed or resolved.)

### Outcome
Commit:  none — REPORT-only sitting; only these minutes are committed,
         on branch `council/2026-08-04-1753`, no `docs/**` change.
Gates:   desktop n/a | light n/a | 375px n/a | data n/a | secrets n/a |
         map-tester n/a — no code shipped this sitting, so no gate
         applies.
Carried: OBJ-map-debugger-1 stays next-ranked for the SHIP slot once the
         24 h window since 14:15 clears. OBJ-coord-validator-2 and
         OBJ-platform-engineer-1 remain ruled REPORT but unexecuted for a
         second consecutive sitting — if a third sitting passes with
         neither actually investigated, the Chair should note in that
         sitting's minutes that the REPORT slot is not converting to any
         real output under the current unattended setup, since no
         mechanism exists this run to dispatch a seat to actually
         execute read-only investigative work. OBJ-map-debugger-2 (L)
         and OBJ-platform-engineer-1 (M) remain not-yet-shippable as
         before.
