# Council sitting — 2026-08-04

## Sitting — 13:05 (Europe/Paris)

First-ever sitting of the Atlas Nexus Council. `council/BOARD.md` was empty
(no In Progress, no Docketed, no Shipped, no Vetoed) — nothing to pull
forward. No earlier minutes file existed for today.

### State
`git log` since Council ratification shows only Council scaffolding commits
(`f6da5d8`, `6863189`, `71b900f`) — no map work has landed since the Council
stood up. Working tree was clean at sitting start. No visible outage.

### Docket

**frontend-engineer**
- OBJ-frontend-engineer-1 | Make the six layer-toggle checkboxes keyboard- and screen-reader-operable (`display:none` currently drops them from the tab order and a11y tree entirely)
- OBJ-frontend-engineer-2 | Give `loadAllData()` a visible per-layer failure state instead of silently swallowing fetch errors into an empty FeatureCollection
- OBJ-frontend-engineer-3 | Fix topbar control layout at the Council's own ≤375px gate (brand + country selector + 3 ghost buttons + icon button overflow with no wrap/overflow handling)

**coord-validator** — flagged rung-1: Renault Tangier Plant coordinate ~22km from its real Melloussa/Tanger-Med site despite `precision:"exact"`
- OBJ-coord-validator-1 | Correct the Renault Tangier Plant coordinate in `industrial.geojson`
- OBJ-coord-validator-2 | Add `vintage`/`source_url` to all 947 features in `national-hv.geojson` (currently neither)
- OBJ-coord-validator-3 | Add explicit `vintage` field to all 541 features in `transmission-lines.geojson` (WBG-sourced, no structured date)

**map-debugger** — confirmed no rung-2 (site-down) issue live on static read-through
- OBJ-map-debugger-1 | Point the Methodology modal's source-doc links at files that exist in the deployed site (`../DATA_SOURCES.md`/`../ASSUMPTIONS.md` 404 in production — those files exist only at repo root, `docs/` is the deploy root)
- OBJ-map-debugger-2 | Replace the fake `@example.com` error-report email (index.html:122, app.js:968,997) with a reachable address
- OBJ-map-debugger-3 | Fix the feature-id mismatch that silently disables hover-dim on the industrial and digital layers (`promoteId:"id"` vs. numeric `f.id` used by `setFeatureState`, app.js:670/709 vs. 850-871)

**map-tester**
- ~~OBJ-map-tester-1 | Add a `?selftest=1` in-browser harness~~ — **rejected, failed North Star Test** (unlocks names "a map-tester GO," the Council's own process, not a regulator/developer's job)
- OBJ-map-tester-2 | Record layer load failures to `window.__loadErrors` instead of only a transient `console.warn`
- OBJ-map-tester-3 | Detect and report basemap tile failures that occur after map init, not just at load

**platform-engineer** — confirmed no rung-2 issue live
- OBJ-platform-engineer-1 | CI: `node --check app.js` + validate every `docs/data/**/*.geojson` as parseable JSON on every push/PR to main
- OBJ-platform-engineer-2 | Scheduled hourly health check against both Pages URLs, fails visibly on non-200
- ~~OBJ-platform-engineer-3 | Auto-deploy workflow on push to main~~ — **cannot be actioned**, requires a `CLOUDFLARE_API_TOKEN` repo secret; the Council does not handle credentials (hard rule)

**security-engineer** — confirmed no live secret leak or active exposure
- OBJ-security-engineer-1 | Reject `javascript:`/`data:` URI schemes before rendering `source_url` as an `<a href>` (XSS-shaped; app.js lines 893, 903, 961, 969, 990)
- OBJ-security-engineer-2 | Add Subresource Integrity to the unpkg `maplibre-gl@4.7.1` tags
- OBJ-security-engineer-3 | Self-host Inter + JetBrains Mono instead of loading from Google Fonts (leaks visitor IP to Google on every load)

### Ruling

**SHIP: none shipped** (attempted — see Outcome below).

Rung 1 (a published fact is wrong) pre-empts the docket: **OBJ-coord-validator-1** (Renault Tangier Plant, ~22km mislocated despite `precision:"exact"`) outranks every other objective this sitting regardless of size or seat. Ruled SHIP-eligible: S-sized, single file, reversible.

Independently re-derived (WebSearch + geographic reasoning, since live geocoding of the exact site was not fetchable — see Outcome) that the plant sits in Zone Franche Melloussa 1 / Tanger-Med free zone, ~22km from the dataset's point near the Ceuta/Fnideq coast. Coordinate corrected to `[-5.684, 35.695]`, `precision` honestly downgraded from `"exact"` to `"approximate"` (consistent with 9 of the file's other 13 features already using that label) since no live source could be fetched to certify survey-grade precision in this sandbox.

Data gate (coord-validator, re-verify): **PASS**. Secrets gate (security-engineer): **PASS**. Release gate (map-tester): **GO-STATIC only** — MapLibre GL JS (unpkg.com) and basemap tiles (basemaps.cartocdn.com) are both blocked by this sandbox's outbound network policy (confirmed via proxy CONNECT-tunnel 403s and a live headless-Chromium run: `net::ERR_TUNNEL_CONNECTION_FAILED` on both, `[MoroccoMap] MapLibre GL not loaded`, no map ever rendered). Per COUNCIL.md's hard rule, **GO-STATIC is not shippable**. The fix itself is not in question — the sandbox cannot render the map at all, for any change, so no genuine browser-level Release gate could be cleared this sitting.

Per the task's own instruction ("include the change on the branch only if every §5 gate passed"), the data commit was **reverted** from this branch. `docs/data/morocco/industrial.geojson` is unchanged relative to `main`.

REPORT: OBJ-security-engineer-1, OBJ-map-debugger-3 — both got full read-only root-cause diagnosis this sitting (exact line numbers, exact mechanism) with no edits made; ready for direct implementation next time they're picked up.

Vetoed: none (no seat exercised veto power this sitting).

Rejected (failed North Star Test): OBJ-map-tester-1 — Chair — `unlocks:` named "a map-tester GO," the Council's own process, not a regulator or DC developer doing their job.

Un-actionable (moved to Icebox): OBJ-platform-engineer-3 — requires a `CLOUDFLARE_API_TOKEN` repo secret; Council hard rule forbids handling credentials.

Docketed (all remaining 15 objectives, unranked changes from above): OBJ-frontend-engineer-1/2/3, OBJ-coord-validator-1/2/3, OBJ-map-debugger-1/2/3, OBJ-map-tester-2/3, OBJ-platform-engineer-1/2, OBJ-security-engineer-1/2/3.

### Outcome
Commit:  none — gate failed (Release gate: GO-STATIC only; data fix committed then reverted on `council/2026-08-04-1303`)
Gates:   desktop n/a (render blocked) | light n/a (render blocked) | 375px n/a (render blocked) | data ✓ | secrets ✓ | map-tester GO-STATIC (not shippable)

**Structural finding, not specific to this fix:** this execution environment's outbound network policy blocks `unpkg.com` (MapLibre GL JS) and `basemaps.cartocdn.com` (basemap tiles) — the two external hosts `docs/index.html` depends on to render at all. Any future unattended sitting attempting a SHIP that touches `docs/**` will hit the same Release-gate wall, because no genuine browser verification is possible here regardless of what changed. This is worth the user's attention: either the sandbox's network allowlist needs `unpkg.com` and `basemaps.cartocdn.com` added, or the Council's autonomous SHIP slot is effectively inert until a sitting runs somewhere that can reach those hosts.

Carried: OBJ-coord-validator-1 is the top-ranked SHIP candidate for the next sitting — Data and Secrets gates already PASS; it only needs a Release gate cleared in an environment that can actually reach unpkg.com/cartocdn.com. Exact diff: `industrial.geojson`'s `renault-tangier` feature, `coordinates` → `[-5.684, 35.695]`, `precision` → `"approximate"`. Everything else stays on the Board per COUNCIL.md §8.
