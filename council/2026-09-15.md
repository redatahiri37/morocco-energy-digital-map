# Council sitting — 2026-09-15

## Sitting — (autonomous run, no human in the loop)

### State established before convening

- Working branch at sitting start: `council/2026-08-28-1047` (tip `8f093f9`).
  No Council commits have landed anywhere since that sitting — the interval
  since 2026-08-28 carried no map sitting at all.
- **Rung-2 pre-empt check, negative.** `https://atlas-nexus-69o.pages.dev/`
  returns HTTP 200. `node --check` is clean on `docs/app.js` and
  `docs/countries.config.js` on `origin/main`; all 9
  `docs/data/morocco/*.geojson` parse as valid JSON. The site is not down.
- **`origin/main` moved substantially since this branch was cut**, via three
  merged PRs unrelated to the map (`#49`, `#51` — Atlas Solar instrumentation
  and branding, out of remit; `#52` — a constitutional amendment, see below).
  None of the three touch `docs/**`.
- **A load-bearing discovery: the last sitting's own SHIP never reached
  production.** `git log origin/main` does not contain `bb0f017`. PR #47
  (which carried it) was **closed without merging** on 2026-08-29. Reading
  `origin/main:docs/app.js` directly confirms the file is unchanged since
  `4100942` — the OBJ-map-debugger-5 defect (layer panel publishes
  "Interconnectors — 3", map renders zero) **is still live on the deployed
  site today.** The Board's "Shipped" claim for this objective, and for
  OBJ-map-debugger-4 (also claimed via a closed PR, #46), were both false.
  Both are corrected on the Board this sitting; see the Shipped-table note.
- **COUNCIL.md was amended 2026-09-06 (§9, via merged PR #52) and this
  Council had not yet operated under it.** The amendment: (a) splits every
  sitting's output into a **record** (`council/**`, merged by the Chair
  itself, guarded by `scripts/council-record.sh`) and a **change**
  (`docs/**`, human-gated, recorded as `Awaiting merge`); (b) a **merge-queue
  alarm** — at 3+ SHIPs `Awaiting merge`, no new SHIP may be approved until
  one lands; (c) any objective already sitting in an open PR is `Awaiting
  merge` and may not be re-proposed. This Council was convened under an
  operating brief that predates the amendment and does not grant it the
  tooling to complete §9 in full (merging to `origin/main`, sending the
  mandated sitting email to Reda) — see "Not executed this sitting," below.
- **PR #48** (`council/consolidation-2026-08-28` → `main`) has been open
  since 2026-08-29, is `MERGEABLE`/`CLEAN`, +8212/−562 across 26 files, and
  carries a `map-tester` **GO** in its own description. It bundles a
  *better* (per-source, not shared-source) fix for OBJ-map-debugger-5,
  originally authored in PR #24 on 2026-08-12 — sixteen days before the
  2026-08-28 sitting re-derived the same defect from scratch, not knowing
  it had already been fixed once. It also bundles OBJ-map-debugger-1
  (issues route, not mailbox), OBJ-map-debugger-4, OBJ-coord-validator-3,
  OBJ-coord-validator-1 (re-sized S→M, popup-only), OBJ-security-engineer-2,
  and OBJ-platform-engineer-1 — **seven** objectives' worth of gated,
  reviewed code sitting unmerged.
- **Per COUNCIL.md §9 rule 5, the merge-queue alarm is triggered** — seven
  SHIPs `Awaiting merge` is well past the "three or more" threshold. Stated
  plainly, as the rule requires: **no new SHIP may be approved this sitting,
  or any sitting, until PR #48 lands (or is explicitly split/rejected).**
  This holds regardless of what else is proposed, and regardless of the
  fact that OBJ-map-debugger-5 is independently rung-1 (a published fact is
  wrong) on its own merits — the correct remedy is merging code that
  already exists and is already gated, not authoring more of it.

### Docket

**frontend-engineer**
- OBJ-frontend-engineer-3 | Give `.topbar` explicit overflow handling at ≤375px — reaffirmed, severity still elevated, not touched by PR #48
- OBJ-frontend-engineer-4 | Show an explicit empty state on a layer row whose source renders zero features — reaffirmed, strengthened by OBJ-map-debugger-8 below
- OBJ-frontend-engineer-2 | Add a visible loading state to `#layerList`/`#kpiGrid` while data loads — reaffirmed, unchanged

**coord-validator**
- OBJ-coord-validator-2 | Sample-verify `national-hv.geojson` (947) + `transmission-lines.geojson` (541) — reaffirmed; only genuinely open item this seat has (OBJ-coord-validator-1 and -3 are now Awaiting merge in PR #48 and may not be re-proposed per COUNCIL.md §9 rule 4)

**map-debugger**
- OBJ-map-debugger-8 | Fix the digital-infrastructure layer rendering zero features — NEW, minted this sitting (matching the ID `map-tester` had already informally assigned while gating PR #48, to avoid a future collision); same panel-says-N/map-draws-zero class as OBJ-map-debugger-5, confirmed live on `main` today
- OBJ-map-debugger-7 | Replace or key the CARTO raster basemap ("API KEY REQUIRED" watermark) — reaffirmed; still requires a principal decision, still not shipped by PR #48 either
- OBJ-map-debugger-3 | Surface a visible error state for mid-session MapLibre runtime failures — reaffirmed, unchanged

**map-tester**
- OBJ-map-tester-3 | Audit popup field-name mapping against each source file's actual property keys — reaffirmed, materially strengthened again by OBJ-map-debugger-8
- OBJ-map-tester-4 | Fix bold map labels 404ing against `demotiles.maplibre.org` — NEW, minted this sitting (matching `map-tester`'s own ID from gating PR #48)
- OBJ-map-tester-1 | Add an orphan-data check across `docs/data/morocco/*.geojson` vs the load path — reaffirmed, unchanged

**platform-engineer**
- OBJ-platform-engineer-3 | Add `docs/_headers` with explicit cache-control for `docs/data/*.geojson` — reaffirmed, unchanged
- OBJ-platform-engineer-2 | Wire an uptime check against the live map URL — reaffirmed, unchanged
- (only two filed — OBJ-platform-engineer-1 is now Awaiting merge in PR #48; no third item had a fresh observation behind it, so none was invented to pad the docket)

**security-engineer**
- OBJ-security-engineer-3 | Add a Content-Security-Policy via `docs/_headers` — reaffirmed, unchanged
- OBJ-security-engineer-1 | Add Subresource Integrity hashes to the MapLibre tags — reaffirmed; still blocked on tree state, `docs/index.html` is still dirty as of this sitting
- (only two filed — OBJ-security-engineer-2 is now Awaiting merge in PR #48, on a clean-tree implementation that resolves its prior block)

### Ruling

SHIP:    none — **blocked by the merge-queue alarm, COUNCIL.md §9 rule 5.**
         Seven SHIPs are `Awaiting merge` in the single open PR #48
         (threshold is three), so this Council approves no new SHIP until
         PR #48 lands or is explicitly split/rejected. This is stated as
         the rule requires, not as a soft preference. Separately, and
         reinforcing the same conclusion: the strongest rung-1 candidate on
         today's docket (OBJ-map-debugger-8, and OBJ-map-debugger-5 in its
         already-fixed form) would be the wrong thing to re-author from
         scratch a third time — OBJ-map-debugger-5 has now been
         independently diagnosed and fixed twice (PR #24, then again as
         `bb0f017`) without either fix reaching production, which is
         exactly the failure pattern §9 was written to stop.

REPORT:  OBJ-coord-validator-2 — reaffirmed. Oldest unresolved item on the
         Board, blocks OBJ-map-debugger-2 (L), and is the only coord-
         validator item not already resolved in PR #48.

         OBJ-map-debugger-8 — new finding surfaced by `map-tester`'s gate of
         PR #48, not yet root-caused by this Council. Recorded here rather
         than shipped, since size and root cause are both undetermined
         (S–M, diagnosis not done).

Vetoed:  none this sitting. The standing veto (OBJ-map-debugger-1, mailbox
         route) is unchanged and re-confirmed still in force; a *different*
         implementation (GitHub issues, no mailbox) is Awaiting merge in
         PR #48 and does not revisit the vetoed approach.

Docketed: OBJ-frontend-engineer-2, OBJ-frontend-engineer-3,
OBJ-frontend-engineer-4, OBJ-coord-validator-2, OBJ-map-debugger-2,
OBJ-map-debugger-3, OBJ-map-debugger-7, OBJ-map-debugger-8,
OBJ-map-tester-1, OBJ-map-tester-3, OBJ-map-tester-4,
OBJ-platform-engineer-2, OBJ-platform-engineer-3, OBJ-security-engineer-1,
OBJ-security-engineer-3

Awaiting merge (PR #48, not docketed, not re-proposed): OBJ-map-debugger-1,
OBJ-map-debugger-4, OBJ-map-debugger-5, OBJ-coord-validator-1,
OBJ-coord-validator-3, OBJ-platform-engineer-1, OBJ-security-engineer-2

IDs minted this sitting: OBJ-map-debugger-8, OBJ-map-tester-4 (both chosen
to match the IDs `map-tester` had already informally used while gating
PR #48, so they will not collide if that PR's own Board rewrite ever lands).
Ledger moves to: map-debugger 9, map-tester 5. All others unchanged.

### Outcome
Commit:  none — no SHIP was approved, so there is no code change to record.
Gates:   desktop n/a | light n/a | 375px n/a | data n/a | secrets n/a |
         map-tester n/a — REPORT-only sitting, no gates apply.

**Not executed this sitting, and flagged rather than silently skipped:**
COUNCIL.md §9 (ratified 2026-09-06, after this Council's operating brief was
last updated) directs the Chair to (a) land this sitting's record itself by
committing `council/**` only to a fresh branch, verifying with
`scripts/council-record.sh --verify`, and merging that PR before the sitting
ends, and (b) email a summary plus the telemetry briefing to
`reda.tahiri1@gmail.com`. Neither was performed: this run's brief explicitly
asked for a ruling to hand off for implementation rather than for the Chair
to push/merge to `origin/main` or send mail, and no mail-sending capability
was available regardless. `council/BOARD.md` and this minutes file were
written to disk but are **not yet committed or merged** — the operator
handling hand-off should either run `scripts/council-record.sh --branch
council/record-2026-09-15` themselves or confirm the Chair should do so on
a future run now that §9 is known to apply.

Carried: 1. **PR #48 must be reviewed and merged (or explicitly split/
            rejected) by a human before any new SHIP can be approved.** This
            is the sitting's central finding. It is not a Council action.
         2. OBJ-map-debugger-8 (digital layer renders zero features) needs
            root-causing before it can be sized and ranked for SHIP — it is
            live on production today and is the strongest rung-1 candidate
            once the merge queue clears.
         3. OBJ-map-debugger-7 (CARTO watermark) still needs a principal
            decision (credential vs. keyless provider); still not shipped
            by PR #48 either.
         4. `docs/index.html` is still dirty (unrelated SEO/JSON-LD/OG
            work) — OBJ-security-engineer-1 stays blocked on tree state,
            not merit, for the third sitting running.
         5. Once PR #48's status is resolved, the Board's "Awaiting merge"
            section must be reconciled against whatever actually lands —
            some of its seven objectives may land, some may be rejected and
            need re-opening as ordinary Docketed items.
         6. This Council should be re-briefed under COUNCIL.md §9 in full
            (record-landing + telemetry + mail) before its next run, so the
            gap noted above under "Not executed this sitting" does not
            recur.
