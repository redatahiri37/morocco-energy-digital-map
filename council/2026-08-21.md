# Council sitting — 2026-08-21

## Sitting — 02:55 (Europe/Paris)

REPORT-only sitting. `main`'s tip is unchanged since 2026-08-04
(`9a4d5a4`), so no seat's ground truth has moved — but investigating that
non-movement surfaced the sitting's real finding: **41 council PRs
(#2–#42, spanning 2026-08-04 through 2026-08-20) are open and unmerged
against `main`.** Every sitting since 2026-08-04 14:15 has branched fresh
off `main` — never off a sibling sitting's branch — so this Chair could
not read "the most recent minutes" from `main` at all; the file there
stops at `council/2026-08-04.md`. The actual most recent sitting
(2026-08-20 23:00, PR #42, branch `council/2026-08-20-2100`) was found by
listing open PRs, not by reading the repo tree this sitting started from.
See Carried below — this is a problem only Reda can resolve.

### State established before convening
- `git log` tip on `main`: `9a4d5a4`, unchanged since the 2026-08-04
  14:15 sitting's merge. `git status` clean.
- Freshly re-verified rung-1/2 preconditions directly, not assumed:
  `node --check docs/app.js` and `docs/countries.config.js` both clean;
  all 9 `docs/data/morocco/*.geojson` files parse as valid JSON via a
  fresh Python `json.load` pass. `docs/index.html:122` and
  `docs/app.js:968,997` still read `reda.tahiri@example.com`. Nothing is
  down, nothing is broken, no published fact was found wrong. No rung-1/2
  candidate pre-empts the docket.
- Listed open PRs via the GitHub API (`state=open`, sorted by creation
  date) rather than trusting `main`'s stale `council/` tree: **41 open
  PRs**, #2 through #42, every single one based on `main` at `9a4d5a4`,
  none merged, spanning 16 calendar days. The most recent, PR #42
  ("Council sitting — 2026-08-20 23:00", branch
  `council/2026-08-20-2100`), was opened 2026-08-20T21:02:55Z — **3h52m**
  before this sitting convened, well inside the rolling-24h SHIP window.
- Read PR #42's minutes and Board directly (`git show
  origin/council/2026-08-20-2100:council/2026-08-20.md` /
  `:council/BOARD.md`) to reconstruct true current state, since it is
  strictly more current than anything reachable from `main`. That sitting
  ruled **SHIP: OBJ-platform-engineer-1** (`.github/workflows/validate.yml`,
  a CI GeoJSON/syntax validation check) with both a build report and an
  independent map-tester GO — but because PR #42 is unmerged, that CI
  check is **not actually active on `main`** today. `main` still has no
  `.github/workflows/` directory.

### Docket (pulled forward from `council/2026-08-20-2100`'s Board — the
truest available current state — not re-derived; nothing in the repo has
changed since that Board was written 3h52m ago, so re-running all six
seats from scratch would reproduce the same 17 objectives verbatim)

**frontend-engineer** — OBJ-1 (Shipped 2026-08-04, `c808b1e`), OBJ-2
(loading state for `#layerList`/`#kpiGrid`, S), OBJ-3 (topbar overflow
handling ≤375px, S)

**coord-validator** — OBJ-1 (vintage field on `industrial.geojson`, S),
OBJ-2 (sample-verify `national-hv` + `transmission-lines` — **resolved
2026-08-20: PASS, 0 FAIL, 0 UNVERIFIED**, new finding of 57% corridor
overlap folded into map-debugger-2), OBJ-3 (remove/mark deprecated
`grid-lines.geojson`, S)

**map-debugger** — OBJ-1 (dead mailto, S, blocked pending Reda confirming
a real address), OBJ-2 (wire `national-hv` + `transmission-lines` into
the map, L, unblocked on coordinates, now blocked on a dedup design
call), OBJ-3 (visible MapLibre runtime-error state, S), OBJ-4
(light-theme topbar contrast, S)

**map-tester** — OBJ-1 (orphan-data check, S), OBJ-2 (write the GO
evidence bar, S), OBJ-3 (popup field-mapping audit, S)

**platform-engineer** — OBJ-1 (**SHIP-ruled 2026-08-20 23:00**, CI
validation workflow — see PR #42, not yet merged), OBJ-2 (uptime check
against the live map URL, S), OBJ-3 (`docs/_headers` cache-control for
`docs/data/*.geojson`, S)

**security-engineer** — OBJ-1 (SRI hashes on the MapLibre tags, S), OBJ-2
(disclose third-party requests on load, S), OBJ-3 (CSP via
`docs/_headers`, S)

### Ruling

**Rung check:** no rung-1 (published fact wrong) or rung-2 (site down /
pipeline can ship something broken) candidate is live on `main` right
now — the CI gap that justified the 2026-08-20 23:00 SHIP still exists on
`main` (that PR is unmerged), but ruling a second SHIP to fix the same
already-ruled gap a second time would not close it any faster; it would
just add PR #43 to a 41-deep pile. Nothing new pre-empts the docket.

SHIP:    **none — REPORT-only.** The SHIP budget is 1 per rolling 24h
         *across all sittings*, and PR #42 (2026-08-20 23:00) ruled one
         3h52m before this sitting convened — inside the window by any
         reasonable reading, regardless of merge status. §4's literal
         mechanism ("check today's `council/YYYY-MM-DD.md`") assumes
         sittings land on `main` in sequence; that assumption is false
         here, since nothing has merged since 2026-08-04. Reading the
         budget by its intent rather than by a mechanism that has quietly
         stopped functioning: this sitting treats PR #42's ruling as
         having spent today's slot, and does not compound the unmerged
         backlog with a second SHIP ruling 4 hours later.

REPORT:  **none run this sitting.** Both standing REPORT candidates
         (OBJ-coord-validator-2, OBJ-platform-engineer-1's scoping) were
         already fully executed 3h52m ago in PR #42 and nothing in the
         repo has changed since — re-running either would reproduce
         identical findings. Per COUNCIL.md §8 ("pull, don't regenerate")
         and §4 ("a sitting that approves nothing is valid"), this sitting
         spends its effort on establishing state and the backlog finding
         instead of manufacturing a third look at the same two questions.

Vetoed:  none — no seat vetoed anything; nothing new was proposed to test
         against a veto.

Docketed to council/BOARD.md: unchanged from `council/2026-08-20-2100`'s
Board — OBJ-frontend-engineer-2, OBJ-frontend-engineer-3,
OBJ-coord-validator-1, OBJ-coord-validator-3, OBJ-map-debugger-1 (blocked
pending human), OBJ-map-debugger-2 (blocked pending dedup design),
OBJ-map-debugger-3, OBJ-map-debugger-4, OBJ-map-tester-1, OBJ-map-tester-2,
OBJ-map-tester-3, OBJ-platform-engineer-2, OBJ-platform-engineer-3,
OBJ-security-engineer-1, OBJ-security-engineer-2, OBJ-security-engineer-3

(No IDs minted this sitting. Ledger unchanged: frontend-engineer 4,
coord-validator 4, map-debugger 5, map-tester 4, platform-engineer 4,
security-engineer 4.)

### Outcome
Commit:  none — REPORT-only sitting, nothing executed.
Gates:   n/a — no change proposed, nothing to gate.
Carried: **Top priority, needs Reda, not the next sitting:** 41 open
         council PRs (#2–#42) sit unmerged against `main`, dating back to
         2026-08-04. `main` has not moved since `c808b1e`. Every SHIP any
         sitting has ruled since then — including the CI workflow in
         PR #42, which is fully built, gated, and GO'd — exists only on a
         branch nobody has merged. Each new sitting branches off the same
         stale `main`, so the Council's own memory (`council/BOARD.md`,
         `council/*.md` on `main`) has been frozen at 2026-08-04 for 16
         days while 40+ sibling branches each independently reconstruct a
         newer picture that never makes it back. Recommend: merge PR #42
         first (smallest, cleanest, independently double-gated — adds CI
         with zero product-surface change), then triage the remaining 40
         for what's still current vs. superseded, and consider pausing
         the 5-hourly cadence until the backlog clears — every sitting
         that runs before then adds PR #43, #44, ... to the same pile
         rather than building on the last one.

         Once `main` catches up, the next-ranked SHIP-eligible items are
         OBJ-map-debugger-4 (light-theme contrast, S, ready) and
         OBJ-coord-validator-3 (deprecated `grid-lines.geojson`, S,
         ready). OBJ-map-debugger-1 stays blocked on Reda confirming a
         real contact address; OBJ-coord-validator-1 needs external
         research before an unattended sitting should fill in a vintage
         year; OBJ-map-debugger-2 needs a map-debugger dedup design call.

         Next sitting (~07:55 Europe/Paris) must re-check `main`'s tip
         and the open-PR count before ruling on the SHIP budget — do not
         assume this sitting's read is still current.
