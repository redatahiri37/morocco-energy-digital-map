# Council sitting — 2026-08-12

**Editorial note (added by the 21:04 sitting):** this file was reconstructed
from four independently-branched, mutually-blind sittings today (03:03,
07:57, 13:12, 18:00) — each branched from the same stale `main` (still at
the PR #1 merge commit, since no council PR has merged since) and so each
believed itself to be the day's first sitting. See the 21:04 section below
for the consequences (duplicate SHIPs, an ID collision) discovered while
reconstructing this record. Sitting bodies below are otherwise verbatim.

## Sitting — 03:03 (Europe/Paris)

First sitting of the day and the first sitting since 2026-08-04 14:15 — an
8-day gap in the cadence (unexplained by anything in this repo; noted, not
investigated further, since it is outside the Chair's remit). Today's SHIP
budget (max 1 per rolling 24 h) is fully unspent regardless of that gap:
no `council/2026-08-12.md` existed before this sitting.

### State established before convening
- `git log` tip unchanged since the last sitting: `9a4d5a4` (merge of
  `c808b1e`, the 2026-08-04 keyboard-focus SHIP). Nothing under `docs/**`
  had been touched in the 8 days between sittings.
- Rung-1/rung-2 pre-empt check: `node --check docs/app.js` and
  `docs/countries.config.js` both clean; all `docs/data/morocco/*.geojson`
  files parse as valid JSON. Nothing is down, nothing is broken. No
  rung-1/2 candidate this sitting.
- Re-verified the standing next-ranked candidate directly: `grep -rn
  "example.com" docs/` still returned all 3 dead mailto targets
  (`docs/index.html:122`, `docs/app.js:968,997`), unchanged since first
  flagged.

### Docket

**frontend-engineer**
- OBJ-frontend-engineer-2 | Loading state for `#layerList`/`#kpiGrid` — reaffirmed, unchanged
- OBJ-frontend-engineer-3 | `.topbar` overflow handling at ≤375px — reaffirmed, unchanged
- OBJ-frontend-engineer-4 | *(new)* Clamp the hover-tooltip position to the viewport in `positionTooltip()` (`docs/app.js:906-911`) — a tooltip hovered within ~120px of the map canvas's left/right/top edge renders partially or fully off-screen (confirmed by source read: zero clamping against `rect.left`/`point.x`/`point.y`, `.tooltip` is centered via `transform:translate(-50%,-130%)`)

**coord-validator**
- OBJ-coord-validator-1 | `vintage` field on all 13 `industrial.geojson` features — reaffirmed, unchanged
- OBJ-coord-validator-2 | Sample-verify `national-hv.geojson` + `transmission-lines.geojson` — reaffirmed; **REPORT executed this sitting, see Ruling**
- OBJ-coord-validator-3 | Remove/mark deprecated `grid-lines.geojson` — reaffirmed, unchanged

**map-debugger**
- OBJ-map-debugger-1 | Fix dead mailto targets — reaffirmed; **ruled SHIP this sitting**
- OBJ-map-debugger-4 | Light-theme topbar button contrast (`docs/brand.css`) — reaffirmed, unchanged
- OBJ-map-debugger-3 | Visible error state for mid-session MapLibre runtime failures — reaffirmed, unchanged
- *(OBJ-map-debugger-2, wiring `national-hv`/`transmission-lines` into the map, stays on the Board — not filed this sitting, still the seat's largest/lowest-ranked item; see Ruling for how this sitting's REPORT affects it)*

**map-tester**
- OBJ-map-tester-1 | Orphan-data check — reaffirmed, unchanged
- OBJ-map-tester-2 | Written evidence bar for a release-gate GO — reaffirmed, unchanged
- OBJ-map-tester-3 | Popup field-name mapping audit — reaffirmed, unchanged

**platform-engineer**
- OBJ-platform-engineer-1 | Minimal CI check on push to `main` — reaffirmed; **REPORT executed this sitting, see Ruling**
- OBJ-platform-engineer-2 | Uptime check against the live map URL — reaffirmed, unchanged
- OBJ-platform-engineer-3 | `docs/_headers` cache-control for `docs/data/*.geojson` — reaffirmed, unchanged

**security-engineer**
- OBJ-security-engineer-1 | Subresource Integrity hashes on MapLibre tags — reaffirmed, unchanged
- OBJ-security-engineer-2 | Disclose third-party requests on load — reaffirmed, unchanged
- OBJ-security-engineer-3 | Content-Security-Policy via `docs/_headers` — reaffirmed, unchanged
- Secrets scan of `docs/**` diff and recent history: clean, no matches

### Ruling

SHIP:    OBJ-map-debugger-1 — no rung-1 or rung-2 candidate exists this
         sitting, so the docket falls to the next-ranked rung-3/4 item, as
         it was already carried from the 2026-08-04 minutes. It is S,
         zero schema/data risk, a pure string substitution (the dead
         `reda.tahiri@example.com` placeholder — IANA-reserved, RFC 2606,
         non-deliverable — replaced with the real, monitored
         `[principal’s personal address — redacted]`, confirmed by the site owner) in the
         three "Report an error" mailto links across `docs/index.html`
         and `docs/app.js`. Every correction a regulator or DC developer
         tries to report currently vanishes silently; this SHIP lets that
         report actually land. Dispatched to `map-debugger` (executing
         seat) for implementation and gates, then `map-tester` for the
         Release gate — both cleared, see Outcome.

REPORT:  OBJ-coord-validator-2, OBJ-platform-engineer-1 — both executed
         in full this sitting (read-only, no edits), same two items
         carried from 2026-08-04 as the only M-sized board entries
         blocking a future S.

         **coord-validator-2** — full findings in the report; summary:
         zero FAILs across all 1,488 features (947 `national-hv` + 541
         `transmission-lines`). Full-population bbox/geometry sweep found
         6 duplicate-vertex hygiene WARNs (location itself unaffected).
         Full-population cross-source matching found 536 of
         `national-hv`'s 150/225/400 kV lines and 536 of
         `transmission-lines`'s "existing" lines are the *same* real-world
         corridors cited under two source labels (~300 m median
         reprojection offset, not independent corroboration) — so the
         board's "1,488 curated features" framing overstates independent
         coverage; true distinct-corridor count is closer to 952. A
         21-feature stratified geographic-plausibility sample (all
         endpoints within 100 km of a real Moroccan population center)
         found nothing new. Net: 1,066 features PASS, 6 WARN (hygiene
         only), 416 UNVERIFIED (411 ONEE-only 60 kV lines with no
         independent source + 5 WBG-only planned lines with no ground
         truth yet — OSM/Nominatim/Overpass egress is blocked from this
         environment, so the fallback external check could not run), 0
         FAIL. This clears the data-quality blocker on OBJ-map-debugger-2:
         nothing here forbids wiring the 1,072 cross-corroborated
         150/225/400 kV features in as a first slice; the 60 kV-only and
         planned features should stay excluded or clearly marked
         unverified until OSM egress is available or another source is
         found. Board updated accordingly.

         **platform-engineer-1** — full spec in the report; summary: no
         `.github/` directory exists yet. Root-caused the actual incident
         this objective exists to prevent (conflict markers merged into
         `main`, breaking `docs/app.js`'s syntax, masked only by stale
         CDN cache) and scoped a 4-step, pure-validation GitHub Action —
         conflict-marker grep scoped to code/data extensions, `node
         --check` on every top-level `.js` file, `jq empty` on every
         `docs/data/**/*.geojson`, and a referenced-file-existence check
         against `docs/countries.config.js`/`docs/index.html` — with no
         `package.json`, no `npm install`, no `setup-node`, path-filtered
         to `docs/**` so it never fires on a solar-only commit. Concrete
         enough for direct implementation; the Board's size for this item
         is updated M→S on the strength of that bound.

Vetoed:  none — no seat proposed anything that struck a structural or
         truth veto this sitting.

Docketed: OBJ-frontend-engineer-2, OBJ-frontend-engineer-3,
OBJ-frontend-engineer-4 (new — minted this sitting), OBJ-coord-validator-1,
OBJ-coord-validator-2, OBJ-coord-validator-3, OBJ-map-debugger-2,
OBJ-map-debugger-3, OBJ-map-debugger-4, OBJ-map-tester-1, OBJ-map-tester-2,
OBJ-map-tester-3, OBJ-platform-engineer-1, OBJ-platform-engineer-2,
OBJ-platform-engineer-3, OBJ-security-engineer-1, OBJ-security-engineer-2,
OBJ-security-engineer-3.

ID minted this sitting: OBJ-frontend-engineer-4 (ledger: frontend-engineer
4 → 5). No other ledger changed.

### Outcome
Commit:  `a61eb9d` — "map-debugger: fix dead correction-report mailto
         targets". Code fix and this council record are two separate
         commits on `council/2026-08-12-0303`, not pushed to `main`
         directly.
Gates:   desktop ✓ (Playwright/Chromium, 0 `pageErrors`, correct
         `mailto:[principal’s personal address — redacted]…` href confirmed live, no
         horizontal overflow) | light ✓ (same probe under
         `[data-theme="light"]`, identical evidence) | 375px ✓ (0
         `pageErrors`, correct href, `documentElement.scrollWidth ===
         clientWidth === 375`; one pre-existing `body`-level overflow
         condition found and confirmed via `git stash` to be
         byte-identical before/after this diff — not a regression, already
         tracked as OBJ-frontend-engineer-3, not blocking) | data n/a (no
         `docs/data/**` touched) | secrets n/a (string-only diff, no
         dependency/secret surface touched; security-engineer's sweep of
         `docs/**` this sitting was independently clean) | map-tester
         **GO** — quoted verdict: "all 3 mailto targets... verified
         switched to `[principal’s personal address — redacted]` with zero remaining
         placeholder references, zero console errors... and zero
         uncaught `pageErrors` across desktop/light/375px... diff scope is
         a pure single-purpose S... Nothing to hand back to map-debugger."
         Known environment limitation disclosed by both `map-debugger` and
         `map-tester` independently, not caused by this change: this
         sandbox's outbound proxy blocks `unpkg.com` (403), so MapLibre GL
         itself never loads here — pre-existing, identical before and
         after the diff, unrelated to the shipped fix.
Carried: OBJ-map-debugger-1 is now Shipped — see `council/BOARD.md`.
         OBJ-map-debugger-4 (light-theme contrast) is next-ranked for the
         SHIP slot once today's budget resets. OBJ-map-debugger-2 (L,
         wiring `national-hv`/`transmission-lines` in) is no longer
         blocked on data quality (coord-validator-2 PASS, 0 FAILs) — a
         future sitting should split it, with the 1,072 cross-corroborated
         150/225/400 kV features as the safe first S-sized slice, holding
         back the 416 UNVERIFIED 60 kV/planned features until an
         independent source is reachable. OBJ-platform-engineer-1 is
         reclassified S given its now-concrete spec and is a live SHIP
         candidate for a future sitting. New this sitting:
         OBJ-frontend-engineer-4 (tooltip viewport clamping), filed to the
         Board, not yet ranked against the rest of the docket. Next
         sitting is REPORT-only regardless of ranking — one SHIP per
         rolling 24 h, and this one already landed today.

## Sitting — 07:57 (Europe/Paris)

First sitting in over a week — the prior recorded sitting was 2026-08-04
14:15. `git log` tip had moved from `71b900f` to `9a4d5a4` only via the
already-recorded merge of `c808b1e` (`OBJ-frontend-engineer-1`, shipped
2026-08-04) plus the minutes commit `4100942` — nothing new landed under
`docs/**` in the gap. No `council/2026-08-12.md` existed before this
sitting, so today's SHIP budget (max 1 per rolling 24h) was unspent
regardless of the gap.

### State established before convening
- `git log` tip before this sitting: `9a4d5a4` (merge PR #1). Working tree
  clean. No incident, no broken pipeline.
- `node --check docs/app.js` and `docs/countries.config.js` both clean;
  all `docs/data/morocco/*.geojson` parse as valid JSON. Nothing is down,
  nothing is broken — no rung-1 (published fact wrong) or rung-2 (site
  down / pipeline broken) candidate exists this sitting.
- Live-URL check from this environment returned a proxy-level
  `connect_rejected`/403 — confirmed this is an outbound network policy
  artifact of the sandbox, not evidence the live GitHub Pages/Cloudflare
  Pages site is actually down; not treated as a rung-2 finding.

### Docket
**frontend-engineer** (Board honestly carries 2, not 3 — no third genuinely
distinct item found this sitting)
- OBJ-frontend-engineer-2 | Add a visible loading state to `#layerList`/`#kpiGrid` while `loadAllData()` awaits its fetches — reaffirmed, unchanged (S)
- OBJ-frontend-engineer-3 | Give `.topbar` explicit overflow handling at ≤375px — reaffirmed, unchanged (S)

**coord-validator**
- OBJ-coord-validator-1 | Add `vintage` field to all 13 `industrial.geojson` features — reaffirmed, unchanged (S)
- OBJ-coord-validator-2 | Sample-verify `national-hv.geojson` (947) + `transmission-lines.geojson` (541) — reaffirmed, unchanged (M)
- OBJ-coord-validator-3 | Remove or mark deprecated `grid-lines.geojson` (11 features, unreferenced) — reaffirmed, unchanged (S)

**map-debugger**
- OBJ-map-debugger-1 | Fix dead mailto targets (`index.html:122`, `app.js:968,997`, still `example.com`) — reaffirmed, re-verified fresh (S) — **SHIPPED this sitting**
- OBJ-map-debugger-4 | Fix light-theme topbar button contrast in `docs/brand.css` — reaffirmed, unchanged (S)
- OBJ-map-debugger-3 | Surface a visible error state for mid-session MapLibre runtime failures — reaffirmed, unchanged (S)
- (not filed — out of top-3, unchanged: OBJ-map-debugger-2, L, still blocked on OBJ-coord-validator-2)

**map-tester**
- OBJ-map-tester-1 | Orphan-data check across `docs/data/morocco/*.geojson` vs load-path references — reaffirmed, unchanged (S)
- OBJ-map-tester-2 | Write down the required evidence bar for a release-gate GO — reaffirmed, unchanged (S)
- OBJ-map-tester-3 | Audit popup field-name mapping against each source file's actual keys — reaffirmed, unchanged (S)

**platform-engineer**
- OBJ-platform-engineer-1 | Add a minimal CI check on push to `main` (no build step, no bundler) — reaffirmed, unchanged (M)
- OBJ-platform-engineer-2 | Wire an uptime check against the live map URL — reaffirmed, unchanged (S)
- OBJ-platform-engineer-3 | Add `docs/_headers` with cache-control for `docs/data/*.geojson` — reaffirmed, unchanged (S)

**security-engineer**
- OBJ-security-engineer-1 | Add Subresource Integrity hashes to the MapLibre `<script>`/`<link>` tags — reaffirmed, unchanged (S)
- OBJ-security-engineer-2 | Disclose the third-party requests the page makes on load — reaffirmed, unchanged (S)
- OBJ-security-engineer-3 | Add a Content-Security-Policy via `docs/_headers` — reaffirmed, unchanged (S, risk: medium — an overly strict CSP could break the map)

### Ruling

SHIP:    OBJ-map-debugger-1 — no rung-1 or rung-2 candidate is live this
         sitting, so the docket ranks on rung-3/4. `OBJ-coord-validator-1`
         (vintage field on `industrial.geojson`) is the constitution's own
         §6 worked example and was seriously considered, but on inspection
         every one of the 13 features cites only a company homepage as
         `source_url` (OCP, Holcim, SONASID, Renault, Stellantis, etc.),
         not a dated report — there is no in-repo basis to derive a
         truthful per-feature `vintage` without new external research per
         company. Filling it in anyway (e.g. reusing the file's
         `compiled: 2026-04-17` metadata date as a stand-in) would imply
         more currency than the underlying estimates actually have — a
         Truth-veto risk (COUNCIL.md §5: "if it cannot be sourced, the
         page says the data does not exist"), not something to fabricate
         unattended. `OBJ-map-debugger-1` carries no such risk: the fix is
         a pure string swap to a real, already-known address
         (`[principal’s personal address — redacted]`), was already ranked "next for the SHIP
         slot" by the 2026-08-04 sitting, and directly serves the
         correction pathway the North Star depends on — a regulator or DC
         developer who spots a wrong coordinate or stale figure can now
         actually get the correction delivered, instead of every click
         going to a non-deliverable `example.com` placeholder. S, zero
         risk, no schema/structural veto triggered, reversible.
REPORT:  OBJ-coord-validator-2, OBJ-platform-engineer-1 — reaffirmed for
         the same reason as the 2026-08-04 sitting: both are the only
         M-sized items on the Board and both gate a future S.
         `coord-validator-2` (sample-verifying 1,488 unchecked ONEE/WBG
         line features) must resolve before `OBJ-map-debugger-2` (L,
         wiring those features into the map) can be split into anything
         shippable. `platform-engineer-1` (CI check) needs its scope
         pinned to a pure-validation checklist before it can split into
         an S without tripping the build-step/bundler structural veto.
         Neither has been executed yet — reports only, no edits.
Vetoed:  none — no seat vetoed anything this sitting; every reaffirmed
         objective still sits inside the standing structural/truth vetoes
         and nothing changed to require relitigating any of them.
Docketed: OBJ-frontend-engineer-2, OBJ-frontend-engineer-3,
OBJ-coord-validator-1, OBJ-coord-validator-2, OBJ-coord-validator-3,
OBJ-map-debugger-4, OBJ-map-debugger-3, OBJ-map-debugger-2,
OBJ-map-tester-1, OBJ-map-tester-2, OBJ-map-tester-3,
OBJ-platform-engineer-1, OBJ-platform-engineer-2, OBJ-platform-engineer-3,
OBJ-security-engineer-1, OBJ-security-engineer-2, OBJ-security-engineer-3

(No IDs minted this sitting — every seat reaffirmed existing Board items;
none proposed a genuinely new observation. Ledger unchanged from
2026-08-04: frontend-engineer 4, coord-validator 4, map-debugger 5,
map-tester 4, platform-engineer 4, security-engineer 4.)

### Outcome
Commit:  `4cac17b` — "map-debugger: point 'Report an error' mailto at a
         real address". `docs/index.html:122`, `docs/app.js:968`,
         `docs/app.js:997` — `reda.tahiri@example.com` →
         `[principal’s personal address — redacted]`, string-only, no other lines touched.
         Re-grepped after the edit: zero remaining `example.com`
         references anywhere under `docs/`.
Gates:   desktop ✓ | light ✓ | 375px ✓ — real headless-Chromium evidence
         via Playwright against a local static server (`localhost:8123`,
         serving `docs/` unmodified): at 1440×900 dark, 1440×900 with
         `data-theme="light"`, and 375×800, `document.querySelectorAll('a[href^="mailto:"]')`
         on the footer link returns exactly
         `mailto:[principal’s personal address — redacted]?subject=MoroccoMap%20data%20correction`
         in all three states; `scrollWidth === clientWidth` at every
         width (no horizontal overflow); screenshots captured for all
         three. Disclosed evidence gap: this sandbox's outbound network
         policy blocks the CDN origins the live page depends on (unpkg
         MapLibre JS/CSS, Google Fonts, CARTO/OSM tiles — confirmed via
         `ERR_TUNNEL_CONNECTION_FAILED`/`ERR_CONNECTION_RESET` in the
         captured console log), so MapLibre never initialized and the two
         popup-triggered mailto links (`app.js:968,997`, reached only by
         clicking a rendered map feature) could not be exercised live
         end-to-end. Those two are covered instead by direct source
         inspection: `git diff` shows both are the byte-identical
         string-substitution pattern as the footer link that *did* render
         correctly, with `subject=`/`body=` `encodeURIComponent(...)`
         wrapping unchanged around the new address. This gap is an
         artifact of this sandbox's network policy, not of the change or
         of the live deployed site (which does have unrestricted CDN
         access). | data n/a (no `docs/data/**` touched) | secrets ✓ —
         security-engineer PASS: diff is exactly 3 lines, all
         `example.com`→`gmail.com` string swaps, zero credential-pattern
         matches, no new third-party data flow | map-tester GO — quoted:
         "GO — string-literal-only mailto swap verified via git diff,
         zero example.com residue, JS parses clean, no code path exists
         for this change to affect rendering/layout" (map-tester's initial
         pass reasoned this class of change didn't need live browser
         evidence; the Chair judged that reasoning alone was a
         `GO-STATIC`-shaped gap per COUNCIL.md §5 and independently
         obtained the real headless-browser evidence above before
         treating the release gate as cleared).
Carried: `OBJ-map-debugger-1` is now Shipped — see `council/BOARD.md`.
         `OBJ-coord-validator-1` stays Docketed, not vetoed — it remains a
         strong, constitution-example-matching S candidate, but any future
         sitting that wants to ship it must first do real per-feature
         source research (WebSearch a dated report/filing per company) to
         populate a truthful `vintage`, not infer one from the file's
         `compiled` metadata date. `OBJ-map-debugger-2` (L) stays blocked
         on `OBJ-coord-validator-2`'s report; `OBJ-platform-engineer-1`
         (M) stays blocked on its own scoping report. Next sitting is
         REPORT-only regardless of ranking — one SHIP per rolling 24h, and
         this one already landed today.

## Sitting — 13:12 (Europe/Paris)

Third sitting on record; first of the day, so today's SHIP budget (max 1 per
rolling 24h) was unspent going in — confirmed independently: no
`council/2026-08-12.md` existed before this one, and `council/BOARD.md`'s
Shipped table's only row is dated 2026-08-04. `git log` tip is `9a4d5a4`
(merge of PR #1); `git diff 4100942 9a4d5a4 --stat` is empty — nothing under
`docs/**` has changed since the last sitting. Every seat's Board section was
pulled and reaffirmed as-is; no genuinely new observation surfaced in any of
the six domains at ruling time.

### State established before convening
- `node --check docs/app.js`/`docs/countries.config.js` clean; all
  `docs/data/morocco/*.geojson` valid JSON. No rung-1/2 candidate.
- Independently re-verified every standing Board claim rather than trusting
  it stale: mailto still `example.com` (3 sites); `brand.css` still has no
  light-theme override for `.topbar .ghost-btn`/`.icon-btn`; `national-hv`/
  `transmission-lines`/`grid-lines` still absent from `countries.config.js`'s
  load list; `industrial.geojson` still had zero `vintage` occurrences; no
  `docs/_headers`, no `.github/workflows/`, no SRI, no CSP.

### Docket (reaffirmed, unchanged from council/BOARD.md — no new proposals at ruling time)

**frontend-engineer**
- OBJ-frontend-engineer-2 | Loading state for `#layerList`/`#kpiGrid` — reaffirmed
- OBJ-frontend-engineer-3 | `.topbar` overflow at ≤375px — reaffirmed

**coord-validator**
- OBJ-coord-validator-1 | `vintage` field on all 13 `industrial.geojson` features — reaffirmed
- OBJ-coord-validator-2 | Sample-verify `national-hv`+`transmission-lines` (M) — reaffirmed
- OBJ-coord-validator-3 | Remove/mark deprecated `grid-lines.geojson` — reaffirmed

**map-debugger**
- OBJ-map-debugger-1 | Fix dead mailto targets — reaffirmed
- OBJ-map-debugger-2 | Wire `national-hv`+`transmission-lines` into the map (L, blocked on CV-2) — reaffirmed
- OBJ-map-debugger-4 | Light-theme topbar contrast fix — reaffirmed
- OBJ-map-debugger-3 | Visible error state for mid-session MapLibre failures — reaffirmed

**map-tester**
- OBJ-map-tester-1 | Orphan-data check — reaffirmed
- OBJ-map-tester-2 | Written GO evidence bar — reaffirmed (re-ranked to top during execution, see Outcome)
- OBJ-map-tester-3 | Popup field-name audit — reaffirmed

**platform-engineer**
- OBJ-platform-engineer-1 | Minimal CI check (M) — reaffirmed
- OBJ-platform-engineer-2 | Uptime check — reaffirmed
- OBJ-platform-engineer-3 | `docs/_headers` cache-control — reaffirmed

**security-engineer**
- OBJ-security-engineer-1 | SRI hashes on MapLibre tags — reaffirmed
- OBJ-security-engineer-2 | Disclose third-party requests — reaffirmed
- OBJ-security-engineer-3 | CSP via `docs/_headers` — reaffirmed

### Ruling

SHIP:    OBJ-coord-validator-1 — a regulator citing OCP's, Renault's, or
         another site's estimated demand can state which year that estimate
         is from, instead of it being presented as implicitly current.
         Re-ranked above the previously-flagged OBJ-map-debugger-1 (dead
         mailto): CV-1 sits cleanly on rung 3 of the precedence ladder and
         mirrors COUNCIL.md §6's own worked example almost exactly, while
         the mailto fix's `unlocks:` does not clearly name a decision the
         audience gains. S, additive only, does not touch the protected
         schema fields.
REPORT:  OBJ-coord-validator-2, OBJ-platform-engineer-1 — reaffirmed,
         unexecuted; both remain the only M items on the Board and both
         still gate a downstream S-split. Not executed this sitting either
         — the SHIP attempt consumed the sitting's capacity.
Vetoed:  none.
Docketed: OBJ-frontend-engineer-2, OBJ-frontend-engineer-3,
OBJ-coord-validator-2, OBJ-coord-validator-3, OBJ-map-debugger-1,
OBJ-map-debugger-2, OBJ-map-debugger-3, OBJ-map-debugger-4,
OBJ-map-tester-1, OBJ-map-tester-2, OBJ-map-tester-3,
OBJ-platform-engineer-1, OBJ-platform-engineer-2, OBJ-platform-engineer-3,
OBJ-security-engineer-1, OBJ-security-engineer-2, OBJ-security-engineer-3

### Outcome

**Commit: none — gate failed.**

OBJ-coord-validator-1 was executed: `docs/data/morocco/industrial.geojson`
was given a `vintage` property on all 13 features, and
`docs/app.js`'s `openPointPopup()` source-row was wired to surface it
conditionally (`p.vintage ? " · vintage: " + p.vintage : ""`, so every
other layer's popup is byte-identical to before). No fabrication: none of
the 13 sources in `docs/data/morocco/industrial.geojson` or
`DATA_SOURCES.md` cite a specific per-feature year anywhere in the repo,
and live web verification is unavailable in this execution sandbox (see
below), so 12 features carry `"vintage": "unknown"` and one
(`samir-mohammedia`) carries `"unknown (refinery idled since 2015)"` — the
one bound that is independently corroborable from data already in the file
(`"status": "idle"`), not a guessed report year. Per COUNCIL.md's own
framing, "the page says the data does not exist" is the honest outcome
here, not a shortfall.

Gates:
- **Data (coord-validator): PASS, 0 FAILs.** Confirmed the diff touches
  only the new `vintage` keys (verified via `git diff`); confirmed
  `vintage` present and non-fabricated on all 13 features. Coordinate
  re-verification against Nominatim could not be completed — outbound
  requests to `nominatim.openstreetmap.org` returned `403`/`EGRESS_BLOCKED`
  from this sandbox's proxy on all 13 attempts — so all 13 coordinate
  checks are recorded UNVERIFIED (bbox-consistent, byte-identical to the
  pre-diff, already-shipped state) rather than freshly reconfirmed PASS.
  This did not block the gate: the "zero FAILs" veto text is about FAILs,
  not UNVERIFIEDs, and the coordinates were provably untouched by this
  diff.
- **Release (map-tester): GO-STATIC — not GO. Gate did not clear.**
  map-tester confirmed `node --check` clean, JSON valid, diff scope
  correct, and — going beyond source-reading — stood up a local Chromium
  DOM harness (real `app.js`/`index.html`/`style.css`, real
  `industrial.geojson`, only the unreachable CDN calls stubbed) and
  observed the exact expected popup output with zero console errors across
  desktop/light/375px, plus a clean regression check on a power-plant
  popup (no `vintage:` leakage into layers that don't have the field).
  But COUNCIL.md §5 requires real browser evidence against the **live**
  page and CDNs, and this sandbox cannot reach any of them: `WebFetch` on
  `redatahiri37.github.io` returns `EGRESS_BLOCKED`; direct `curl` to
  `unpkg.com`, `a.basemaps.cartocdn.com`, and `openinframap.org` all return
  `403` `CONNECT tunnel failed` from the environment's own agent proxy.
  This is a structural sandbox limitation, not a defect in the change or a
  skipped step — GO-STATIC is explicitly "not shippable" per COUNCIL.md
  §5, so per the standing Truth veto ("Announcing 'shipped' without a
  map-tester GO"), this cannot be committed as shipped.
- Secrets/Deploy: not reached — release gate failure stops the pipeline
  before those gates matter.

**Because the release gate did not clear, the working tree was reverted**
(`git checkout -- docs/app.js docs/data/morocco/industrial.geojson`) and no
product-code commit is included on this branch. Only these minutes and the
Board update are committed.

Carried: OBJ-coord-validator-1 stays **In Progress** on the Board — the
data-side work is fully specified and gate-verified (0 FAILs), and the
exact `app.js` edit is documented above; only live-browser confirmation is
missing. The next sitting (or a human) can re-apply the same two edits and
close it out with a real GO the moment live-browser evidence is available
— either a human pasting DevTools console output per map-tester's 6-step
manual checklist, or a future run with working network egress. Two new
Board items surfaced during execution (not part of the original docket,
filed by map-tester while producing the release-gate report):
**OBJ-map-tester-4** — document that this sandbox cannot reach the live
URL or required CDNs and define the accepted GO fallback path, so future
sittings don't rediscover this from zero; and a re-ranking of
**OBJ-map-tester-2** (written GO evidence bar) to the top of map-tester's
docket, since this sitting is the concrete case it exists to resolve.
**Today's SHIP budget remains unspent** — no commit landed, so a later
sitting today may still attempt a SHIP, subject to the same release-gate
constraint applying to any candidate that needs browser evidence
(which is effectively all of them under the current gate rules).

## Sitting — 18:00 (Europe/Paris)

First sitting today. `council/2026-08-12.md` did not exist before this
sitting, so today's SHIP budget (max 1 per rolling 24h) was unspent.
**Anomaly noted, not corrected by ruling:** the previous sitting was
2026-08-04 14:15 (`council/2026-08-04.md`) — an ~8-day gap with zero
recorded sittings against the nominal 5-hourly cadence. This is a
`platform-engineer`-shaped "what has no alarm" concern (the sitting
cadence itself had none), but the Chair files no objectives of its own;
flagged here for the record only.

### State established before convening
- `git log` tip: `9a4d5a4` (merge of `c808b1e`, the OBJ-frontend-engineer-1
  SHIP from 2026-08-04 14:15). Nothing landed on `main` since. Working tree
  clean, `main` up to date with origin before this sitting began.
- Rung-1/rung-2 sweep: `node --check` on `docs/app.js` and
  `docs/countries.config.js` clean; all 9 `docs/data/morocco/*.geojson`
  files parse as valid JSON. No incident, nothing down.
- Every reaffirmed Board item was independently re-verified against source
  this sitting, not taken on trust from the Board's prose: dead
  `mailto:reda.tahiri@example.com` still present at `docs/index.html:122`,
  `docs/app.js:968,997`; `docs/brand.css` still has zero
  `[data-theme="light"]` override for `.topbar .ghost-btn`, still
  white-on-white; `industrial.geojson` still has zero `vintage`
  occurrences; no `.github/workflows/` and no `docs/_headers` exist.
- **New finding this sitting**, from map-debugger reading `docs/app.js`
  end-to-end rather than only the Board's prior summary:
  `buildLineLayer(dataLayerId, fc)` is called once for `"interconnectors"`
  and once for `"planned-corridors"` (`app.js:488-494`), and **both calls
  hardcoded `const srcId = "src-grid"`**. `addOrReplace()` removed and
  recreated that shared source on every call, so the second call
  (`planned-corridors`) silently overwrote the first's
  (`interconnectors`') data before any frame ever painted —
  `docs/data/morocco/interconnectors.geojson`'s 3 features never rendered,
  under any toggle state, ever. Worse, `layersFor()` returned the
  *identical* 5-layer-id set for both `dataLayerId`s, so the two
  separately-titled, separately-cited (REE/ONEE vs Xlinks/WBG) sidebar
  checkboxes drove the exact same underlying MapLibre layers. Live in
  production at the time of the sitting.

### Docket

**frontend-engineer**
- OBJ-frontend-engineer-2 | Add a visible loading state to `#layerList`/`#kpiGrid` while `loadAllData()` awaits its fetches — reaffirmed, unchanged
- OBJ-frontend-engineer-3 | Give `.topbar` explicit overflow handling at ≤375px — reaffirmed, unchanged
- OBJ-frontend-engineer-4 | Add Escape-to-close and focus handling to the Methodology modal (`role="dialog" aria-modal="true"`) — new; confirmed zero `keydown`/`Escape` listeners anywhere in `docs/app.js`; modal only closes via click on `#methodologyClose` or click-outside

**coord-validator**
- OBJ-coord-validator-1 | Add `vintage` field to all 13 `industrial.geojson` features — reaffirmed, unchanged
- OBJ-coord-validator-2 | Sample-verify `national-hv.geojson` (947) + `transmission-lines.geojson` (541) — reaffirmed, unchanged
- OBJ-coord-validator-3 | Remove or mark deprecated `grid-lines.geojson` (11 features, unreferenced) — reaffirmed, unchanged

**map-debugger**
- OBJ-map-debugger-5 | Give "Interconnectors" and "Planned corridors" independent MapLibre sources/layer ids in `docs/app.js` — new; both `buildLineLayer()` calls hardcoded `srcId="src-grid"` and `layersFor()` returned identical layer-id sets for both `dataLayerId`s, so `interconnectors.geojson`'s data was silently overwritten by `planned-corridors.geojson` before any paint, and the two sidebar checkboxes drove the same underlying layers
- OBJ-map-debugger-1 | Fix dead mailto targets (`index.html:122`, `app.js:968,997`, still `example.com`) — reaffirmed, re-verified still present
- OBJ-map-debugger-4 | Fix light-theme topbar button contrast in `docs/brand.css` — reaffirmed, re-verified still white-on-white

**map-tester**
- OBJ-map-tester-1 | Orphan-data check across `docs/data/morocco/*.geojson` vs load-path references — reaffirmed, unchanged
- OBJ-map-tester-2 | Write down the required evidence bar for a release-gate GO — reaffirmed, unchanged
- OBJ-map-tester-3 | Audit popup field-name mapping against each source file's actual keys — reaffirmed, unchanged

**platform-engineer**
- OBJ-platform-engineer-1 | Add a minimal CI check on push to `main` — reaffirmed, unchanged (still no `.github/workflows/`)
- OBJ-platform-engineer-2 | Wire an uptime check against the live map URL — reaffirmed, unchanged
- OBJ-platform-engineer-3 | Add `docs/_headers` with cache-control for `docs/data/*.geojson` — reaffirmed, unchanged (still no `docs/_headers`)

**security-engineer**
- OBJ-security-engineer-1 | Add Subresource Integrity hashes to the MapLibre `<script>`/`<link>` tags — reaffirmed, unchanged
- OBJ-security-engineer-2 | Disclose third-party requests made on load — reaffirmed, unchanged
- OBJ-security-engineer-3 | Add a Content-Security-Policy via `docs/_headers` — reaffirmed, unchanged

### Ruling
SHIP:    OBJ-map-debugger-5 — a DC developer assessing cross-border
         interconnection capacity can now actually see which
         interconnector lines are operational (ES-MA I/II) versus idle
         (DZ-MA), and toggle that layer independently of planned
         corridors, instead of the sidebar citing REE/ONEE-sourced
         "Interconnectors" data that had never rendered a single one of
         its own features. Root cause verified fresh by reading
         `docs/app.js` directly this sitting: `buildLineLayer()` is
         called once for `"interconnectors"` and once for
         `"planned-corridors"` (`app.js:488-494`), both hardcoding
         `srcId="src-grid"`; `addOrReplace()` removed and recreated that
         source on every call, so the second call (planned-corridors)
         silently overwrote the first before any paint. `layersFor()`
         additionally returned the identical 5-id set for both
         `dataLayerId`s, so the two separately-titled, separately-cited
         sidebar checkboxes drove the same underlying MapLibre layers.
         This is rung 3 on the precedence ladder ("audience cannot make
         a decision it should be able to make — missing capability"),
         which deterministically outranks the two rung-4 candidates also
         on the docket this sitting (OBJ-map-debugger-1 dead mailto,
         OBJ-map-debugger-4 light-theme contrast — both "cannot
         read/use what's already there"). No rung-1 or rung-2 candidate
         existed this sitting (JS syntax clean, all 9 GeoJSON files
         valid, nothing down), so nothing pre-empted. S-sized: one file,
         one rendering concern, reversible; touched no `docs/data/**`, no
         schema field, no new dependency — cleared every structural and
         truth veto. Today's SHIP budget was unspent. Handed to
         frontend-engineer for execution (the only seat owning any part
         of `docs/app.js` per COUNCIL.md §3).

REPORT:  OBJ-coord-validator-2, OBJ-platform-engineer-1 — reaffirmed,
         unchanged rationale from 2026-08-04: the only two M-sized items
         on the Board, both still unexecuted (no CI workflow exists, no
         sample-verification report exists), and both gate a future S
         (OBJ-map-debugger-2, L, cannot split until coord-validator-2
         resolves; OBJ-platform-engineer-1 needs its scope pinned to
         pure validation before it can become an S without risking the
         build-step/bundler structural veto). Not executed this sitting
         (unchanged from the 2026-08-04 precedent) — the SHIP consumed
         this sitting's execution capacity.

Vetoed:  none — nothing proposed this sitting collides with a
         structural or truth veto; no new dependency, no schema
         mutation, no unsourced figure, no multi-section sweep.

Docketed: OBJ-frontend-engineer-2, OBJ-frontend-engineer-3,
OBJ-frontend-engineer-4, OBJ-coord-validator-1, OBJ-coord-validator-2,
OBJ-coord-validator-3, OBJ-map-debugger-1, OBJ-map-debugger-2,
OBJ-map-debugger-3, OBJ-map-debugger-4, OBJ-map-tester-1,
OBJ-map-tester-2, OBJ-map-tester-3, OBJ-platform-engineer-1,
OBJ-platform-engineer-2, OBJ-platform-engineer-3,
OBJ-security-engineer-1, OBJ-security-engineer-2,
OBJ-security-engineer-3.

Minted this sitting: OBJ-map-debugger-5 (map-debugger ledger 5→6),
OBJ-frontend-engineer-4 (frontend-engineer ledger 4→5). All other
ledgers unchanged.

### Outcome
Commit:  see PR — `docs/app.js` change to split interconnector/planned-
         corridor MapLibre source and layer identity.
Gates:   desktop ✓ (`map.getStyle().sources` shows two independent
         sources — `src-grid-interconnectors` with 3 features,
         `src-grid-planned` with 8 — each backing its own 5 layer ids;
         toggling one sidebar checkbox off drove only its own
         `lyr-gridint-*`/`lyr-gridplan-*` set to `"none"` while the other
         stayed `"visible"`, in both directions; click/hover spot-checked
         on both layers, correct distinct popup content per layer;
         console clean of app-level errors) | light ✓ (identical
         independent-toggle result under `[data-theme="light"]`, console
         clean) | 375px ✓ (`scrollWidth === clientWidth === 375`, zero
         horizontal overflow; both layer rows reachable/tappable at
         their real bounding boxes; toggle independence held) | data n/a
         (no `docs/data/**` touched) | secrets ✓ — security-engineer
         verdict: "CLEAN diff-scan / CLEAN scope / CLEAN network-surface
         / CLEAN content ... SAFE-TO-PUSH — secrets gate PASSES" | 
         map-tester **GO** — quoted verdict: "Independent-source/
         independent-toggle behavior verified with quoted browser-level
         DOM/API evidence (visibility states, feature counts, popup
         bodies) across desktop, light mode, and 375px, console clean of
         app-level errors in all three, and the diff is scope-correct
         (LAYER_KIND untouched, no stray old ids)." One disclosed gap:
         map-tester's sandbox proxy blocks the live CDN/tile hosts, so
         real basemap-tile rendering on the actual live domain was not
         directly observed by either gating agent — closed by source
         audit and prior-sitting precedent that basemap loading is
         unrelated to this diff's scope (line-layer source/layer wiring
         only), and left as a manual follow-up item in map-tester's
         report rather than blocking the GO.
Carried: OBJ-map-debugger-5 is now Shipped — see `council/BOARD.md`.
         OBJ-map-debugger-1 (dead mailto) and OBJ-map-debugger-4
         (light-theme contrast) remain next-ranked for the SHIP slot
         once today's budget resets. OBJ-coord-validator-2 and
         OBJ-platform-engineer-1 REPORTs remain unexecuted; the same
         blocking relationships to OBJ-map-debugger-2 (L) and a future
         CI-check S persist. Next sitting today is REPORT-only
         regardless of ranking — one SHIP per rolling 24h, and this one
         already claimed the slot. Separately noted, not ruled on: the
         unexplained ~8-day gap between the 2026-08-04 14:15 sitting and
         this one, against a nominal 5-hourly cadence — if this recurs,
         a seat should raise it through the normal docket next time it's
         observed.

## Sitting — 21:04 (Europe/Paris)

**Reconstruction note.** Before convening, I fetched all council branches
(none merge to `main` past PR #1, so remote branches are the only place
state actually lives) and read forward from the most recent one,
`council/2026-08-12-1800`, per COUNCIL.md's "read the most recent file in
council/" instruction. That surfaced something the 18:00 sitting's own
Board couldn't see: **today had four independent sittings (03:03, 07:57,
13:12, 18:00), each branched from the same never-advancing `main`, each
genuinely believing it was the day's first.** Consequences, verified
directly against each branch's actual commits and Board state:

- **The SHIP budget (1 per rolling 24h) was exceeded at least 2x today**,
  purely because no chair could see a same-day sibling's ruling:
  - 03:03 shipped `OBJ-map-debugger-1` (dead mailto fix) as commit
    `a61eb9d` (PR #21).
  - 07:57 independently rediscovered the *same* dead mailto and shipped a
    *second*, different commit fixing it (`4cac17b`, PR #22) — duplicate
    work, because 07:57's view of `main` didn't include 03:03's fix (never
    merged).
  - 13:12 ruled a SHIP on `OBJ-coord-validator-1` but failed its own gate
    (no commit landed — self-correcting, doesn't count against budget by
    this Council's own precedent).
  - 18:00 shipped `OBJ-map-debugger-5` (interconnector/planned-corridor
    layer-id collision), commit `89f5d74` (PR #24) — a real, distinct,
    valuable fix, but still a 3rd same-day commit against a 1-per-24h rule.
- **The OBJ ID ledger collided across branches.** 03:03 minted
  `OBJ-frontend-engineer-4` for "tooltip viewport clamping"; 18:00
  independently minted the *same* ID for "Escape-to-close the Methodology
  modal" — two different objectives now claim one ID, because §8's "IDs
  are minted once, never renumbered" assumes a single linear Board, which
  breaks the moment sittings branch in parallel without merging.
- **Root cause, confirmed by platform-engineer this sitting** (see below):
  of 24 council-related PRs opened since 2026-08-04, only PR #1 has ever
  merged. `main`'s `docs/**` has been physically unchanged for over a
  week regardless of what any sitting's minutes claim was "shipped."

This is a process/pipeline fact, not a map defect, and outside what any
sitting can fix by ruling — merging (or deliberately closing/reconciling)
PRs is a human action the Council structurally cannot take (COUNCIL.md:
"Never push to main"). I'm recording it plainly here and flagging it to
Reda directly, per usual escalation practice, rather than pretending a
Chair ruling can resolve it. **No renumbering or Board reconciliation
across the colliding branches is attempted in this sitting** — that
requires a human decision about which PRs/objectives survive, which I
should not make unilaterally.

### State established before convening

- `git log` on `origin/main`: unchanged at `9a4d5a4` since 2026-08-04 —
  confirmed independently, not assumed.
- `node --check docs/app.js` / `docs/countries.config.js`: clean. All 9
  `docs/data/morocco/*.geojson` files: valid JSON. Nothing down, nothing
  broken on `main` right now. No rung-1/rung-2 candidate.
- **Today's SHIP budget is spent** — `council/BOARD.md` (as carried
  forward from the 18:00 branch, corrected below) shows a real, gated
  commit already landed today at 18:00 (`OBJ-map-debugger-5`), on top of
  the two duplicate mailto fixes at 03:03/07:57. This sitting is
  **REPORT-only**, full stop, regardless of anything ranked below.

### Docket (six seats convened, each reaffirmed/re-verified against the
current tree; full detail in each seat's Board section)

**frontend-engineer** — reaffirmed all 3, re-verified against current
`docs/app.js`/`docs/style.css`: OBJ-frontend-engineer-2 (loading state),
OBJ-frontend-engineer-3 (topbar overflow ≤375px), OBJ-frontend-engineer-4
(Escape-to-close Methodology modal — this sitting's framing; see ID
collision note above, a human must reconcile against 03:03's differently-
scoped OBJ-frontend-engineer-4 before either ships).

**coord-validator** — reaffirmed all 3: OBJ-coord-validator-1 (vintage
field, industrial.geojson), OBJ-coord-validator-2 (sample-verify
national-hv/transmission-lines — **executed this sitting as the REPORT
slot, see Outcome**), OBJ-coord-validator-3 (deprecate grid-lines.geojson).

**map-debugger** — reaffirmed OBJ-map-debugger-1 (dead mailto — note: a
fix for this already exists as two independent unmerged commits,
`a61eb9d` and `4cac17b`; not re-filing a third), OBJ-map-debugger-4
(light-theme topbar contrast), OBJ-map-debugger-3 (mid-session MapLibre
error state). Confirmed OBJ-map-debugger-5's fix (src-grid collision)
remains correct-but-unmerged on `main`.

**map-tester** — reaffirmed all 3: OBJ-map-tester-1 (orphan-data check),
OBJ-map-tester-2 (GO evidence checklist), OBJ-map-tester-3 (popup field-
mapping audit, sharpened this sitting: confirmed `national-hv.geojson`
and `transmission-lines.geojson` both lack the `precision` key
`openLinePopup()` reads at `app.js:936/985` — currently latent, since
neither layer is wired into the map yet).

**platform-engineer** — reaffirmed OBJ-platform-engineer-1 (CI check —
**executed this sitting as the REPORT slot, see Outcome**) and
OBJ-platform-engineer-2 (uptime check). Proposed a 3rd, new objective
("alert when a shipped PR sits unmerged >24h") — **rejected by the
Chair**, see Ruling.

**security-engineer** — reaffirmed all 3, re-verified live: no
`integrity=` attributes exist, no third-party disclosure text exists, no
`docs/_headers`/CSP exists. OBJ-security-engineer-1/2/3 unchanged.

### Ruling

SHIP:    **none — today's budget is spent.** Even setting aside the
         03:03/07:57 duplicate, 18:00 already shipped
         `OBJ-map-debugger-5` within the current rolling 24h window. This
         sitting rules REPORT-only per COUNCIL.md, and I am additionally
         declining to treat the 03:03/07:57 duplication as "budget
         available because it was a mistake" — the honest state is that
         real, gated commits already exist for today; adding a fourth
         only compounds the reconciliation a human already has to do.

REPORT:  OBJ-coord-validator-2, OBJ-platform-engineer-1 — both executed
         this sitting (no SHIP competing for capacity, so both ran in
         full rather than being deferred like at prior sittings):

         **OBJ-coord-validator-2 result: PASS, 0 FAILs.** Full-file scan
         (not just a sample) of `national-hv.geojson` (947 features) and
         `transmission-lines.geojson` (541 features): 0 out-of-bbox (once
         corrected to the true Morocco-administered bbox, which includes
         Laâyoune/Dakhla — the task's initial narrower bbox produced 90
         false positives there), 0 degenerate/NaN geometries. A 26-feature
         stratified sample per file: 0 implausible segment lengths, 0
         voltage/geometry mismatches. Side-finding (not a FAIL): 5 exact-
         duplicate geometry pairs per file (10 features each) — same
         physical line double-counted under two IDs, likely from OSM
         derivation — flagged for a future dedup pass, not blocking. This
         **unblocks OBJ-map-debugger-2** (L, wiring these 1,488 features
         into the live map) from its coordinate-quality concern; it still
         needs splitting into a shippable S before it can rank for SHIP.

         **OBJ-platform-engineer-1 result: fully scoped, locally verified,
         reclassified M→S-ready.** A complete `.github/workflows/
         docs-ci.yml` was drafted and its logic run locally against the
         real repo (not just theorized) — syntax-checks `app.js`/
         `countries.config.js`, validates all 9 GeoJSON files as
         structurally-correct FeatureCollections, and checks every file
         `index.html`/`countries.config.js`/`app.js` references actually
         exists. Confirmed genuinely S: one new file, no new dependency,
         reversible, touches no product code. Grounding notes recorded
         for whoever ships it: 3 of 9 GeoJSON files are intentionally
         orphaned (expected, not a bug); `oim-grid`'s `file: null` must be
         skipped; `boundary.geojson` must only be checked for *enabled*
         countries (`COUNTRIES_ENABLED`), not all four `COUNTRIES`
         entries. This report is now ready for a future sitting to rule
         SHIP with minimal ambiguity.

Vetoed:  none from a seat. **Chair-rejected (fails the North Star Test,
         not a formal §5 veto):** platform-engineer's proposed 3rd
         objective, an automated alert for council PRs that sit unmerged
         past 24h after a SHIP ruling. Its `unlocks:` line was worded to
         name a regulator/DC developer ("...can trust that a fix the
         Council announced as shipped is actually live...") but the
         actual beneficiary of a merge-backlog alert is the Council's own
         operator, not a regulator or DC developer using the map — they
         never see council minutes or PR state. COUNCIL.md is explicit
         that this is exactly the framing seats sometimes reach for and
         exactly what the Chair should decline. Not minted as an ID; not
         added to the Board. The underlying fact (the merge backlog
         itself) is real and is recorded in this sitting's narrative and
         escalated directly instead.

Docketed: OBJ-frontend-engineer-2, OBJ-frontend-engineer-3,
OBJ-frontend-engineer-4, OBJ-coord-validator-1, OBJ-coord-validator-3,
OBJ-map-debugger-1, OBJ-map-debugger-2, OBJ-map-debugger-3,
OBJ-map-debugger-4, OBJ-map-tester-1, OBJ-map-tester-2, OBJ-map-tester-3,
OBJ-platform-engineer-1 (report delivered, ready for SHIP),
OBJ-platform-engineer-2, OBJ-platform-engineer-3, OBJ-security-engineer-1,
OBJ-security-engineer-2, OBJ-security-engineer-3.
(OBJ-coord-validator-2's report is delivered — see Ruling — so it is no
longer waiting on sitting time; kept on the Board as evidence, not as a
pending pick.)

No new IDs minted this sitting.

### Outcome
Commit:  none — REPORT-only sitting, no SHIP, no product-code change.
         This sitting's own commit contains only `council/BOARD.md` and
         `council/2026-08-12.md` (reconstructed to include all four
         earlier sittings today, previously only visible on their own
         isolated, unmerged branches).
Gates:   n/a — nothing shipped this sitting.
Carried: **For the next sitting:** today's SHIP budget is spent (3
         real commits already, possibly more once a human reconciles the
         03:03/07:57 duplicate) — the next sitting should also expect to
         be REPORT-only unless it falls past the 24h rolling window from
         18:00. OBJ-coord-validator-2 and OBJ-platform-engineer-1 are both
         now report-complete and need no further sitting time — the next
         SHIP-eligible sitting can pick OBJ-platform-engineer-1 (CI check)
         directly off this sitting's YAML draft, or split
         OBJ-map-debugger-2 (now unblocked on data quality) into a
         shippable S.
         **For Reda, outside the Council's own authority:** the 23-PR
         unmerged backlog (PRs #2–#24) needs human triage. At minimum:
         (a) decide between the two duplicate dead-mailto fixes (PR #21
         `a61eb9d` vs PR #22 `4cac17b` — functionally the same change,
         only one should merge, close the other); (b) merge PR #24
         (18:00, the interconnector/planned-corridor fix — real,
         verified, highest-value pending change); (c) resolve the
         OBJ-frontend-engineer-4 ID collision between PR #21's framing
         (tooltip clamping) and this sitting's/PR #24's framing
         (Methodology modal Escape-key) once a decision is made about
         which PRs merge; (d) more broadly, decide whether council PRs
         should start merging on a regular cadence, or the "1 SHIP per
         24h" budget will keep being silently exceeded every time
         multiple sittings land inside one unmerged window.
