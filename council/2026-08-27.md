# Council sitting — 2026-08-27

## Sitting — 12:11 (Europe/Paris)

First sitting of the day, and the first sitting since 2026-08-04 14:15 —
a 23-day gap. The 5-hourly cadence evidently did not fire in between; this
sitting cannot fix that, only note it for the record. `git log` tip is
unchanged since the last sitting (`9a4d5a4`, the merge that landed
`c808b1e`/OBJ-frontend-engineer-1): zero commits under `docs/**` or
`council/**` in the gap. Re-verified independently before ranking anything:
`node --check` on `docs/app.js` and `docs/countries.config.js` both clean;
all 9 `docs/data/morocco/*.geojson` files parse as valid JSON. Nothing is
down, nothing is broken — no rung-1/rung-2 candidate exists this sitting.
No `council/2026-08-27.md` existed before this sitting, so today's SHIP
budget (max 1/24h) was unspent at convening time.

Every seat pulled from `council/BOARD.md` rather than re-deriving from
scratch, per COUNCIL.md §8 — no `docs/**` change happened in the 23-day
gap, so no seat had anything genuinely new to observe. No new OBJ IDs
minted this sitting.

### Docket (18 objectives, grouped by seat)

**frontend-engineer**
- OBJ-frontend-engineer-2 | Visible loading state for `#layerList`/`#kpiGrid` during `loadAllData()` — reaffirmed, unchanged
- OBJ-frontend-engineer-3 | `.topbar` overflow handling at ≤375px — reaffirmed; severity raised this sitting (see Outcome)

**coord-validator**
- OBJ-coord-validator-1 | Add `vintage` to all 13 `industrial.geojson` features — reaffirmed, unchanged
- OBJ-coord-validator-2 | Sample-verify `national-hv.geojson`(947) + `transmission-lines.geojson`(541) — reaffirmed, unchanged, M, blocks OBJ-map-debugger-2
- OBJ-coord-validator-3 | Remove/mark-deprecated `grid-lines.geojson` (11, unreferenced) — reaffirmed, unchanged

**map-debugger**
- OBJ-map-debugger-1 | Fix dead mailto (`index.html:122`, `app.js:968,997`, still `example.com`) — reaffirmed, re-verified live; ranked highest rung-3 candidate, **not executed** (see Ruling)
- OBJ-map-debugger-2 | Wire `national-hv`+`transmission-lines` into the live map — reaffirmed, still L, still blocked on OBJ-coord-validator-2
- OBJ-map-debugger-4 | Fix light-theme topbar contrast in `brand.css` — reaffirmed, risk note corrected (brand.css confirmed independent of `solar/brand.css`), **ruled SHIP, executed**
- OBJ-map-debugger-3 | Visible error state for mid-session MapLibre runtime failures — reaffirmed, unchanged

**map-tester**
- OBJ-map-tester-1 | Orphan-data check across `docs/data/morocco/*.geojson` vs load-path refs — reaffirmed, unchanged
- OBJ-map-tester-2 | Written evidence bar for a release-gate GO — reaffirmed but **flagged**: `unlocks:` names the Chair, not a regulator/DC developer; disqualified from ranking until reworded
- OBJ-map-tester-3 | Audit popup field-name mapping vs source-file keys — reaffirmed, unchanged

**platform-engineer**
- OBJ-platform-engineer-1 | Minimal CI check on push to main — reaffirmed, unchanged, M
- OBJ-platform-engineer-2 | Uptime check against live map URL — reaffirmed, unchanged
- OBJ-platform-engineer-3 | `docs/_headers` cache-control for `docs/data/*.geojson` — reaffirmed, unchanged

**security-engineer**
- OBJ-security-engineer-1 | SRI hashes on MapLibre `<script>`/`<link>` — reaffirmed, unchanged
- OBJ-security-engineer-2 | Disclose third-party requests on load — reaffirmed, unchanged
- OBJ-security-engineer-3 | CSP via `docs/_headers` — reaffirmed, unchanged

### Ruling

SHIP:    OBJ-map-debugger-4 — the Chair (via the `standup-chair` agent)
         initially ranked OBJ-map-debugger-1 (dead mailto) highest, as the
         next-ranked rung-3 item carried forward from 2026-08-04. Executing
         it turned out to require publishing a real, live, publicly-
         scraped contact address in 3 places in `docs/index.html`/`app.js`
         — the only address on file for this account is the repo owner's
         personal one. Publishing a person's private email to a public,
         indexed website is a one-way disclosure decision with real
         consequences (spam, scraping, permanence); it is not something an
         unattended, scheduled sitting may decide on the owner's behalf,
         even for their own address, even though the underlying objective
         (give the audience a working error-report channel) is legitimate
         and correctly ranked. This is a chair/execution-level hold, not a
         seat veto — recorded honestly rather than silently forcing the
         disclosure to fill the slot. OBJ-map-debugger-1 remains Docketed,
         still next-ranked, pending the owner naming or approving a
         publishable address (ideally a role address, not a personal one).
         The SHIP slot rolled to the next fully-actionable rung-4 item
         with no such blocker: OBJ-map-debugger-4, the light-theme topbar
         contrast bug. Unlocks: a regulator or DC developer using light
         mode can actually read the Solaire/Methodology/GitHub/theme-
         toggle buttons — confirmed live, they were rendering white text
         on a white background (`docs/style.css`'s light-theme rule gives
         `.ghost-btn`/`.icon-btn` a white background; `docs/brand.css`'s
         unconditional, later-loaded rule kept the text near-white on the
         cascade tie). S, reversible (one scoped CSS rule addition, no
         layout/logic/data change), no structural or truth veto applies —
         confirmed this sitting that `docs/brand.css` and `solar/brand.css`
         are two independent files, so Atlas Solar is untouched. Today's
         SHIP budget was unspent going in.

REPORT:  OBJ-coord-validator-2, OBJ-platform-engineer-1 — reaffirmed for
         the same reason as the last two sittings, not re-litigated: both
         remain the only M-sized items on the board and both gate a
         future S. Neither has been executed across three consecutive
         sittings now (2026-08-03, 2026-08-04, 2026-08-27) — worth a
         direct note to Reda outside the council process, since a REPORT
         ruling alone doesn't force execution and the gap between
         sittings means these have sat untouched for over three weeks.

Vetoed:  none — no seat surfaced a veto; nothing proposed conflicts with a
         structural or truth veto in COUNCIL.md §5.

Docketed to council/BOARD.md: OBJ-frontend-engineer-2, OBJ-frontend-engineer-3,
OBJ-coord-validator-1, OBJ-coord-validator-2, OBJ-coord-validator-3,
OBJ-map-debugger-1, OBJ-map-debugger-2, OBJ-map-debugger-3,
OBJ-map-tester-1, OBJ-map-tester-2 (flagged, non-compliant unlocks),
OBJ-map-tester-3, OBJ-platform-engineer-1, OBJ-platform-engineer-2,
OBJ-platform-engineer-3, OBJ-security-engineer-1, OBJ-security-engineer-2,
OBJ-security-engineer-3

(No IDs minted this sitting — ledger unchanged: frontend-engineer 4,
coord-validator 4, map-debugger 5, map-tester 4, platform-engineer 4,
security-engineer 4.)

### Outcome
Commit:  see branch `council/2026-08-27-1211` — `docs/brand.css` change
         plus this minutes file and the Board update, committed together.
Gates:   desktop ✓ (Playwright/Chromium against a local static server;
         console carries only pre-existing CDN/proxy-sandbox network
         failures — `unpkg.com` and `fonts.googleapis.com` blocked by this
         environment's outbound proxy — and app.js's own graceful-
         degradation warning reacting to them; nothing references
         `brand.css`/`ghost-btn`/`icon-btn`; confirmed identical with the
         change `git stash`-ed, so none of it is caused by this diff) |
         light ✓ (real computed styles pulled from the live DOM after the
         fix: `color: rgb(24,24,26)` on `background-color: rgb(255,255,255)`
         for all four topbar buttons — contrast ratio 17.73:1, exceeds even
         WCAG AAA's 7:1; screenshot confirms legible dark-on-white text,
         no white-on-white) | dark-theme regression ✓ (computed styles
         after forcing `data-theme="dark"` are byte-identical to the
         pre-existing rule: `color: rgba(255,255,255,.85)`, same background
         and border — the new rule is scoped to `[data-theme="light"]` and
         has zero effect elsewhere) | 375px: no new overflow introduced by
         this diff (confirmed via A/B `git stash` test — see below) | data
         n/a (no `docs/data/**` touched) | secrets: clean diff, no
         credentials, dependencies, or third-party payload touched — a
         CSS color/border rule only | isolation ✓ (`docs/brand.css` and
         `solar/brand.css` confirmed to be two separate, non-symlinked
         files by direct comparison; Atlas Solar's chrome is untouched) |
         map-tester **GO** — quoted verdict: "Browser-level evidence
         (Playwright + Chromium): resting-state contrast 17.73:1 on
         Solaire/Methodology/GitHub/theme-toggle, screenshot confirms
         legible dark-on-white text, dark theme unchanged byte-for-byte,
         solar/brand.css untouched, zero new console errors... GO — for
         the specific docs/brand.css light-theme button-contrast fix under
         review." Not `GO-STATIC` — every claim carries live browser
         evidence.

         One finding surfaced during this sitting's gate-check, disclosed
         rather than folded silently into the "no overflow" claim above:
         map-tester's 375px pass was checked at the `document.documentElement`
         level (375 === 375, because `html`/`body` set `overflow-x:hidden`),
         but the *topbar itself* overflows its own box —
         `document.body.scrollWidth` is 600 at 375px width, and Methodology,
         GitHub, and the theme-toggle button sit off-screen (x = 387–600px)
         with no scrollbar, wrap, or collapse to reach them; only "Solaire"
         is visible. This was independently reproduced with the fix
         `git stash`-ed against the pre-existing baseline commit `9a4d5a4`
         and found byte-identical — it predates this sitting entirely and
         is not caused or fixed by the `brand.css` change. It is exactly
         the problem OBJ-frontend-engineer-3 already describes; its Board
         entry has been updated with this sitting's live confirmation and
         raised severity ("unreachable", not merely "clipped"), not filed
         as a new objective.

Carried: OBJ-map-debugger-1 (dead mailto) is next-ranked for the SHIP slot
         once the owner names or approves a publishable contact address —
         this is a decision for Reda, not something a future unattended
         sitting should attempt to resolve on its own by picking an
         address. OBJ-frontend-engineer-3 (topbar overflow at ≤375px) is
         now confirmed live and more severe than previously described
         (3 of 4 topbar controls fully unreachable, not just visually
         clipped) — a strong S-sized candidate for the next SHIP slot.
         OBJ-coord-validator-2 and OBJ-platform-engineer-1 remain
         unexecuted after three sittings as REPORT-only. OBJ-map-tester-2
         needs its `unlocks:` line reworded to name a regulator or DC
         developer before it can be reaffirmed cleanly. Next sitting's
         SHIP slot is spent for the next ~24h — today's ships out at
         OBJ-map-debugger-4.
