# Council sitting — 2026-08-26

## Sitting — 12:28 (Europe/Paris)

Scheduled, unattended sitting. No `council/2026-08-26.md` existed before this
sitting, so today's SHIP budget (max 1 per rolling 24h) was unspent going in.
`git log` tip at convening time was `9a4d5a4` — no commits to `docs/**` since
the 2026-08-04 sitting other than the merge that landed its already-shipped
`OBJ-frontend-engineer-1`. State check before convening: `node --check` clean
on `docs/app.js` and `docs/countries.config.js`; all 9
`docs/data/morocco/*.geojson` files parse as valid JSON. Nothing broken, no
rung-1/rung-2 candidate. Full detail for every objective lives on
`council/BOARD.md`; this file records the ruling.

All six seats convened, each reading its own Board section first per §8
("pull, don't regenerate") and re-verifying against current source rather
than trusting old text. Five seats reaffirmed their existing Docketed items
unchanged. `frontend-engineer` additionally surfaced one genuinely new
finding (zero keyboard/focus handling on the Methodology modal), minted as
`OBJ-frontend-engineer-4`.

### Docket

**frontend-engineer**
- OBJ-frontend-engineer-2 | Add a visible loading state to `#layerList`/`#kpiGrid` while `loadAllData()` awaits its fetches — reaffirmed, unchanged
- OBJ-frontend-engineer-3 | Give `.topbar` explicit overflow handling at ≤375px — reaffirmed, unchanged
- OBJ-frontend-engineer-4 | Add Escape-to-close and focus management to `#methodologyModal` — **new**, minted this sitting (zero `keydown`/`Escape`/`focus()` handling anywhere in `docs/app.js`, confirmed by direct read)

**coord-validator**
- OBJ-coord-validator-1 | Add `vintage` field to all 13 `industrial.geojson` features — reaffirmed, unchanged
- OBJ-coord-validator-2 | Sample-verify `national-hv.geojson` (947) + `transmission-lines.geojson` (541) — reaffirmed, unchanged
- OBJ-coord-validator-3 | Remove or mark deprecated `grid-lines.geojson` (11 features, unreferenced) — reaffirmed, unchanged

**map-debugger**
- OBJ-map-debugger-1 | Fix dead mailto targets (`index.html:122`, `app.js:968,997`) — reaffirmed, re-verified still broken pre-sitting; **ruled SHIP, executed, shipped this sitting**
- OBJ-map-debugger-4 | Fix light-theme topbar button contrast in `docs/brand.css` — reaffirmed, re-verified still broken (confirmed independently a second time by the executing frontend-engineer while gathering evidence for the mailto fix)
- OBJ-map-debugger-3 | Surface a visible error state for mid-session MapLibre runtime failures — reaffirmed, unchanged
- (OBJ-map-debugger-2, L, wiring `national-hv`/`transmission-lines` in — still Docketed, still blocked on `OBJ-coord-validator-2`; not one of this seat's top 3 this sitting)

**map-tester**
- OBJ-map-tester-1 | Orphan-data check across `docs/data/morocco/*.geojson` vs load-path references — reaffirmed, unchanged (still exactly 3 orphans)
- OBJ-map-tester-2 | Write down the required evidence bar for a release-gate GO — reaffirmed; this sitting's seat holder produced that bar ad hoc for the mailto/contrast candidates (see Outcome), but the standing written-checklist objective itself remains unshipped
- OBJ-map-tester-3 | Audit popup field-name mapping against each source file's actual keys — reaffirmed, unchanged (defect confirmed still dormant — only activates once `OBJ-map-debugger-2` ships)

**platform-engineer**
- OBJ-platform-engineer-1 | Add a minimal CI check on push to `main` (no build step, no bundler) — reaffirmed, unchanged
- OBJ-platform-engineer-2 | Wire an uptime check against the live map URL — reaffirmed, unchanged
- OBJ-platform-engineer-3 | Add `docs/_headers` with cache-control for `docs/data/*.geojson` — reaffirmed, unchanged

**security-engineer**
- OBJ-security-engineer-1 | Add Subresource Integrity hashes to the MapLibre `<script>`/`<link>` tags — reaffirmed, unchanged
- OBJ-security-engineer-2 | Disclose the third-party requests the page makes on load — reaffirmed, evidence tightened (index.html:146 discloses CARTO+OSM only, names 0 of Google Fonts/unpkg)
- OBJ-security-engineer-3 | Add a Content-Security-Policy via `docs/_headers` — reaffirmed, unchanged
- Secrets/dependency sweep: clean — no committed secret, key, or credential found in `docs/**` working tree or last 5 `docs/`-touching commits

### Ruling

SHIP:    OBJ-map-debugger-1 — rung 3 ("the audience cannot make a decision
         it should be able to make"): a regulator or DC developer who spots
         a wrong coordinate or stale figure could not get the correction to
         land, because every "Report an error" link pointed at
         `reda.tahiri@example.com`, an IANA-reserved documentation domain
         (RFC 2606) incapable of receiving mail. No rung-1 (published fact
         wrong) or rung-2 (site down / broken pipeline) candidate existed
         this sitting, so nothing pre-empted the docket. Outranks
         OBJ-map-debugger-4 (rung 4 — "the audience cannot read what is
         already there"), the only other live SHIP candidate. S-sized,
         reversible, pure 3-location string replacement across 2 files, no
         schema/data/logic change. Every seat cleared it: security-engineer
         found no privacy/security objection to publishing the maintainer's
         real address (matches the existing pattern for other contact
         links); platform-engineer found no deploy-risk objection and
         confirmed today's SHIP budget was unspent (no `council/2026-08-26.md`
         existed before this sitting). Carried forward as next-ranked from
         the 2026-08-04 sitting's minutes.

REPORT:  OBJ-coord-validator-2, OBJ-platform-engineer-1 — reaffirmed for the
         same reason as the 2026-08-04 sitting, not re-litigated: both
         remain the only M-sized items on the Board, and both still block a
         future S (coord-validator-2 must resolve before OBJ-map-debugger-2
         can split into anything shippable; platform-engineer-1 needs its
         scope pinned to a pure-validation checklist before it can split
         without tripping the build-step/bundler structural veto). Neither
         has actually been executed yet — REPORT slots are read-only and
         feed the next docket, not this sitting's SHIP.

Vetoed:  none — all six seats reviewed the full docket against COUNCIL.md §5
         structural and truth vetoes and raised none. Every `unlocks:`
         field names a regulator or DC developer decision, not the Council,
         the codebase, or code quality.

Docketed: OBJ-frontend-engineer-2, OBJ-frontend-engineer-3,
OBJ-frontend-engineer-4, OBJ-coord-validator-1, OBJ-coord-validator-2,
OBJ-coord-validator-3, OBJ-map-debugger-2, OBJ-map-debugger-3,
OBJ-map-debugger-4, OBJ-map-tester-1, OBJ-map-tester-2, OBJ-map-tester-3,
OBJ-platform-engineer-1, OBJ-platform-engineer-2, OBJ-platform-engineer-3,
OBJ-security-engineer-1, OBJ-security-engineer-2, OBJ-security-engineer-3

(One ID minted this sitting: `OBJ-frontend-engineer-4`. Ledger updated:
frontend-engineer next → 5. All other ledgers unchanged.)

### Outcome

Commit:  `8fe19d2` — "map: fix dead mailto correction links
         (OBJ-map-debugger-1)". Executed by frontend-engineer after this
         ruling: `reda.tahiri@example.com` → `[principal’s personal address — redacted]` in
         `docs/index.html:122` and `docs/app.js:968,997`. Pure 3-line
         string replacement, no other change (confirmed by `git diff` —
         2 files, 3 insertions, 3 deletions).
Gates:   desktop ✓ (real Playwright/Chromium run against `docs/` — only
         third-party CDN assets substituted via request interception due to
         this sandbox's egress policy blocking unpkg/CARTO/Google Fonts, zero
         repo files touched for the test; console captured, only benign
         SwiftShader/WebGL driver warnings present, zero
         `MoroccoMap|maplibre|Error` matches) | light ✓ (`document.body
         .dataset.theme === "light"` via the real theme-toggle button,
         point-popup mailto re-checked, same corrected href, console clean)
         | 375px ✓ (viewport 375×812, popup link fully in-bounds and
         tappable — `elementFromPoint` at its center resolves to the link
         itself — `scrollWidth === clientWidth === 375`, no horizontal
         overflow, console clean) | data n/a (no `docs/data/**` touched) |
         secrets ✓ (security-engineer: clean diff scan, clean 5-commit
         history scan, no credential found) | map-tester **GO** — quoted
         verdict: "real Chromium ran the actual `docs/index.html` +
         `docs/app.js`... fired genuine click events on real rendered map
         features, and every rendered 'Report an error' link — point
         popup, line popup, light mode, 375px viewport, and the static
         footer link — carries the corrected
         `mailto:[principal’s personal address — redacted]` address with zero
         `console.error`/`pageerror` entries." 6 of 6 checked mailto
         instances corrected (2 dynamic templates × 3 contexts + the static
         footer link); 0 remaining occurrences of the stale address anywhere
         in `docs/`.
Carried: OBJ-map-debugger-1 is now Shipped — see `council/BOARD.md`.
         OBJ-map-debugger-4 (light-theme topbar contrast) is next-ranked
         for the SHIP slot — independently re-confirmed broken twice this
         sitting (once by map-debugger's own re-verification, once
         incidentally by frontend-engineer's screenshot while gathering
         mailto evidence) — but today's SHIP slot is now spent; next
         sitting is REPORT-only regardless of ranking, one SHIP per
         rolling 24h. New this sitting: OBJ-frontend-engineer-4
         (Methodology modal keyboard/focus handling) filed to the Board,
         not fixed here — out of scope, no sweep. map-tester's evidence
         bar for OBJ-map-debugger-4, spelled out this sitting (WCAG AA
         contrast ratio stated numerically, cross-app screenshot check on
         Solar's chrome since `brand.css`'s header claims shared
         ownership) — though platform-engineer separately determined
         `docs/brand.css` and `solar/brand.css` are physically separate
         files, not a shared include, lowering that objective's real risk;
         the Board note should be corrected accordingly next sitting.
