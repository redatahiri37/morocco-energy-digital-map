# Secrets — never commit the Mapbox token
docs/config.js

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*.swo

# Node (if tooling is added later)
node_modules/
npm-debug.log*
yarn-debug.log*

# Python (if data-fetch scripts are added later)
__pycache__/
*.pyc
.venv/
venv/
